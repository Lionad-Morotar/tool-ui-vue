# Plan 04-03: Migrate 7 Medium Components

## Goal

Migrate 7 medium-complexity components from `src/components/` to `packages/components/src/`:
- chart, code-block, data-table, option-list, question-flow, stats-display, video

## Migration Pattern

Same as Plan 04-02:
1. Copy entire directory from `src/components/{name}/` to `packages/components/src/{name}/`
2. Update imports to use `@lionad/core`
3. Fix index.ts barrel exports
4. Update packages/components/src/index.ts

## Components

| Component | Sub-files | Notes |
|-----------|-----------|-------|
| chart | states/ | Chart.js integration |
| code-block | states/ | Shiki syntax highlighting |
| data-table | states/ | Complex table with sorting/filtering |
| option-list | states/ | List selection |
| question-flow | states/ | Multi-step flow |
| stats-display | cmpts/, states/ | Sparkline component |
| video | states/ | Video player |

## Success Criteria

1. All 7 components compile without TypeScript errors
2. All existing tests pass
3. Components importable from `@lionad/components`
