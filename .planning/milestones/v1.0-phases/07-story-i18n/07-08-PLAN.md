---
phase: 07-story-i18n
plan: 08
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/stories/data-table.story.vue
  - src/stories/data-table/index.story.vue
  - src/stories/data-table/i18n/zh.ts
  - src/stories/data-table/i18n/en.ts
  - src/stories/data-table/i18n/index.ts
  - src/stories/stats-display.story.vue
  - src/stories/stats-display/index.story.vue
  - src/stories/stats-display/i18n/zh.ts
  - src/stories/stats-display/i18n/en.ts
  - src/stories/stats-display/i18n/index.ts
  - src/stories/progress-tracker.story.vue
  - src/stories/progress-tracker/index.story.vue
  - src/stories/progress-tracker/i18n/zh.ts
  - src/stories/progress-tracker/i18n/en.ts
  - src/stories/progress-tracker/i18n/index.ts
autonomous: true
requirements:
  - STORY-STRUCT-01
  - STORY-STRUCT-02
  - STORY-STRUCT-03
must_haves:
  truths:
    - data-table, stats-display, and progress-tracker stories live in directory structures
    - Each has extracted i18n/zh.ts and i18n/en.ts files
    - No inline useStoryLocale label objects remain in any of the three stories
  artifacts:
    - path: src/stories/data-table/index.story.vue
      provides: Migrated data-table story
    - path: src/stories/stats-display/index.story.vue
      provides: Migrated stats-display story
    - path: src/stories/progress-tracker/index.story.vue
      provides: Migrated progress-tracker story
  key_links:
    - from: data-table/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
    - from: stats-display/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
    - from: progress-tracker/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
---

<objective>
Migrate data-table, stats-display, and progress-tracker stories into directory-based structures with extracted i18n files.

Purpose: Remove ~123 combined inline useStoryLocale calls across the three stories.
Output: Three new directories under src/stories/ with index.story.vue and i18n files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-story-i18n/07-CONTEXT.md
@src/stories/data-table.story.vue
@src/stories/stats-display.story.vue
@src/stories/progress-tracker.story.vue
@.planning/phases/07-story-i18n/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate data-table</name>
  <files>src/stories/data-table.story.vue, src/stories/data-table/index.story.vue, src/stories/data-table/i18n/zh.ts, src/stories/data-table/i18n/en.ts, src/stories/data-table/i18n/index.ts</files>
  <read_first>src/stories/data-table.story.vue</read_first>
  <action>
    Migrate data-table to `src/stories/data-table/index.story.vue`.

    1. Extract all `useStoryLocale` strings into `src/stories/data-table/i18n/zh.ts` and `i18n/en.ts`. Use namespaces: `variant.*`, `column.*`, `row.*`, `desc.*`, `ui.*`, `content.*`.
    2. Create `i18n/index.ts`.
    3. Rewrite: update import to `'../_shared/use-story-locale'`, add `import messages from './i18n'`, replace inline calls with key-based lookups.
    4. Preserve table data objects and any reactive controls.
    5. Delete old `src/stories/data-table.story.vue`.
  </action>
  <verify>
    <automated>test -f src/stories/data-table/index.story.vue && test ! -f src/stories/data-table.story.vue && ! grep -q "useStoryLocale({" src/stories/data-table/index.story.vue && grep -q "import messages from './i18n'" src/stories/data-table/index.story.vue && grep -q "../_shared/use-story-locale" src/stories/data-table/index.story.vue</automated>
  </verify>
  <acceptance_criteria>
    - `src/stories/data-table/index.story.vue` exists
    - `src/stories/data-table.story.vue` does NOT exist
    - i18n files exist under `src/stories/data-table/i18n/`
    - No `useStoryLocale({` remains in the new file
    - File imports `messages` and `useStoryLocale` from correct paths
  </acceptance_criteria>
  <done>data-table story is fully migrated.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate stats-display</name>
  <files>src/stories/stats-display.story.vue, src/stories/stats-display/index.story.vue, src/stories/stats-display/i18n/zh.ts, src/stories/stats-display/i18n/en.ts, src/stories/stats-display/i18n/index.ts</files>
  <read_first>src/stories/stats-display.story.vue</read_first>
  <action>
    Migrate stats-display to `src/stories/stats-display/index.story.vue`.

    1. Extract all `useStoryLocale` strings into `src/stories/stats-display/i18n/zh.ts` and `i18n/en.ts`. Use namespaces: `variant.*`, `stat.*`, `label.*`, `desc.*`, `ui.*`, `content.*`.
    2. Create `i18n/index.ts`.
    3. Rewrite: update import to `'../_shared/use-story-locale'`, add `import messages from './i18n'`, replace inline calls with key-based lookups.
    4. Preserve any helper functions (like `percentAriaLabel` if present) and reactive data.
    5. Delete old `src/stories/stats-display.story.vue`.
  </action>
  <verify>
    <automated>test -f src/stories/stats-display/index.story.vue && test ! -f src/stories/stats-display.story.vue && ! grep -q "useStoryLocale({" src/stories/stats-display/index.story.vue && grep -q "import messages from './i18n'" src/stories/stats-display/index.story.vue && grep -q "../_shared/use-story-locale" src/stories/stats-display/index.story.vue</automated>
  </verify>
  <acceptance_criteria>
    - `src/stories/stats-display/index.story.vue` exists
    - `src/stories/stats-display.story.vue` does NOT exist
    - i18n files exist under `src/stories/stats-display/i18n/`
    - No `useStoryLocale({` remains in the new file
    - File imports `messages` and `useStoryLocale` from correct paths
  </acceptance_criteria>
  <done>stats-display story is fully migrated.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate progress-tracker</name>
  <files>src/stories/progress-tracker.story.vue, src/stories/progress-tracker/index.story.vue, src/stories/progress-tracker/i18n/zh.ts, src/stories/progress-tracker/i18n/en.ts, src/stories/progress-tracker/i18n/index.ts</files>
  <read_first>src/stories/progress-tracker.story.vue</read_first>
  <action>
    Migrate progress-tracker to `src/stories/progress-tracker/index.story.vue`.

    1. Extract all `useStoryLocale` strings into `src/stories/progress-tracker/i18n/zh.ts` and `i18n/en.ts`. Use namespaces: `variant.*`, `step.*`, `desc.*`, `ui.*`, `content.*`.
    2. Create `i18n/index.ts`.
    3. Rewrite: update import to `'../_shared/use-story-locale'`, add `import messages from './i18n'`, replace inline calls with key-based lookups.
    4. Preserve any reactive step data and control bindings.
    5. Delete old `src/stories/progress-tracker.story.vue`.
  </action>
  <verify>
    <automated>test -f src/stories/progress-tracker/index.story.vue && test ! -f src/stories/progress-tracker.story.vue && ! grep -q "useStoryLocale({" src/stories/progress-tracker/index.story.vue && grep -q "import messages from './i18n'" src/stories/progress-tracker/index.story.vue && grep -q "../_shared/use-story-locale" src/stories/progress-tracker/index.story.vue</automated>
  </verify>
  <acceptance_criteria>
    - `src/stories/progress-tracker/index.story.vue` exists
    - `src/stories/progress-tracker.story.vue` does NOT exist
    - i18n files exist under `src/stories/progress-tracker/i18n/`
    - No `useStoryLocale({` remains in the new file
    - File imports `messages` and `useStoryLocale` from correct paths
  </acceptance_criteria>
  <done>progress-tracker story is fully migrated.</done>
</task>

</tasks>

<verification>
- `pnpm story:dev` shows data-table, stats-display, and progress-tracker variants without errors
- Locale toggle updates all labels and descriptions correctly
</verification>

<success_criteria>
- data-table, stats-display, and progress-tracker directories exist with index.story.vue and i18n files
- Old flat files are removed
- All inline locale labels converted to key-based lookups
</success_criteria>

<output>
After completion, create `.planning/phases/07-story-i18n/07-08-SUMMARY.md`
</output>
