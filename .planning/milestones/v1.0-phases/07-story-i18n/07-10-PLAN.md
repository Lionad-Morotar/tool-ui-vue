---
phase: 07-story-i18n
plan: 10
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/stories/terminal.story.vue
  - src/stories/terminal/index.story.vue
  - src/stories/terminal/i18n/zh.ts
  - src/stories/terminal/i18n/en.ts
  - src/stories/terminal/i18n/index.ts
  - src/stories/image-gallery.story.vue
  - src/stories/image-gallery/index.story.vue
  - src/stories/image-gallery/i18n/zh.ts
  - src/stories/image-gallery/i18n/en.ts
  - src/stories/image-gallery/i18n/index.ts
  - src/stories/video.story.vue
  - src/stories/video/index.story.vue
  - src/stories/video/i18n/zh.ts
  - src/stories/video/i18n/en.ts
  - src/stories/video/i18n/index.ts
autonomous: true
requirements:
  - STORY-STRUCT-01
  - STORY-STRUCT-02
  - STORY-STRUCT-03
must_haves:
  truths:
    - terminal, image-gallery, and video stories live in directory structures
    - Each has extracted i18n/zh.ts and i18n/en.ts files
    - No inline useStoryLocale label objects remain in any of the three stories
  artifacts:
    - path: src/stories/terminal/index.story.vue
      provides: Migrated terminal story
    - path: src/stories/image-gallery/index.story.vue
      provides: Migrated image-gallery story
    - path: src/stories/video/index.story.vue
      provides: Migrated video story
  key_links:
    - from: terminal/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
    - from: image-gallery/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
    - from: video/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
---

<objective>
Migrate terminal, image-gallery, and video stories into directory-based structures with extracted i18n files.

Purpose: Remove ~93 combined inline useStoryLocale calls across the three stories.
Output: Three new directories under src/stories/ with index.story.vue and i18n files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-story-i18n/07-CONTEXT.md
@src/stories/terminal.story.vue
@src/stories/image-gallery.story.vue
@src/stories/video.story.vue
@.planning/phases/07-story-i18n/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate terminal</name>
  <files>src/stories/terminal.story.vue, src/stories/terminal/index.story.vue, src/stories/terminal/i18n/zh.ts, src/stories/terminal/i18n/en.ts, src/stories/terminal/i18n/index.ts</files>
  <read_first>src/stories/terminal.story.vue</read_first>
  <action>
    Migrate terminal to `src/stories/terminal/index.story.vue`.

    1. Extract all `useStoryLocale` strings into `src/stories/terminal/i18n/zh.ts` and `i18n/en.ts`. Use namespaces: `variant.*`, `command.*`, `desc.*`, `ui.*`, `content.*`.
    2. Create `i18n/index.ts`.
    3. Rewrite: update import to `'../_shared/use-story-locale'`, add `import messages from './i18n'`, replace inline calls with key-based lookups.
    4. Preserve terminal command strings and any reactive controls.
    5. Delete old `src/stories/terminal.story.vue`.
  </action>
  <verify>
    <automated>test -f src/stories/terminal/index.story.vue && test ! -f src/stories/terminal.story.vue && ! grep -q "useStoryLocale({" src/stories/terminal/index.story.vue && grep -q "import messages from './i18n'" src/stories/terminal/index.story.vue && grep -q "../_shared/use-story-locale" src/stories/terminal/index.story.vue</automated>
  </verify>
  <acceptance_criteria>
    - `src/stories/terminal/index.story.vue` exists
    - `src/stories/terminal.story.vue` does NOT exist
    - i18n files exist under `src/stories/terminal/i18n/`
    - No `useStoryLocale({` remains in the new file
    - Correct import paths for `messages` and `useStoryLocale`
  </acceptance_criteria>
  <done>terminal story is fully migrated.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate image-gallery</name>
  <files>src/stories/image-gallery.story.vue, src/stories/image-gallery/index.story.vue, src/stories/image-gallery/i18n/zh.ts, src/stories/image-gallery/i18n/en.ts, src/stories/image-gallery/i18n/index.ts</files>
  <read_first>src/stories/image-gallery.story.vue</read_first>
  <action>
    Migrate image-gallery to `src/stories/image-gallery/index.story.vue`.

    1. Extract all `useStoryLocale` strings into `src/stories/image-gallery/i18n/zh.ts` and `i18n/en.ts`. Use namespaces: `variant.*`, `image.*`, `desc.*`, `ui.*`, `content.*`.
    2. Create `i18n/index.ts`.
    3. Rewrite: update import to `'../_shared/use-story-locale'`, add `import messages from './i18n'`, replace inline calls with key-based lookups.
    4. Preserve image data arrays and `watch(currentLocale, ...)` logic if present.
    5. Delete old `src/stories/image-gallery.story.vue`.
    
    Note: image-gallery was fixed in Phase 6 for Variant titles. Ensure all `useStoryLocale` calls are extracted.
  </action>
  <verify>
    <automated>test -f src/stories/image-gallery/index.story.vue && test ! -f src/stories/image-gallery.story.vue && ! grep -q "useStoryLocale({" src/stories/image-gallery/index.story.vue && grep -q "import messages from './i18n'" src/stories/image-gallery/index.story.vue && grep -q "../_shared/use-story-locale" src/stories/image-gallery/index.story.vue</automated>
  </verify>
  <acceptance_criteria>
    - `src/stories/image-gallery/index.story.vue` exists
    - `src/stories/image-gallery.story.vue` does NOT exist
    - i18n files exist under `src/stories/image-gallery/i18n/`
    - No `useStoryLocale({` remains in the new file
    - Correct import paths for `messages` and `useStoryLocale`
  </acceptance_criteria>
  <done>image-gallery story is fully migrated.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate video</name>
  <files>src/stories/video.story.vue, src/stories/video/index.story.vue, src/stories/video/i18n/zh.ts, src/stories/video/i18n/en.ts, src/stories/video/i18n/index.ts</files>
  <read_first>src/stories/video.story.vue</read_first>
  <action>
    Migrate video to `src/stories/video/index.story.vue`.

    1. Extract all `useStoryLocale` strings into `src/stories/video/i18n/zh.ts` and `i18n/en.ts`. Use namespaces: `variant.*`, `video.*`, `desc.*`, `ui.*`, `content.*`.
    2. Create `i18n/index.ts`.
    3. Rewrite: update import to `'../_shared/use-story-locale'`, add `import messages from './i18n'`, replace inline calls with key-based lookups.
    4. Preserve any reactive video states and control bindings.
    5. Delete old `src/stories/video.story.vue`.
  </action>
  <verify>
    <automated>test -f src/stories/video/index.story.vue && test ! -f src/stories/video.story.vue && ! grep -q "useStoryLocale({" src/stories/video/index.story.vue && grep -q "import messages from './i18n'" src/stories/video/index.story.vue && grep -q "../_shared/use-story-locale" src/stories/video/index.story.vue</automated>
  </verify>
  <acceptance_criteria>
    - `src/stories/video/index.story.vue` exists
    - `src/stories/video.story.vue` does NOT exist
    - i18n files exist under `src/stories/video/i18n/`
    - No `useStoryLocale({` remains in the new file
    - Correct import paths for `messages` and `useStoryLocale`
  </acceptance_criteria>
  <done>video story is fully migrated.</done>
</task>

</tasks>

<verification>
- `pnpm story:dev` shows terminal, image-gallery, and video variants without errors
- Locale toggle updates all labels correctly
</verification>

<success_criteria>
- terminal, image-gallery, and video directories exist with index.story.vue and i18n files
- Old flat files are removed
- All inline locale labels converted to key-based lookups
</success_criteria>

<output>
After completion, create `.planning/phases/07-story-i18n/07-10-SUMMARY.md`
</output>
