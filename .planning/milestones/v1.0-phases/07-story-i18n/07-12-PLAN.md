---
phase: 07-story-i18n
plan: 12
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/stories/option-list.story.vue
  - src/stories/option-list/index.story.vue
  - src/stories/option-list/i18n/zh.ts
  - src/stories/option-list/i18n/en.ts
  - src/stories/option-list/i18n/index.ts
  - src/stories/x-post.story.vue
  - src/stories/x-post/index.story.vue
  - src/stories/x-post/i18n/zh.ts
  - src/stories/x-post/i18n/en.ts
  - src/stories/x-post/i18n/index.ts
  - src/stories/linkedin-post.story.vue
  - src/stories/linkedin-post/index.story.vue
  - src/stories/linkedin-post/i18n/zh.ts
  - src/stories/linkedin-post/i18n/en.ts
  - src/stories/linkedin-post/i18n/index.ts
  - src/stories/instagram-post.story.vue
  - src/stories/instagram-post/index.story.vue
  - src/stories/instagram-post/i18n/zh.ts
  - src/stories/instagram-post/i18n/en.ts
  - src/stories/instagram-post/i18n/index.ts
  - src/stories/landing.story.vue
  - src/stories/landing/index.story.vue
  - src/stories/landing/i18n/zh.ts
  - src/stories/landing/i18n/en.ts
  - src/stories/landing/i18n/index.ts
  - src/stories/_shared/histoire-setup.ts
autonomous: true
requirements:
  - STORY-STRUCT-01
  - STORY-STRUCT-02
  - STORY-STRUCT-03
  - STORY-STRUCT-04
must_haves:
  truths:
    - option-list, x-post, linkedin-post, instagram-post, and landing stories live in directory structures
    - Each has extracted i18n/zh.ts and i18n/en.ts files
    - No inline useStoryLocale label objects remain in any of the five stories
    - landing.story.vue hrefs point to new directory-based Histoire story paths
    - Histoire site groups all migrated stories correctly and locale switching works
  artifacts:
    - path: src/stories/option-list/index.story.vue
      provides: Migrated option-list story
    - path: src/stories/x-post/index.story.vue
      provides: Migrated x-post story
    - path: src/stories/linkedin-post/index.story.vue
      provides: Migrated linkedin-post story
    - path: src/stories/instagram-post/index.story.vue
      provides: Migrated instagram-post story
    - path: src/stories/landing/index.story.vue
      provides: Migrated landing story with updated hrefs
  key_links:
    - from: landing/index.story.vue
      to: other story directories
      via: href="#/story/src-stories-{name}-index-story-vue"
    - from: histoire.config.ts tree.include
      to: all migrated story directories
      via: file.path.includes('/{story}/index.story.vue')
---

<objective>
Migrate the remaining low-density stories (option-list, x-post, linkedin-post, instagram-post) and the landing page. Update landing hrefs to match the new directory-based Histoire story paths. Perform final phase-wide verification.

Purpose: Complete the migration of all 28 stories and ensure Histoire navigation and locale switching work end-to-end.
Output: All stories in directory structures, landing page links updated, and verified Histoire functionality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-story-i18n/07-CONTEXT.md
@src/stories/option-list.story.vue
@src/stories/x-post.story.vue
@src/stories/linkedin-post.story.vue
@src/stories/instagram-post.story.vue
@src/stories/landing.story.vue
@src/stories/_shared/histoire-setup.ts
@.planning/phases/07-story-i18n/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate option-list and social post stories (x-post, linkedin-post, instagram-post)</name>
  <files>src/stories/option-list.story.vue, src/stories/option-list/index.story.vue, src/stories/option-list/i18n/zh.ts, src/stories/option-list/i18n/en.ts, src/stories/option-list/i18n/index.ts, src/stories/x-post.story.vue, src/stories/x-post/index.story.vue, src/stories/x-post/i18n/zh.ts, src/stories/x-post/i18n/en.ts, src/stories/x-post/i18n/index.ts, src/stories/linkedin-post.story.vue, src/stories/linkedin-post/index.story.vue, src/stories/linkedin-post/i18n/zh.ts, src/stories/linkedin-post/i18n/en.ts, src/stories/linkedin-post/i18n/index.ts, src/stories/instagram-post.story.vue, src/stories/instagram-post/index.story.vue, src/stories/instagram-post/i18n/zh.ts, src/stories/instagram-post/i18n/en.ts, src/stories/instagram-post/i18n/index.ts</files>
  <read_first>src/stories/option-list.story.vue, src/stories/x-post.story.vue, src/stories/linkedin-post.story.vue, src/stories/instagram-post.story.vue</read_first>
  <action>
    Migrate option-list, x-post, linkedin-post, and instagram-post to their respective directory structures.

    For each story:
    1. Extract all `useStoryLocale` strings into `i18n/zh.ts` and `i18n/en.ts` with appropriate namespaces (`variant.*`, `post.*`, `desc.*`, `ui.*`, `content.*`).
    2. Create `i18n/index.ts` exporting `{ zh, en }`.
    3. Rewrite: update `use-story-locale` import to `'../_shared/use-story-locale'`, add `import messages from './i18n'`, replace inline calls with `useStoryLocale('namespace.key', messages)`.
    4. Preserve reactive data, controls, and event handlers.
    5. Delete old flat `.story.vue` files.
  </action>
  <verify>
    <automated>for name in option-list x-post linkedin-post instagram-post; do test -f "src/stories/$name/index.story.vue" || exit 1; test ! -f "src/stories/$name.story.vue" || exit 1; grep -q "useStoryLocale({" "src/stories/$name/index.story.vue" && exit 1; grep -q "import messages from './i18n'" "src/stories/$name/index.story.vue" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - All four `src/stories/{name}/index.story.vue` files exist
    - Old flat files do NOT exist
    - i18n files exist under all four directories
    - No `useStoryLocale({` remains in any new file
    - All files import `messages` from `'./i18n'` and `useStoryLocale` from `'../_shared/use-story-locale'`
  </acceptance_criteria>
  <done>option-list, x-post, linkedin-post, and instagram-post stories are fully migrated.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate landing story and update all internal hrefs</name>
  <files>src/stories/landing.story.vue, src/stories/landing/index.story.vue, src/stories/landing/i18n/zh.ts, src/stories/landing/i18n/en.ts, src/stories/landing/i18n/index.ts</files>
  <read_first>src/stories/landing.story.vue</read_first>
  <action>
    Migrate `src/stories/landing.story.vue` to `src/stories/landing/index.story.vue` and update all internal story hrefs to use the new directory-based Histoire paths.

    1. Extract all `useStoryLocale` strings into `src/stories/landing/i18n/zh.ts` and `i18n/en.ts`. Use namespaces: `hero.*`, `category.*`, `footer.*`, `content.*`. For the `categories` array, keep the `{ zh, en }` object structure for titles but move them into the i18n files (e.g. under `category.titles.dataDisplay = { zh: '数据展示', en: 'Data Display' }`), then import and reference them in the story.
    2. Create `i18n/index.ts` exporting `{ zh, en }`.
    3. Rewrite the story: update `use-story-locale` import to `'../_shared/use-story-locale'`, add `import messages from './i18n'`, replace inline `useStoryLocale({ zh, en })` calls with key-based lookups.
    4. Update ALL internal hrefs in the `categories` array and any `<a>` tags from the old pattern `#/story/src-stories-{name}-story-vue` to the new pattern `#/story/src-stories-{name}-index-story-vue`. The names to update are: chart, code-block, audio, approval-card, option-list, geo-map. Also update the code-block CTA link in the hero section.
    5. Delete old `src/stories/landing.story.vue`.
  </action>
  <verify>
    <automated>test -f src/stories/landing/index.story.vue && test ! -f src/stories/landing.story.vue && ! grep -q "useStoryLocale({" src/stories/landing/index.story.vue && grep -q "import messages from './i18n'" src/stories/landing/index.story.vue && ! grep -q "story-vue\"" src/stories/landing/index.story.vue && grep -q "index-story-vue" src/stories/landing/index.story.vue</automated>
  </verify>
  <acceptance_criteria>
    - `src/stories/landing/index.story.vue` exists
    - `src/stories/landing.story.vue` does NOT exist
    - i18n files exist under `src/stories/landing/i18n/`
    - No `useStoryLocale({` remains in the new file
    - No old `-story-vue"` href patterns remain (all updated to `-index-story-vue`)
    - File imports `messages` and `useStoryLocale` from correct paths
  </acceptance_criteria>
  <done>landing story is fully migrated with all internal links pointing to new Histoire paths.</done>
</task>

<task type="auto">
  <name>Task 3: Final phase-wide verification</name>
  <files>src/stories/*</files>
  <read_first>histoire.config.ts, src/stories/_shared/histoire-setup.ts</read_first>
  <action>
    Perform comprehensive verification that the entire Phase 7 migration is complete and functional.

    1. Run the verification command `ls src/stories/*.story.vue` and confirm that ONLY `tailwind-test.story.vue` remains (all other 28 flat story files should be deleted).
    2. Confirm that all 28 migrated directories exist with `index.story.vue` and i18n files by running: `for d in src/stories/*/; do test -f "$d"index.story.vue || continue; test -f "$d"i18n/zh.ts || echo "missing $d i18n/zh.ts"; test -f "$d"i18n/en.ts || echo "missing $d i18n/en.ts"; done`
    3. Confirm that no migrated story still contains inline `useStoryLocale({` calls: `grep -r "useStoryLocale({" src/stories/*/index.story.vue` should return zero results.
    4. Confirm that `histoire.config.ts` tree.include contains the updated path matching.
    5. Run `pnpm story:dev` and verify the build starts without errors.
    6. Verify Histoire grouping by confirming stories appear in their respective groups (e.g. link-preview under Social, item-carousel under Media) rather than "Uncategorized".
    7. Test locale toggle: switch locale and confirm at least 3 migrated stories (link-preview, item-carousel, geo-map) update their Variant titles dynamically.
  </action>
  <verify>
    <automated>ls src/stories/*.story.vue | wc -l | grep -q "^1$" && test -f src/stories/tailwind-test.story.vue && ! grep -rl "useStoryLocale({" src/stories/*/index.story.vue 2>/dev/null | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - Only one flat `.story.vue` file remains in `src/stories/` (tailwind-test.story.vue)
    - All 28 migrated directories have `index.story.vue`, `i18n/zh.ts`, and `i18n/en.ts`
    - Zero `useStoryLocale({` patterns across all `src/stories/*/index.story.vue` files
    - `pnpm story:dev` builds without errors
    - Histoire groups stories correctly (no mass migration to Uncategorized)
    - Locale toggle updates Variant titles dynamically in verified stories
  </acceptance_criteria>
  <done>Phase 7 migration is fully verified and complete.</done>
</task>

</tasks>

<verification>
- `pnpm story:dev` starts successfully
- All 28 stories appear in correct Histoire groups
- Locale switching works across verified stories
- Landing page links navigate correctly to new story paths
</verification>

<success_criteria>
- All remaining stories (option-list, x-post, linkedin-post, instagram-post, landing) are migrated
- Landing page hrefs use the new `index-story-vue` path format
- Only tailwind-test.story.vue remains as a flat file
- Histoire builds and functions correctly with all migrated stories
</success_criteria>

<output>
After completion, create `.planning/phases/07-story-i18n/07-12-SUMMARY.md`
</output>
