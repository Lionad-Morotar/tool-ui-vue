---
phase: 07-story-i18n
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/stories/question-flow.story.vue
  - src/stories/question-flow/index.story.vue
  - src/stories/question-flow/i18n/zh.ts
  - src/stories/question-flow/i18n/en.ts
  - src/stories/question-flow/i18n/index.ts
autonomous: true
requirements:
  - STORY-STRUCT-01
  - STORY-STRUCT-02
  - STORY-STRUCT-03
must_haves:
  truths:
    - question-flow story lives in src/stories/question-flow/index.story.vue
    - question-flow has extracted i18n/zh.ts and i18n/en.ts
    - No inline useStoryLocale label objects remain in question-flow
  artifacts:
    - path: src/stories/question-flow/index.story.vue
      provides: Migrated question-flow story
    - path: src/stories/question-flow/i18n/zh.ts
      provides: Chinese translations
    - path: src/stories/question-flow/i18n/en.ts
      provides: English translations
  key_links:
    - from: question-flow/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
---

<objective>
Migrate the question-flow story to a directory-based structure and extract all ~76 inline useStoryLocale calls into namespaced i18n files.

Purpose: Remove dense inline locale labels and make the story maintainable.
Output: src/stories/question-flow/ directory with index.story.vue and i18n files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-story-i18n/07-CONTEXT.md
@src/stories/question-flow.story.vue
@.planning/phases/07-story-i18n/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract question-flow translations into i18n files</name>
  <files>src/stories/question-flow/i18n/zh.ts, src/stories/question-flow/i18n/en.ts, src/stories/question-flow/i18n/index.ts</files>
  <read_first>src/stories/question-flow.story.vue</read_first>
  <action>
    Extract all strings from `useStoryLocale({ zh, en })` in `src/stories/question-flow.story.vue` into `src/stories/question-flow/i18n/` with these namespaces:
    - `variant.*` for Variant titles
    - `step.*` for step labels or question text
    - `option.*` for answer options or choices
    - `desc.*` for descriptions or helper text
    - `ui.*` for button or action labels
    - `content.*` for any remaining body text

    Create `zh.ts`, `en.ts`, and `index.ts` (exporting `{ zh, en }`).
  </action>
  <verify>
    <automated>test -f src/stories/question-flow/i18n/zh.ts && test -f src/stories/question-flow/i18n/en.ts && test -f src/stories/question-flow/i18n/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three i18n files exist under `src/stories/question-flow/i18n/`
    - zh.ts and en.ts have identical key structures
    - No `useStoryLocale` references inside i18n files
  </acceptance_criteria>
  <done>question-flow translation files are created with all strings extracted.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite question-flow story and move to new directory</name>
  <files>src/stories/question-flow/index.story.vue, src/stories/question-flow.story.vue</files>
  <read_first>src/stories/question-flow.story.vue</read_first>
  <action>
    Create `src/stories/question-flow/index.story.vue` by converting all inline `useStoryLocale({ zh, en })` calls to key-based lookups.

    1. Change `use-story-locale` import to `'../_shared/use-story-locale'`.
    2. Add `import messages from './i18n'`.
    3. Replace each inline call with `useStoryLocale('namespace.key', messages)`.
    4. Preserve all reactive state, functions, and event handlers exactly.
    5. Delete `src/stories/question-flow.story.vue`.
  </action>
  <verify>
    <automated>test -f src/stories/question-flow/index.story.vue && test ! -f src/stories/question-flow.story.vue && ! grep -q "useStoryLocale({" src/stories/question-flow/index.story.vue && grep -q "import messages from './i18n'" src/stories/question-flow/index.story.vue && grep -q "../_shared/use-story-locale" src/stories/question-flow/index.story.vue</automated>
  </verify>
  <acceptance_criteria>
    - `src/stories/question-flow/index.story.vue` exists
    - `src/stories/question-flow.story.vue` does NOT exist
    - No `useStoryLocale({` remains in the new file
    - File imports `messages` from `'./i18n'`
    - File imports `useStoryLocale` from `'../_shared/use-story-locale'`
  </acceptance_criteria>
  <done>question-flow story is fully migrated with key-based locale lookups.</done>
</task>

</tasks>

<verification>
- `pnpm story:dev` shows question-flow variants without errors
- Locale toggle updates all question text and option labels correctly
</verification>

<success_criteria>
- question-flow directory exists with index.story.vue and i18n files
- Old flat file is removed
- All inline locale labels converted to key-based lookups
</success_criteria>

<output>
After completion, create `.planning/phases/07-story-i18n/07-04-SUMMARY.md`
</output>
