---
phase: 07-story-i18n
plan: 07
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/stories/weather-widget.story.vue
  - src/stories/weather-widget/index.story.vue
  - src/stories/weather-widget/i18n/zh.ts
  - src/stories/weather-widget/i18n/en.ts
  - src/stories/weather-widget/i18n/index.ts
  - src/stories/plan.story.vue
  - src/stories/plan/index.story.vue
  - src/stories/plan/i18n/zh.ts
  - src/stories/plan/i18n/en.ts
  - src/stories/plan/i18n/index.ts
autonomous: true
requirements:
  - STORY-STRUCT-01
  - STORY-STRUCT-02
  - STORY-STRUCT-03
must_haves:
  truths:
    - weather-widget and plan stories live in their respective directory structures
    - Both have extracted i18n/zh.ts and i18n/en.ts files
    - No inline useStoryLocale label objects remain in either story
  artifacts:
    - path: src/stories/weather-widget/index.story.vue
      provides: Migrated weather-widget story
    - path: src/stories/plan/index.story.vue
      provides: Migrated plan story
  key_links:
    - from: weather-widget/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
    - from: plan/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
---

<objective>
Migrate the weather-widget and plan stories into directory-based structures with extracted i18n files.

Purpose: Remove ~100 combined inline useStoryLocale calls across both stories.
Output: src/stories/weather-widget/ and src/stories/plan/ directories with index.story.vue and i18n files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-story-i18n/07-CONTEXT.md
@src/stories/weather-widget.story.vue
@src/stories/plan.story.vue
@.planning/phases/07-story-i18n/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate weather-widget</name>
  <files>src/stories/weather-widget.story.vue, src/stories/weather-widget/index.story.vue, src/stories/weather-widget/i18n/zh.ts, src/stories/weather-widget/i18n/en.ts, src/stories/weather-widget/i18n/index.ts</files>
  <read_first>src/stories/weather-widget.story.vue</read_first>
  <action>
    Migrate `src/stories/weather-widget.story.vue` to `src/stories/weather-widget/index.story.vue`.

    1. Extract all `useStoryLocale({ zh, en })` strings into `src/stories/weather-widget/i18n/zh.ts` and `i18n/en.ts`. Use namespaces like `variant.*`, `condition.*` (weather conditions), `data.*`, `desc.*`, `ui.*`, and `content.*`.
    2. Create `i18n/index.ts` exporting `{ zh, en }`.
    3. Rewrite the story: update `use-story-locale` import to `'../_shared/use-story-locale'`, add `import messages from './i18n'`, and replace all inline calls with `useStoryLocale('namespace.key', messages)`.
    4. Preserve reactive weather data objects and `watch(currentLocale, ...)` logic.
    5. Delete the old `src/stories/weather-widget.story.vue`.
  </action>
  <verify>
    <automated>test -f src/stories/weather-widget/index.story.vue && test ! -f src/stories/weather-widget.story.vue && ! grep -q "useStoryLocale({" src/stories/weather-widget/index.story.vue && grep -q "import messages from './i18n'" src/stories/weather-widget/index.story.vue && grep -q "../_shared/use-story-locale" src/stories/weather-widget/index.story.vue</automated>
  </verify>
  <acceptance_criteria>
    - `src/stories/weather-widget/index.story.vue` exists
    - `src/stories/weather-widget.story.vue` does NOT exist
    - i18n files exist under `src/stories/weather-widget/i18n/`
    - No `useStoryLocale({` remains in the new file
    - File imports `messages` from `'./i18n'` and `useStoryLocale` from `'../_shared/use-story-locale'`
  </acceptance_criteria>
  <done>weather-widget story is fully migrated.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate plan</name>
  <files>src/stories/plan.story.vue, src/stories/plan/index.story.vue, src/stories/plan/i18n/zh.ts, src/stories/plan/i18n/en.ts, src/stories/plan/i18n/index.ts</files>
  <read_first>src/stories/plan.story.vue</read_first>
  <action>
    Migrate `src/stories/plan.story.vue` to `src/stories/plan/index.story.vue`.

    1. Extract all `useStoryLocale({ zh, en })` strings into `src/stories/plan/i18n/zh.ts` and `i18n/en.ts`. Use namespaces like `variant.*`, `step.*`, `feature.*`, `desc.*`, `ui.*`, and `content.*`.
    2. Create `i18n/index.ts` exporting `{ zh, en }`.
    3. Rewrite the story: update `use-story-locale` import to `'../_shared/use-story-locale'`, add `import messages from './i18n'`, and replace all inline calls with `useStoryLocale('namespace.key', messages)`.
    4. Preserve any reactive plan data and control bindings.
    5. Delete the old `src/stories/plan.story.vue`.
  </action>
  <verify>
    <automated>test -f src/stories/plan/index.story.vue && test ! -f src/stories/plan.story.vue && ! grep -q "useStoryLocale({" src/stories/plan/index.story.vue && grep -q "import messages from './i18n'" src/stories/plan/index.story.vue && grep -q "../_shared/use-story-locale" src/stories/plan/index.story.vue</automated>
  </verify>
  <acceptance_criteria>
    - `src/stories/plan/index.story.vue` exists
    - `src/stories/plan.story.vue` does NOT exist
    - i18n files exist under `src/stories/plan/i18n/`
    - No `useStoryLocale({` remains in the new file
    - File imports `messages` from `'./i18n'` and `useStoryLocale` from `'../_shared/use-story-locale'`
  </acceptance_criteria>
  <done>plan story is fully migrated.</done>
</task>

</tasks>

<verification>
- `pnpm story:dev` shows weather-widget and plan variants without errors
- Locale toggle updates all weather conditions and plan step labels correctly
</verification>

<success_criteria>
- weather-widget and plan directories exist with index.story.vue and i18n files
- Old flat files are removed
- All inline locale labels converted to key-based lookups
</success_criteria>

<output>
After completion, create `.planning/phases/07-story-i18n/07-07-SUMMARY.md`
</output>
