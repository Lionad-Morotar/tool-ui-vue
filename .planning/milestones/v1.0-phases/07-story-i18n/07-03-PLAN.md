---
phase: 07-story-i18n
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/stories/geo-map.story.vue
  - src/stories/geo-map/index.story.vue
  - src/stories/geo-map/i18n/zh.ts
  - src/stories/geo-map/i18n/en.ts
  - src/stories/geo-map/i18n/index.ts
autonomous: true
requirements:
  - STORY-STRUCT-01
  - STORY-STRUCT-02
  - STORY-STRUCT-03
must_haves:
  truths:
    - geo-map story lives in src/stories/geo-map/index.story.vue
    - geo-map has extracted i18n/zh.ts and i18n/en.ts
    - No inline useStoryLocale label objects remain in geo-map
  artifacts:
    - path: src/stories/geo-map/index.story.vue
      provides: Migrated geo-map story
    - path: src/stories/geo-map/i18n/zh.ts
      provides: Chinese translations
    - path: src/stories/geo-map/i18n/en.ts
      provides: English translations
  key_links:
    - from: geo-map/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
---

<objective>
Migrate the geo-map story into a directory-based structure and extract all ~73 inline useStoryLocale calls into namespaced i18n files.

Purpose: Eliminate dense inline locale labels and improve maintainability.
Output: src/stories/geo-map/ directory with index.story.vue and i18n files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-story-i18n/07-CONTEXT.md
@src/stories/geo-map.story.vue
@.planning/phases/07-story-i18n/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract geo-map translations into i18n files</name>
  <files>src/stories/geo-map/i18n/zh.ts, src/stories/geo-map/i18n/en.ts, src/stories/geo-map/i18n/index.ts</files>
  <read_first>src/stories/geo-map.story.vue</read_first>
  <action>
    Extract all strings from `useStoryLocale({ zh, en })` calls in `src/stories/geo-map.story.vue` into `src/stories/geo-map/i18n/`.

    Use these namespaces:
    - `variant.*` for all `<Variant :title="...">` values
    - `marker.*` for marker labels (sfLabel, laLabel, sdLabel, etc.)
    - `route.*` for route labels (startLabel, stop1Label, mainRouteLabel, etc.)
    - `desc.*` for variant descriptions (singleMarkerDesc, multipleMarkersDesc, etc.)
    - `ui.*` for interactive UI text (selectedMarkerText, clearButtonText, etc.)
    - `content.*` for any remaining body text

    Create `src/stories/geo-map/i18n/zh.ts`, `src/stories/geo-map/i18n/en.ts`, and `src/stories/geo-map/i18n/index.ts` exporting `{ zh, en }`.
  </action>
  <verify>
    <automated>test -f src/stories/geo-map/i18n/zh.ts && test -f src/stories/geo-map/i18n/en.ts && test -f src/stories/geo-map/i18n/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three i18n files exist under `src/stories/geo-map/i18n/`
    - zh.ts and en.ts have identical key structures
    - No `useStoryLocale` references inside i18n files
  </acceptance_criteria>
  <done>geo-map translation files are created with all strings extracted.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite geo-map story and move to new directory</name>
  <files>src/stories/geo-map/index.story.vue, src/stories/geo-map.story.vue</files>
  <read_first>src/stories/geo-map.story.vue</read_first>
  <action>
    Create `src/stories/geo-map/index.story.vue` by converting all inline `useStoryLocale({ zh, en })` calls to key-based lookups.

    1. Update import of `use-story-locale` to `'../_shared/use-story-locale'`.
    2. Add `import messages from './i18n'`.
    3. Replace each inline call with `useStoryLocale('namespace.key', messages)`.
    4. Preserve reactive state (`mapState`, event handlers) exactly.
    5. Delete `src/stories/geo-map.story.vue`.
  </action>
  <verify>
    <automated>test -f src/stories/geo-map/index.story.vue && test ! -f src/stories/geo-map.story.vue && ! grep -q "useStoryLocale({" src/stories/geo-map/index.story.vue && grep -q "import messages from './i18n'" src/stories/geo-map/index.story.vue && grep -q "../_shared/use-story-locale" src/stories/geo-map/index.story.vue</automated>
  </verify>
  <acceptance_criteria>
    - `src/stories/geo-map/index.story.vue` exists
    - `src/stories/geo-map.story.vue` does NOT exist
    - No `useStoryLocale({` remains in the new file
    - File imports `messages` from `'./i18n'`
    - File imports `useStoryLocale` from `'../_shared/use-story-locale'`
  </acceptance_criteria>
  <done>geo-map story is fully migrated with key-based locale lookups.</done>
</task>

</tasks>

<verification>
- `pnpm story:dev` shows geo-map variants without errors
- Locale toggle switches all marker labels and descriptions correctly
</verification>

<success_criteria>
- geo-map directory exists with index.story.vue and i18n files
- Old flat file is removed
- All inline locale labels converted to key-based lookups
</success_criteria>

<output>
After completion, create `.planning/phases/07-story-i18n/07-03-SUMMARY.md`
</output>
