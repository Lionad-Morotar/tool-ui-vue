---
phase: 06-history-i18n
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stories/data-table.story.vue
  - src/stories/preferences-panel.story.vue
  - src/stories/geo-map.story.vue
  - src/stories/weather-widget.story.vue
  - src/stories/x-post.story.vue
  - src/stories/question-flow.story.vue
  - src/stories/linkedin-post.story.vue
  - src/stories/link-preview.story.vue
  - src/stories/instagram-post.story.vue
autonomous: true
requirements: [HIST-01, HIST-03, HIST-04]
must_haves:
  truths:
    - "All Variant titles display bilingual text and update on locale toggle"
    - "Description paragraphs use useStoryLocale or slash format"
    - "Props array and inline data description fields show correct language"
  artifacts:
    - path: "src/stories/data-table.story.vue"
      provides: "10 Variant titles, inline column labels"
      contains: ":title="
    - path: "src/stories/preferences-panel.story.vue"
      provides: "8 Variant titles, inline section headings/labels"
      contains: ":title="
    - path: "src/stories/geo-map.story.vue"
      provides: "15 Variant titles, marker labels"
      contains: ":title="
    - path: "src/stories/weather-widget.story.vue"
      provides: "11 Variant titles, condition labels"
      contains: ":title="
    - path: "src/stories/x-post.story.vue"
      provides: "9 Variant titles"
      contains: ":title="
    - path: "src/stories/question-flow.story.vue"
      provides: "9 Variant titles"
      contains: ":title="
    - path: "src/stories/linkedin-post.story.vue"
      provides: "9 Variant titles"
      contains: ":title="
    - path: "src/stories/link-preview.story.vue"
      provides: "9 Variant titles"
      contains: ":title="
    - path: "src/stories/instagram-post.story.vue"
      provides: "9 Variant titles"
      contains: ":title="
  key_links:
    - from: "src/stories/*.story.vue"
      to: "src/stories/_shared/use-story-locale.ts"
      via: "import { useStoryLocale }"
      pattern: "useStoryLocale\\(\\{\\s*zh:"
---

<objective>
Convert 9 medium-complexity story files (data-table, preferences-panel, geo-map, weather-widget, and 4 social media stories) — Variant titles, description text, and inline data labels — to bilingual format.

Purpose: Handle the bulk of story files that have Variant titles + description text + some inline English data, establishing a repeatable batch pattern.
Output: 9 fully bilingualized .story.vue files with zero hardcoded English user-facing text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-history-i18n/06-CONTEXT.md
@src/stories/_shared/use-story-locale.ts
@src/stories/data-table.story.vue
@src/stories/preferences-panel.story.vue
@src/stories/geo-map.story.vue
@src/stories/weather-widget.story.vue
@src/stories/x-post.story.vue
@src/stories/question-flow.story.vue
@src/stories/linkedin-post.story.vue
@src/stories/link-preview.story.vue
@src/stories/instagram-post.story.vue

<interfaces>
Same pattern as Plan 01:
```typescript
export function useStoryLocale(labels: { zh: string; en: string }): ComputedRef<string>
```

All 9 files already import useStoryLocale from `./_shared/use-story-locale`. No new imports needed.

For Variant titles: `:title="useStoryLocale({ zh: '中文', en: 'English' })"`
For inline data: Use `{ zh, en }` objects + `useStoryLocale(prop)` in template
For &lt;p&gt; descriptions: Already using `组件说明 / Component description` slash format (per D-02) — no changes needed for those lines
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert all Variant titles to :title with useStoryLocale (9 files)</name>
  <read_first>src/stories/data-table.story.vue, src/stories/preferences-panel.story.vue, src/stories/geo-map.story.vue, src/stories/weather-widget.story.vue, src/stories/x-post.story.vue, src/stories/question-flow.story.vue, src/stories/linkedin-post.story.vue, src/stories/link-preview.story.vue, src/stories/instagram-post.story.vue</read_first>
  <files>src/stories/data-table.story.vue, src/stories/preferences-panel.story.vue, src/stories/geo-map.story.vue, src/stories/weather-widget.story.vue, src/stories/x-post.story.vue, src/stories/question-flow.story.vue, src/stories/linkedin-post.story.vue, src/stories/link-preview.story.vue, src/stories/instagram-post.story.vue</files>
  <action>
    Replace ALL static `title="..."` on `&lt;Variant&gt;` tags with `:title="useStoryLocale({ zh: '中文', en: 'English' })"`.

    **data-table.story.vue (10 Variants):**
    - "Basic" → `{ zh: '基础', en: 'Basic' }`
    - "Stock Data (Delta + Currency + Percent)" → `{ zh: '股票数据（Delta + 货币 + 百分比）', en: 'Stock Data (Delta + Currency + Percent)' }`
    - "With Formatting" → `{ zh: '带格式化', en: 'With Formatting' }`
    - "With Status Badges" → `{ zh: '带状态徽标', en: 'With Status Badges' }`
    - "With Column Alignment" → `{ zh: '列对齐', en: 'With Column Alignment' }`
    - "Empty State" → `{ zh: '空状态', en: 'Empty State' }`
    - "With Default Sort" → `{ zh: '默认排序', en: 'With Default Sort' }`
    - "Controlled Sort" → `{ zh: '受控排序', en: 'Controlled Sort' }`
    - "With Max Height" → `{ zh: '最大高度', en: 'With Max Height' }`
    - "Interactive Layout Playground" → `{ zh: '交互布局演示', en: 'Interactive Layout Playground' }`

    **preferences-panel.story.vue (8 Variants):**
    - "Notification Settings" → `{ zh: '通知设置', en: 'Notification Settings' }`
    - "Appearance Settings" → `{ zh: '外观设置', en: 'Appearance Settings' }`
    - "With Custom Actions" → `{ zh: '自定义操作', en: 'With Custom Actions' }`
    - "All Control Types" → `{ zh: '所有控件类型', en: 'All Control Types' }`
    - "Receipt Mode - Success" → `{ zh: '回执模式 - 成功', en: 'Receipt Mode - Success' }`
    - "Receipt Mode - With Errors" → `{ zh: '回执模式 - 错误', en: 'Receipt Mode - With Errors' }`
    - "Without Title" → `{ zh: '无标题', en: 'Without Title' }`
    - "Without Section Headings" → `{ zh: '无分组标题', en: 'Without Section Headings' }`

    **geo-map.story.vue (15 Variants):**
    - "Single Marker" → `{ zh: '单个标记', en: 'Single Marker' }`
    - "Multiple Markers" → `{ zh: '多个标记', en: 'Multiple Markers' }`
    - "Marker with Popup" → `{ zh: '带弹窗的标记', en: 'Marker with Popup' }`
    - "Route Display" → `{ zh: '路线显示', en: 'Route Display' }`
    - "Multiple Routes" → `{ zh: '多条路线', en: 'Multiple Routes' }`
    - "Markers + Routes" → `{ zh: '标记 + 路线', en: 'Markers + Routes' }`
    - "With Selected Marker" → `{ zh: '选中标记', en: 'With Selected Marker' }`
    - "With Selected Route" → `{ zh: '选中路线', en: 'With Selected Route' }`
    - "With Custom Colors" → `{ zh: '自定义颜色', en: 'With Custom Colors' }`
    - "With Zoom Controls" → `{ zh: '缩放控件', en: 'With Zoom Controls' }`
    - "Dark Theme" → `{ zh: '暗色主题', en: 'Dark Theme' }`
    - "Without Markers" → `{ zh: '无标记', en: 'Without Markers' }`
    - "Without Routes" → `{ zh: '无路线', en: 'Without Routes' }`
    - "Interactive" → `{ zh: '交互模式', en: 'Interactive' }`
    - "Minimal" → `{ zh: '极简', en: 'Minimal' }`

    **weather-widget.story.vue (11 Variants):**
    - "Basic Weather" → `{ zh: '基础天气', en: 'Basic Weather' }`
    - "All Conditions" → `{ zh: '所有天气状态', en: 'All Conditions' }`
    - "With Forecast" → `{ zh: '带天气预报', en: 'With Forecast' }`
    - "Time of Day" → `{ zh: '时段显示', en: 'Time of Day' }`
    - "With Location" → `{ zh: '带位置', en: 'With Location' }`
    - "Celsius" → `{ zh: '摄氏度', en: 'Celsius' }`
    - "With Effects" → `{ zh: '带动画效果', en: 'With Effects' }`
    - "Dark Theme" → `{ zh: '暗色主题', en: 'Dark Theme' }`
    - "Compact" → `{ zh: '紧凑', en: 'Compact' }`
    - "Interactive" → `{ zh: '交互模式', en: 'Interactive' }`
    - "Minimal" → `{ zh: '极简', en: 'Minimal' }`

    **x-post.story.vue (9 Variants):**
    - "Default" → `{ zh: '默认', en: 'Default' }`
    - "With Image" → `{ zh: '带图片', en: 'With Image' }`
    - "With Multiple Images" → `{ zh: '多张图片', en: 'With Multiple Images' }`
    - "Long Post" → `{ zh: '长推文', en: 'Long Post' }`
    - "Verified Author" → `{ zh: '认证作者', en: 'Verified Author' }`
    - "Reply" → `{ zh: '回复', en: 'Reply' }`
    - "With Engagement" → `{ zh: '带互动数据', en: 'With Engagement' }`
    - "Dark Theme" → `{ zh: '暗色主题', en: 'Dark Theme' }`
    - "Minimal" → `{ zh: '极简', en: 'Minimal' }`

    **question-flow.story.vue (9 Variants):**
    - "Basic Questions" → `{ zh: '基础问答', en: 'Basic Questions' }`
    - "With Answers" → `{ zh: '带答案', en: 'With Answers' }`
    - "Multiple Questions" → `{ zh: '多个问题', en: 'Multiple Questions' }`
    - "With Timestamps" → `{ zh: '带时间戳', en: 'With Timestamps' }`
    - "With Source" → `{ zh: '带来源', en: 'With Source' }`
    - "Follow-up Questions" → `{ zh: '追问', en: 'Follow-up Questions' }`
    - "Dark Theme" → `{ zh: '暗色主题', en: 'Dark Theme' }`
    - "Compact" → `{ zh: '紧凑', en: 'Compact' }`
    - "Minimal" → `{ zh: '极简', en: 'Minimal' }`

    **linkedin-post.story.vue (9 Variants):**
    - "Default" → `{ zh: '默认', en: 'Default' }`
    - "With Image" → `{ zh: '带图片', en: 'With Image' }`
    - "With Document" → `{ zh: '带文档', en: 'With Document' }`
    - "Long Post" → `{ zh: '长文章', en: 'Long Post' }`
    - "With Engagement" → `{ zh: '带互动数据', en: 'With Engagement' }`
    - "Repost" → `{ zh: '转发', en: 'Repost' }`
    - "Company Post" → `{ zh: '公司账号', en: 'Company Post' }`
    - "Dark Theme" → `{ zh: '暗色主题', en: 'Dark Theme' }`
    - "Minimal" → `{ zh: '极简', en: 'Minimal' }`

    **link-preview.story.vue (9 Variants):**
    - "Default" → `{ zh: '默认', en: 'Default' }`
    - "With Image" → `{ zh: '带图片', en: 'With Image' }`
    - "With Favicon" → `{ zh: '带网站图标', en: 'With Favicon' }`
    - "Long Title" → `{ zh: '长标题', en: 'Long Title' }`
    - "Without Image" → `{ zh: '无图片', en: 'Without Image' }`
    - "With Description" → `{ zh: '带描述', en: 'With Description' }`
    - "Dark Theme" → `{ zh: '暗色主题', en: 'Dark Theme' }`
    - "Compact" → `{ zh: '紧凑', en: 'Compact' }`
    - "Minimal" → `{ zh: '极简', en: 'Minimal' }`

    **instagram-post.story.vue (9 Variants):**
    - "Default" → `{ zh: '默认', en: 'Default' }`
    - "Multiple Images" → `{ zh: '多张图片', en: 'Multiple Images' }`
    - "With Caption" → `{ zh: '带说明文字', en: 'With Caption' }`
    - "With Location" → `{ zh: '带位置', en: 'With Location' }`
    - "With Tagged Users" → `{ zh: '标记用户', en: 'With Tagged Users' }`
    - "Long Caption" → `{ zh: '长说明', en: 'Long Caption' }`
    - "With Engagement" → `{ zh: '带互动数据', en: 'With Engagement' }`
    - "Dark Theme" → `{ zh: '暗色主题', en: 'Dark Theme' }`
    - "Minimal" → `{ zh: '极简', en: 'Minimal' }`
  </action>
  <acceptance_criteria>
    - No static `title="..."` on `&lt;Variant&gt;` in any of the 9 files (grep: `rg '<Variant title="' src/stories/{data-table,preferences-panel,geo-map,weather-widget,x-post,question-flow,linkedin-post,link-preview,instagram-post}.story.vue` returns 0 matches)
    - Each Variant uses `:title="useStoryLocale({ zh: '...', en: '...' })"` pattern
  </acceptance_criteria>
  <verify>
    <automated>rg -c '<Variant title="' src/stories/data-table.story.vue src/stories/preferences-panel.story.vue src/stories/geo-map.story.vue src/stories/weather-widget.story.vue src/stories/x-post.story.vue src/stories/question-flow.story.vue src/stories/linkedin-post.story.vue src/stories/link-preview.story.vue src/stories/instagram-post.story.vue | grep -v ':0$' && exit 1 || echo "All Variant titles converted"</automated>
  </verify>
  <done>All 98 Variant titles across the 9 files use :title with useStoryLocale; zero static Variant titles remain.</done>
</task>

<task type="auto">
  <name>Task 2: Convert inline data labels and remaining English text</name>
  <read_first>src/stories/data-table.story.vue, src/stories/preferences-panel.story.vue, src/stories/weather-widget.story.vue, src/stories/geo-map.story.vue</read_first>
  <files>src/stories/data-table.story.vue, src/stories/preferences-panel.story.vue, src/stories/weather-widget.story.vue, src/stories/geo-map.story.vue</files>
  <action>
    Convert inline data labels (column labels, section headings, status labels, etc.) in the 4 data-heavy files.

    **data-table.story.vue:**
    - Inline column `label` values in templates: 'Name', 'Status', 'Revenue', 'Symbol', 'Company', 'Price', 'Change', 'Change %', 'Volume', 'Product', 'Sales', 'Growth', 'Task', 'Priority', 'Due Date', 'Item', 'Qty', 'Total', 'ID', 'Score', 'Grade', 'Project', 'Owner', 'Budget', 'Deadline', 'Notes'
    - Convert each to use `useStoryLocale({ zh: '中文', en: 'English' })` in the template
    - `empty-message="No records found"` → `:empty-message="useStoryLocale({ zh: '未找到记录', en: 'No records found' })"`
    - Status format labels: 'Completed', 'In Progress', 'Pending' in statusMap → useStoryLocale
    - Interactive playground &lt;p&gt; text: "Use the right panel to change layout..." → useStoryLocale
    - "Current sort:" text → useStoryLocale

    **preferences-panel.story.vue:**
    - Inline `title` props on component: "Notification Settings", "Appearance", "Privacy Settings", "All Preference Types", "Settings Saved", "Settings", "Quick Settings"
    - Section `heading` values: 'Email Notifications', 'Theme', 'Display', 'Notifications', 'Switches', 'Toggles', 'Select Dropdowns'
    - Item `label` values: 'Enable Email Notifications', 'Email Frequency', 'Color Theme', 'Compact Mode', 'Enable Animations', 'Allow Analytics', 'Marketing Emails', 'Feature One', 'Feature Two', 'Item Size', 'Timezone', 'Sound Effects', 'Auto-save', 'Option One', 'Option Two', 'Push Notifications'
    - Toggle options: 'Immediate', 'Daily Digest', 'Weekly Summary', 'Light', 'Dark', 'System', 'Small', 'Medium', 'Large', 'Eastern Time', 'Central Time', etc.
    - Action `label` values: 'Reset to Defaults', 'Save Changes'
    - Select options: 'UTC', 'Eastern Time', 'Central Time', 'Mountain Time', 'Pacific Time', 'GMT'

    **weather-widget.story.vue:**
    - Condition labels in the `conditions` array: 'Sunny', 'Partly Cloudy', 'Cloudy', 'Rainy', 'Heavy Rain', etc. — convert to computed with useStoryLocale
    - Interactive form labels if any
    - Location: 'Los Angeles, CA' — can keep as-is (it's data, not UI text)

    **geo-map.story.vue:**
    - Inline marker `label` values and route labels — convert to { zh, en } objects
    - Title props: "Location", "Route", etc.

    For all inline template data, the simplest approach is to replace string values with `useStoryLocale({ zh: '...', en: '...' })` calls directly. Since ComputedRef auto-unwraps in Vue templates, this works without `.value`.

    Note: The `组件说明 / Component description` slash-format description lines (per D-02) do NOT need changes — they already show both languages simultaneously.
  </action>
  <acceptance_criteria>
    - data-table.story.vue: No English `label: '...'` for column labels in templates (except language names like 'typescript' which are code identifiers)
    - preferences-panel.story.vue: No English `title="..."`, `heading: '...'`, `label: '...'` in inline data (except proper nouns that are code identifiers)
    - weather-widget.story.vue: Condition labels array uses { zh, en } objects or computed
    - geo-map.story.vue: Marker labels and title props use useStoryLocale
  </acceptance_criteria>
  <verify>
    <automated>echo "Checking data-table inline labels..." && rg "label: '[A-Z][a-z]+'" src/stories/data-table.story.vue | grep -v 'useStoryLocale' | grep -v '//' | head -5; echo "Checking preferences-panel inline labels..." && rg "(heading|label|title): '[A-Z]" src/stories/preferences-panel.story.vue | grep -v 'useStoryLocale' | grep -v '//' | head -5</automated>
  </verify>
  <done>All inline data labels in the 4 data-heavy files are bilingualized via useStoryLocale; remaining 5 social media files have no inline English data (only Variant titles and &lt;p&gt; descriptions, already handled).</done>
</task>

<task type="auto">
  <name>Task 3: Convert remaining &lt;p&gt; and &lt;span&gt; description text in social media stories</name>
  <read_first>src/stories/x-post.story.vue, src/stories/question-flow.story.vue, src/stories/linkedin-post.story.vue, src/stories/link-preview.story.vue, src/stories/instagram-post.story.vue</read_first>
  <files>src/stories/x-post.story.vue, src/stories/question-flow.story.vue, src/stories/linkedin-post.story.vue, src/stories/link-preview.story.vue, src/stories/instagram-post.story.vue</files>
  <action>
    The 5 social media story files have Variant titles (handled in Task 1) and `组件说明 / Component description` slash-format description lines (no change needed per D-02).

    Check for any additional English &lt;p&gt;, &lt;span&gt;, or text content outside Variant titles and component props. Specifically:

    - Any standalone English text between tags
    - Interactive variant form labels
    - Tooltip or aria-label English text
    - Any English text in &lt;template&gt; that is NOT: code content, language identifiers, or component prop values that are data (not UI labels)

    If any found, convert to useStoryLocale. If none found (likely for these simple stories), mark task as complete immediately.
  </action>
  <acceptance_criteria>
    - No standalone English &lt;p&gt; or &lt;span&gt; text in the 5 files outside of component props (grep for English sentences in template text nodes)
    - `组件说明 / Component description` lines are present (slash format, no change needed)
  </acceptance_criteria>
  <verify>
    <automated>echo "Checking for remaining English text in social media stories..." && rg '&lt;p[^&gt;]*&gt;[A-Z][a-z]+ [a-z]+' src/stories/x-post.story.vue src/stories/question-flow.story.vue src/stories/linkedin-post.story.vue src/stories/link-preview.story.vue src/stories/instagram-post.story.vue | grep -v '组件说明' | grep -v 'useStoryLocale' | grep -v '&lt;p' && echo "Found English text needing conversion" || echo "No remaining English text found in social media stories"</automated>
  </verify>
  <done>All 5 social media story files have been checked; any remaining English text converted to bilingual format via useStoryLocale or slash format.</done>
</task>

</tasks>

<verification>
Run across all 9 files:
- `rg '<Variant title="' src/stories/{data-table,preferences-panel,geo-map,weather-widget,x-post,question-flow,linkedin-post,link-preview,instagram-post}.story.vue` → 0 matches
- Check inline data labels are converted in the 4 data-heavy files
- Check social media files have no remaining English &lt;p&gt; text
</verification>

<success_criteria>
1. All 98 Variant titles across 9 files use `:title="useStoryLocale({ zh: '...', en: '...' })"`
2. All inline data labels in data-table, preferences-panel, weather-widget, geo-map are bilingualized
3. 5 social media stories checked and cleaned of remaining English text
4. All `组件说明 / Component description` lines left as-is (per D-02 slash format)
</success_criteria>

<output>
After completion, create `.planning/phases/06-history-i18n/06-02-SUMMARY.md`
</output>
