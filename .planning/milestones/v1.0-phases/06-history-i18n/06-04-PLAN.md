---
phase: 06-history-i18n
plan: 04
type: execute
wave: 2
depends_on: ["01", "02", "03"]
files_modified:
  - src/stories/landing.story.vue
  - src/stories/*.story.vue
autonomous: true
requirements: [HIST-01, HIST-02, HIST-03, HIST-04, HIST-05]
must_haves:
  truths:
    - "Landing Page hero title, subtitle, category titles, and footer text switch language on toggle"
    - "Zero hardcoded English Variant titles remain across ALL 28+ story files"
    - "Locale toggle works and all text updates without page refresh"
  artifacts:
    - path: "src/stories/landing.story.vue"
      provides: "Hero section, 6 category cards, footer — all bilingual"
      contains: "useStoryLocale"
    - path: "src/stories/*.story.vue (all 28 files)"
      provides: "Phase-wide verification — no static English Variant titles"
      contains: ":title="
  key_links:
    - from: "src/stories/landing.story.vue"
      to: "src/stories/_shared/use-story-locale.ts"
      via: "import { useStoryLocale }"
      pattern: "useStoryLocale\\(\\{\\s*zh:"
---

<objective>
Bilingualize the Landing Page (src/stories/landing.story.vue) and perform phase-wide verification that all 28 story files have zero hardcoded English user-facing text.

Purpose: Complete the last remaining file (Landing Page) which has unique structure — category cards with titles, hero section text, install command area, and footer. Then verify the entire phase is complete.
Output: Fully bilingualized landing.story.vue + verification report across all 28 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-history-i18n/06-CONTEXT.md
@src/stories/_shared/use-story-locale.ts
@src/stories/landing.story.vue
@.planning/phases/06-history-i18n/06-01-PLAN.md
@.planning/phases/06-history-i18n/06-02-PLAN.md
@.planning/phases/06-history-i18n/06-03-PLAN.md

<interfaces>
Landing Page current structure (from code read):
- `subtitle` already uses useStoryLocale (line 14)
- `categories` array has 6 items, each with `title` (English string), `components` (array of names), `id`, `href`, `icon`
- Template: Hero section has "Vue 3 + TypeScript + Tailwind CSS v4" badge, "tool-ui-vue" h1, "Vue 3 components for AI tool calls" p, "pnpm add @lionad/vtu-components" install code, "Get Started" link
- Categories section: "Browse by category" h2, then 6 category cards with `{{ cat.title }}` and `{{ comp }}`
- Footer: "Built by Lionad-Morotar · View on GitHub"

The `useStoryLocale` import is already present at line 12.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bilingualize Landing Page — categories, hero text, footer</name>
  <read_first>src/stories/landing.story.vue</read_first>
  <files>src/stories/landing.story.vue</files>
  <action>
    Convert all English UI text in landing.story.vue to bilingual format. The `subtitle` already uses useStoryLocale — do NOT modify it.

    **Categories array (6 items):** Convert each `title` to use `useStoryLocale`:
    ```ts
    const categories = computed(() => [
      {
        id: 'data-display',
        title: useStoryLocale({ zh: '数据展示', en: 'Data Display' }).value,
        icon: BarChart3,
        components: ['Chart', 'DataTable', 'StatsDisplay', 'WeatherWidget'],
        href: '#/story/src-stories-chart-story-vue',
      },
      // ... repeat for all 6
    ])
    ```

    Wait — using `.value` inside a computed that returns an array used in `v-for` is fine. But the better pattern is to make the entire `categories` a `computed` so it recalculates when `currentLocale` changes:

    ```ts
    const categories = computed(() => [
      {
        id: 'data-display',
        title: useStoryLocale({ zh: '数据展示', en: 'Data Display' }),
        icon: BarChart3,
        components: ['Chart', 'DataTable', 'StatsDisplay', 'WeatherWidget'],
        href: '#/story/src-stories-chart-story-vue',
      },
      {
        id: 'code-terminal',
        title: useStoryLocale({ zh: '代码与终端', en: 'Code & Terminal' }),
        icon: Terminal,
        components: ['CodeBlock', 'CodeDiff', 'Terminal'],
        href: '#/story/src-stories-code-block-story-vue',
      },
      {
        id: 'media',
        title: useStoryLocale({ zh: '媒体', en: 'Media' }),
        icon: ImageIcon,
        components: ['Audio', 'Image', 'ImageGallery', 'ItemCarousel', 'Video'],
        href: '#/story/src-stories-audio-story-vue',
      },
      {
        id: 'social',
        title: useStoryLocale({ zh: '社交', en: 'Social' }),
        icon: Share2,
        components: ['ApprovalCard', 'Citation', 'InstagramPost', 'LinkedInPost', 'LinkPreview', 'MessageDraft', 'XPost'],
        href: '#/story/src-stories-approval-card-story-vue',
      },
      {
        id: 'forms-input',
        title: useStoryLocale({ zh: '表单与输入', en: 'Forms & Input' }),
        icon: MousePointerClick,
        components: ['OptionList', 'ParameterSlider', 'PreferencesPanel'],
        href: '#/story/src-stories-option-list-story-vue',
      },
      {
        id: 'workflow',
        title: useStoryLocale({ zh: '工作流', en: 'Workflow' }),
        icon: Layers,
        components: ['GeoMap', 'Plan', 'ProgressTracker', 'QuestionFlow', 'OrderSummary'],
        href: '#/story/src-stories-geo-map-story-vue',
      },
    ])
    ```

    Since `categories` is now a computed returning ComputedRef for title, and the template uses `{{ cat.title }}`, Vue will auto-unwrap.

    **Hero section text:**
    - `<span>Vue 3 + TypeScript + Tailwind CSS v4</span>` → Keep as-is (technical identifiers, not UI text needing translation)
    - `<h1>tool-ui-vue</h1>` → Keep as-is (brand name)
    - `<p>Vue 3 components for AI tool calls</p>` → `{{ useStoryLocale({ zh: '为 AI 工具调用设计的 Vue 3 组件库', en: 'Vue 3 components for AI tool calls' }) }}`
    - Install command `pnpm add @lionad/vtu-components` → Keep as-is (technical command)
    - `<a>Get Started</a>` → `{{ useStoryLocale({ zh: '开始使用', en: 'Get Started' }) }}`

    **Categories section heading:**
    - `<h2>Browse by category</h2>` → `<h2>{{ useStoryLocale({ zh: '按类别浏览', en: 'Browse by category' }) }}</h2>`

    **Category component names:** The `components` array values (e.g., 'Chart', 'DataTable') are component names — keep as-is (they're technical identifiers).

    **Footer text:**
    - "Built by" → `{{ useStoryLocale({ zh: '由', en: 'Built by' }) }}`
    - "View on" → `{{ useStoryLocale({ zh: '查看', en: 'View on' }) }}`
    - "GitHub" → Keep as-is (platform name)
  </action>
  <acceptance_criteria>
    - landing.story.vue: No English text in hero paragraph, category titles, category heading, footer, or Get Started button (except brand names, technical identifiers, and code commands)
    - categories array is `computed(() => [...])` with useStoryLocale for titles
    - grep check: `rg 'title: "'"[A-Z]' src/stories/landing.story.vue` returns 0 matches for category titles
  </acceptance_criteria>
  <verify>
    <automated>rg 'title: "' src/stories/landing.story.vue | grep -v 'useStoryLocale' && echo "Category titles still plain English" && exit 1 || echo "Landing Page categories converted"</automated>
  </verify>
  <done>Landing Page hero text, category titles, section heading, Get Started button, and footer text all use useStoryLocale; brand names and technical identifiers left as-is.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-wide verification — zero hardcoded English across all 28+ files</name>
  <read_first>src/stories/*.story.vue (all files)</read_first>
  <files>src/stories/*.story.vue</files>
  <action>
    Run comprehensive verification across ALL story files to confirm the phase is complete:

    1. **Check zero static Variant titles:**
       `rg '<Variant title="' src/stories/*.story.vue --no-heading`
       Expected: 0 matches (excluding tailwind-test.story.vue which is excluded from this phase)

    2. **Check useStoryLocale is used for all dynamic titles:**
       `rg -c ':title="useStoryLocale' src/stories/*.story.vue`
       Expected: All files with Variants show counts matching their Variant count

    3. **Check for remaining English h2/h3 text:**
       `rg '<h[23][^>]*>[A-Z]' src/stories/*.story.vue --no-heading`
       Expected: Only useStoryLocale interpolations or tailwind-test

    4. **Check for remaining English &lt;p&gt; descriptions:**
       `rg '&lt;p[^>]*>[A-Z][a-z]+ [a-z]+' src/stories/*.story.vue --no-heading | grep -v '组件说明' | grep -v 'useStoryLocale' | grep -v 'tailwind-test'`
       Expected: 0 matches

    5. **Check landing page specifically:**
       Read landing.story.vue and verify all text areas (hero, categories, heading, footer) use useStoryLocale

    6. **TypeScript check:**
       Run `pnpm tsc --noEmit` or project's type-check command to ensure no type errors from the changes

    If any checks fail, fix the issues before marking complete. Document any findings.
  </action>
  <acceptance_criteria>
    - `rg '<Variant title="' src/stories/*.story.vue --no-heading | grep -v tailwind | wc -l` returns 0
    - `rg ':title="useStoryLocale' src/stories/*.story.vue --no-heading | wc -l` returns 257 (total Variant count)
    - TypeScript compiles without errors
    - Landing Page renders with bilingual category titles
  </acceptance_criteria>
  <verify>
    <automated>echo "=== Variant Title Check ===" && rg -c '<Variant title="' src/stories/*.story.vue | grep -v 'tailwind' | grep -v ':0' && echo "FAIL: Static Variant titles remain" && exit 1 || echo "PASS: All Variant titles converted" && echo "=== useStoryLocale Count ===" && rg -c ':title="useStoryLocale' src/stories/*.story.vue | grep -v ':0' | wc -l && echo "=== TypeScript Check ===" && cd /Users/lionad/Github/Lionad-Morotar/tool-ui-vue && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>All 28 story files pass verification: zero static Variant titles, all use :title with useStoryLocale, TypeScript compiles cleanly, Landing Page fully bilingual.</done>
</task>

</tasks>

<verification>
Phase completion checks:
1. All 257 Variant titles across 28 files converted to :title with useStoryLocale
2. All h2/h3 headings in terminal, code-block, code-diff, chart converted
3. All props array descriptions bilingualized
4. Landing Page fully bilingualized (hero, categories, heading, footer)
5. TypeScript compiles without errors
6. No remaining hardcoded English user-facing text (excluding code, brand names, technical identifiers)
</verification>

<success_criteria>
1. `rg '<Variant title="' src/stories/*.story.vue` returns 0 matches (excluding tailwind-test)
2. `rg ':title="useStoryLocale' src/stories/*.story.vue` returns 257 matches
3. Landing Page category titles, hero text, section heading, footer all use useStoryLocale
4. TypeScript compiles without errors
5. Histoire site renders correctly with language toggle producing real-time text updates
</success_criteria>

<output>
After completion, create `.planning/phases/06-history-i18n/06-04-SUMMARY.md`
</output>
