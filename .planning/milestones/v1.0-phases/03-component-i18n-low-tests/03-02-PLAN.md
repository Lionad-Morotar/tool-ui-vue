---
phase: 03-component-i18n-low-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/components/src/x-post/index.vue
  - packages/components/src/instagram-post/index.vue
  - packages/components/src/linkedin-post/cmpts/linkedin-post.vue
autonomous: true
requirements:
  - COMPS-03
must_haves:
  truths:
    - "instagram-post Like/Share 按钮文本随 locale 切换为中文/英文"
    - "linkedin-post Like/Share 按钮和 'see more'/'Edited' 文本随 locale 切换"
    - "x-post aria-label 使用 t() 调用（品牌名保持英文但在两个 locale 中一致）"
  artifacts:
    - path: "packages/components/src/x-post/index.vue"
      provides: "x-post with useI18n for aria-labels"
      contains: "useI18n, t('xPost."
    - path: "packages/components/src/instagram-post/index.vue"
      provides: "instagram-post with useI18n"
      contains: "useI18n, t('instagramPost."
    - path: "packages/components/src/linkedin-post/cmpts/linkedin-post.vue"
      provides: "linkedin-post with useI18n"
      contains: "useI18n, t('linkedinPost."
  key_links:
    - from: "packages/components/src/instagram-post/index.vue"
      to: "@lionad/vtu-core/i18n"
      via: "import { useI18n } from '@lionad/vtu-core/i18n'"
      pattern: "useI18n\\(\\)"
    - from: "packages/components/src/instagram-post/index.vue"
      to: "core locales (instagramPost)"
      via: "t('instagramPost.like'), t('instagramPost.share')"
      pattern: "t\\('instagramPost\\."
---

<objective>
i18n 改造 3 个社交媒体组件（x-post, instagram-post, linkedin-post），消除可见硬编码英文文本和 aria-label 中的英文。

Purpose: 这些组件有明确的可见英文文本（按钮标签、链接文本），需要响应语言切换。
Output: 3 个组件 Vue 文件修改
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-component-i18n-low-tests/03-CONTEXT.md
@packages/core/src/i18n/locales/zh-CN.ts (instagramPost, linkedinPost, xPost namespaces)
@packages/core/src/i18n/locales/en.ts (instagramPost, linkedinPost, xPost namespaces)
@packages/components/src/terminal/index.vue (Phase 2 pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n-ize instagram-post component</name>
  <files>packages/components/src/instagram-post/index.vue</files>
  <read_first>
    - packages/components/src/instagram-post/index.vue
    - packages/components/src/terminal/index.vue (Phase 2 pattern reference)
  </read_first>
  <action>
    1. 添加 import: `import { useI18n } from '@lionad/vtu-core/i18n';`（在 cn import 之后）
    2. 添加 composable: `const { t } = useI18n();`（在 state 声明之后）
    3. 替换以下硬编码文本：
       - aria-label="Like" → :aria-label="computed(() => t('instagramPost.like').value)"
       - aria-label="Share" → :aria-label="computed(() => t('instagramPost.share').value)"
    4. 需要从 vue 导入 computed（如果尚未导入）

    注意：instagram-post 的 locale 条目已在核心 locale 文件中定义（instagramPost: { like, comment, share, save, likes, comments, ... }）。
    只替换 aria-label 中的文本，因为该组件的可见文本主要来自 props（post.author.handle, post.text）。
  </action>
  <verify>
    <automated>grep -q "useI18n" packages/components/src/instagram-post/index.vue && ! grep -q 'aria-label="Like"' packages/components/src/instagram-post/index.vue && ! grep -q 'aria-label="Share"' packages/components/src/instagram-post/index.vue && echo "OK"</automated>
  </verify>
  <done>instagram-post 导入 useI18n，aria-label 不再使用硬编码 "Like" 和 "Share"，改为 t() 绑定</done>
</task>

<task type="auto">
  <name>Task 2: i18n-ize linkedin-post component</name>
  <files>packages/components/src/linkedin-post/cmpts/linkedin-post.vue</files>
  <read_first>
    - packages/components/src/linkedin-post/cmpts/linkedin-post.vue
    - packages/components/src/terminal/index.vue (Phase 2 pattern reference)
  </read_first>
  <action>
    1. 在第一个 script setup 块中添加：
       ```typescript
       import { useI18n } from '@lionad/vtu-core/i18n';
       ```
    2. 在 state 声明之后添加：
       ```typescript
       const { t } = useI18n();
       ```
    3. 替换以下硬编码文本：
       - aria-label="Like" → :aria-label="computed(() => t('linkedinPost.like').value)"
       - aria-label="Share" → :aria-label="computed(() => t('linkedinPost.share').value)"
       - `<span>Like</span>` → `<span>{{ t('linkedinPost.like') }}</span>`
       - `<span>Share</span>` → `<span>{{ t('linkedinPost.share') }}</span>`
       - `see more` → `{{ t('shared.viewDetails') }}` 或添加 linkedinPost.seeMore 到 locale
       - `Edited` → 需要在 linkedinPost 命名空间中添加 edited 条目

    注意：linkedinPost 的 locale 条目中缺少 edited 和 seeMore 键。需要先在两个核心 locale 文件中添加：
    - zh-CN.ts linkedinPost 中添加: `seeMore: '查看更多', edited: '已编辑',`
    - en.ts linkedinPost 中添加: `seeMore: 'See more', edited: 'Edited',`
  </action>
  <verify>
    <automated>grep -q "useI18n" packages/components/src/linkedin-post/cmpts/linkedin-post.vue && ! grep -q '<span>Like</span>' packages/components/src/linkedin-post/cmpts/linkedin-post.vue && echo "OK"</automated>
  </verify>
  <done>linkedin-post 导入 useI18n，可见 "Like"/"Share"/"see more"/"Edited" 文本被 t() 替换</done>
</task>

<task type="auto">
  <name>Task 3: i18n-ize x-post component (aria-labels only)</name>
  <files>packages/components/src/x-post/index.vue</files>
  <read_first>
    - packages/components/src/x-post/index.vue
    - packages/components/src/terminal/index.vue (Phase 2 pattern reference)
  </read_first>
  <action>
    1. 添加 import: `import { useI18n } from '@lionad/vtu-core/i18n';`
    2. 添加 composable: `const { t } = useI18n();`
    3. 替换 aria-label 中的品牌描述文本：
       - aria-label="Verified account" → :aria-label="computed(() => t('xPost.verified').value)"
       - aria-label="X (formerly Twitter) logo" → :aria-label="computed(() => t('xPost.logo').value)"

    注意：xPost 命名空间缺少 verified 和 logo 键。需要先在核心 locale 文件中添加：
    - zh-CN.ts xPost 中添加: `verified: '认证账号', logo: 'X（原 Twitter）标志',`
    - en.ts xPost 中添加: `verified: 'Verified account', logo: 'X (formerly Twitter) logo',`

    x-post 没有可见硬编码英文文本（按钮无文字标签，仅有图标），但 aria-label 仍需 t() 封装以保持一致性。
  </action>
  <verify>
    <automated>grep -q "useI18n" packages/components/src/x-post/index.vue && ! grep -q 'aria-label="Verified' packages/components/src/x-post/index.vue && echo "OK"</automated>
  </verify>
  <done>x-post 导入 useI18n，aria-label 不再硬编码，使用 t() 并加 .value 解包</done>
</task>

</tasks>

<verification>
- 3 个组件都导入了 useI18n
- instagram-post 和 linkedin-post 的可见按钮文本响应语言切换
- 所有 aria-label 绑定使用 computed(() => t('key').value) 模式
- 运行 pnpm test 确认现有测试未被破坏
</verification>

<success_criteria>
- instagram-post: aria-label 不再包含字面量 "Like" 或 "Share"
- linkedin-post: 不再包含字面量 `<span>Like</span>` 或 `<span>Share</span>` 或 "see more" 或 "Edited"
- x-post: aria-label 不再包含字面量 "Verified account" 或 "X (formerly Twitter) logo"
- 3 个组件的 locale 条目在核心 locale 文件中完整
</success_criteria>

<output>
After completion, create `.planning/phases/03-component-i18n-low-tests/03-02-SUMMARY.md`
</output>
