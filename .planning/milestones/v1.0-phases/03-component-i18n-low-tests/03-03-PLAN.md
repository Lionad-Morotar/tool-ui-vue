---
phase: 03-component-i18n-low-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/components/src/chart/index.vue
  - packages/components/src/stats-display/index.vue
  - packages/components/src/plan/index.vue
  - packages/core/src/i18n/locales/zh-CN.ts
  - packages/core/src/i18n/locales/en.ts
autonomous: true
requirements:
  - COMPS-03
must_haves:
  truths:
    - "plan 组件 'X of Y complete' 和 'X more' 文本随 locale 切换为中文"
    - "chart 组件无可见硬编码英文文本（主要是数据驱动渲染）"
    - "stats-display 组件无可见硬编码英文文本"
  artifacts:
    - path: "packages/components/src/plan/index.vue"
      provides: "plan component with useI18n"
      contains: "useI18n, t('plan."
    - path: "packages/components/src/chart/index.vue"
      provides: "chart component with useI18n"
      contains: "useI18n"
    - path: "packages/components/src/stats-display/index.vue"
      provides: "stats-display component with useI18n"
      contains: "useI18n"
  key_links:
    - from: "packages/components/src/plan/index.vue"
      to: "packages/core/src/i18n/locales/zh-CN.ts (plan namespace)"
      via: "t('plan.complete'), t('plan.more')"
      pattern: "t\\('plan\\."
    - from: "packages/components/src/chart/index.vue"
      to: "@lionad/vtu-core/i18n"
      via: "import { useI18n } from '@lionad/vtu-core/i18n'"
      pattern: "useI18n\\(\\)"
---

<objective>
i18n 改造 chart、stats-display 和 plan 三个组件。plan 组件有明确的硬编码英文文本需要替换，chart 和 stats-display 主要为数据驱动但需要接入 i18n 基础设施。

Purpose: plan 组件有两个明确的硬编码英文字符串需要消除；chart/stats-display 接入 i18n 确保所有 14 个低优组件全覆盖。
Output: 3 个组件 Vue 文件修改 + 2 个核心 locale 文件（添加缺失键）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-component-i18n-low-tests/03-CONTEXT.md
@packages/components/src/plan/index.vue
@packages/components/src/chart/index.vue
@packages/components/src/stats-display/index.vue
@packages/core/src/i18n/locales/zh-CN.ts
@packages/core/src/i18n/locales/en.ts
@packages/components/src/terminal/index.vue (Phase 2 pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing locale keys for linkedin-post and x-post</name>
  <files>packages/core/src/i18n/locales/zh-CN.ts, packages/core/src/i18n/locales/en.ts</files>
  <read_first>
    - packages/core/src/i18n/locales/zh-CN.ts
    - packages/core/src/i18n/locales/en.ts
  </read_first>
  <action>
    在核心 locale 文件中添加 Plan 02 所需但缺失的键：

    1. zh-CN.ts 的 linkedinPost 中添加：
       `seeMore: '查看更多', edited: '已编辑',`

    2. en.ts 的 linkedinPost 中添加：
       `seeMore: 'See more', edited: 'Edited',`

    3. zh-CN.ts 的 xPost 中添加：
       `verified: '认证账号', logo: 'X（原 Twitter）标志',`

    4. en.ts 的 xPost 中添加：
       `verified: 'Verified account', logo: 'X (formerly Twitter) logo',`

    注意：plan 命名空间已在 Plan 01 中添加，此处不重复。
  </action>
  <verify>
    <automated>grep -q 'seeMore' packages/core/src/i18n/locales/zh-CN.ts && grep -q 'verified' packages/core/src/i18n/locales/en.ts && echo "OK"</automated>
  </verify>
  <done>两个 locale 文件包含 linkedinPost.seeMore、linkedinPost.edited、xPost.verified、xPost.logo 键</done>
</task>

<task type="auto">
  <name>Task 2: i18n-ize plan component (hardcoded "complete" and "more")</name>
  <files>packages/components/src/plan/index.vue</files>
  <read_first>
    - packages/components/src/plan/index.vue
    - packages/components/src/terminal/index.vue (Phase 2 pattern reference)
  </read_first>
  <action>
    1. 添加 import: `import { useI18n } from '@lionad/vtu-core/i18n';`（在 cn import 之后）
    2. 添加 composable: `const { t } = useI18n();`（在 state 声明之后）

    3. 替换第 58 行的硬编码文本：
       从: `{{ state.progress.completed }} of {{ state.progress.total }} complete`
       改为: `{{ state.progress.completed }} / {{ state.progress.total }} {{ t('plan.complete') }}`
       （注意中文习惯使用 "/" 而非 "of"）

    4. 替换第 238 行的硬编码文本：
       从: `<span>{{ state.hiddenCount }} more</span>`
       改为: `<span>{{ t('plan.more', { count: state.hiddenCount }) }}</span>`

    5. plan 命名空间的 locale 条目（zh-CN: { complete: '已完成', more: '还有 {count} 项' }；en: { complete: 'complete', more: '{count} more' }）已在 Plan 01 的 Task 1 中添加。
       如果 Plan 01 尚未执行，需要先执行该任务。

    注意：plan 组件中 `todo.status === 'pending'` 等字符串是 JavaScript 比较值（非显示文本），不需要 i18n 化。
  </action>
  <verify>
    <automated>grep -q "useI18n" packages/components/src/plan/index.vue && ! grep -q "} complete" packages/components/src/plan/index.vue && ! grep -q "}} more" packages/components/src/plan/index.vue && echo "OK"</automated>
  </verify>
  <done>plan 组件不再有硬编码 "complete" 和 "more" 文本，使用 t() 从 locale 获取</done>
</task>

<task type="auto">
  <name>Task 3: i18n-ize chart and stats-display components</name>
  <files>packages/components/src/chart/index.vue, packages/components/src/stats-display/index.vue</files>
  <read_first>
    - packages/components/src/chart/index.vue
    - packages/components/src/stats-display/index.vue
    - packages/components/src/terminal/index.vue (Phase 2 pattern reference)
  </read_first>
  <action>
    对 chart 和 stats-display 各应用标准 i18n 模式：

    1. 添加 import: `import { useI18n } from '@lionad/vtu-core/i18n';`
    2. 添加 composable: `const { t } = useI18n();`

    这两个组件主要是数据驱动的（图表渲染和统计数字），可见硬编码文本很少。添加 useI18n 确保：
    - 组件可以通过 t() 访问 chart 和 statsDisplay 命名空间的 locale 条目
    - 如果组件内部有空状态/加载状态的文本，可以使用 t('chart.noData'), t('chart.loading'), t('statsDisplay.change') 等
    - 搜索模板中任何剩余的硬编码英文字符串并替换

    如果组件确实没有任何可见硬编码英文文本，至少导入 useI18n 为将来扩展预留，并确保组件注册到 i18n 系统中。
  </action>
  <verify>
    <automated>grep -l "useI18n" packages/components/src/chart/index.vue packages/components/src/stats-display/index.vue | wc -l | grep -q "2" && echo "OK"</automated>
  </verify>
  <done>chart 和 stats-display 都导入了 useI18n，模板中无未替换的可见硬编码英文文本</done>
</task>

</tasks>

<verification>
- plan 组件不再包含 "complete" 或 "more" 硬编码英文文本
- chart 和 stats-display 导入了 useI18n
- 运行 pnpm test 确认现有测试未被破坏
- 检查 plan 组件 locale 条目在两个文件中都存在
</verification>

<success_criteria>
- plan 组件使用 t('plan.complete') 和 t('plan.more', { count }) 替换所有硬编码文本
- chart 和 stats-display 都 import useI18n from '@lionad/vtu-core/i18n'
- linkedinPost 和 xPost 的缺失 locale 键已添加到两个核心 locale 文件中
</success_criteria>

<output>
After completion, create `.planning/phases/03-component-i18n-low-tests/03-03-SUMMARY.md`
</output>
