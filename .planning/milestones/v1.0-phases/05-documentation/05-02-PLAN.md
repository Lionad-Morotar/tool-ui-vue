---
phase: 05-documentation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - README.md
  - .planning/docs/API-i18n.md
  - .planning/docs/CONSUMER-ONBOARDING.md
  - src/stories/terminal.story.vue
  - src/stories/chart.story.vue
  - src/stories/data-table.story.vue
  - src/stories/stats-display.story.vue
  - src/stories/weather-widget.story.vue
  - src/stories/code-block.story.vue
  - src/stories/code-diff.story.vue
  - src/stories/order-summary.story.vue
  - src/stories/question-flow.story.vue
  - src/stories/message-draft.story.vue
  - src/stories/audio.story.vue
  - src/stories/video.story.vue
  - src/stories/image-gallery.story.vue
  - src/stories/item-carousel.story.vue
  - src/stories/geo-map.story.vue
  - src/stories/preferences-panel.story.vue
  - src/stories/approval-card.story.vue
  - src/stories/citation.story.vue
  - src/stories/link-preview.story.vue
  - src/stories/x-post.story.vue
  - src/stories/instagram-post.story.vue
  - src/stories/linkedin-post.story.vue
  - src/stories/option-list.story.vue
  - src/stories/parameter-slider.story.vue
  - src/stories/progress-tracker.story.vue
  - src/stories/plan.story.vue
  - src/stories/image.story.vue
  - src/stories/landing.story.vue
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-04, DOCS-05]
must_haves:
  truths:
    - "README 包含可复制的 LocaleProvider 使用示例"
    - "切换 Histoire 语言后 story 表格表头和描述文本同步更新"
    - "API 文档覆盖 LocaleProvider props、useI18n、t() 签名、消息格式"
    - "消费者指南包含零侵入模式说明"
  artifacts:
    - path: "README.md"
      provides: "Quick Start, i18n, API Reference, Custom Languages sections"
      contains: "LocaleProvider|useI18n|自定义语言"
    - path: ".planning/docs/API-i18n.md"
      provides: "Full API reference for i18n system"
      exports: ["LocaleProvider props", "useI18n return", "t() signature", "message format"]
    - path: ".planning/docs/CONSUMER-ONBOARDING.md"
      provides: "Step-by-step consumer integration guide"
      contains: "零侵入|copy-paste|setLocale"
    - path: "src/stories/terminal.story.vue"
      provides: "Example of bilingual story with useStoryLocale"
      contains: "useStoryLocale"
  key_links:
    - from: "src/stories/*.story.vue"
      to: "src/stories/_shared/use-story-locale.ts"
      via: "import { useStoryLocale }"
      pattern: "useStoryLocale\\("
    - from: "README.md"
      to: ".planning/docs/API-i18n.md"
      via: "link to full API doc"
      pattern: "API-i18n\\.md"
---

<objective>
Update all 29 story files with locale-aware descriptions, create API documentation and consumer onboarding guide, and expand README with i18n usage sections. Depends on Plan 01 for the `useStoryLocale` utility.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation/05-CONTEXT.md
@.planning/phases/05-documentation/05-01-PLAN.md
@.planning/phases/05-documentation/05-01-SUMMARY.md
@README.md

<interfaces>
From Plan 01 (05-01-SUMMARY.md expected output):
```typescript
// src/stories/_shared/use-story-locale.ts
export interface StoryLocaleLabels { zh: string; en: string }
export function useStoryLocale(labels: StoryLocaleLabels): ComputedRef<string>
export function useStoryLocaleRaw(): { locale: Ref<string>; toggle: () => void }
```

From packages/core/src/i18n/use-i18n.ts:
```typescript
export function useI18n<TMessages>(): I18nReturn<TMessages>
// I18nReturn.t(key, params?) => ComputedRef<string>
// I18nReturn.locale => ReadonlyRef<string>
// I18nReturn.setLocale(locale: string) => void
```

From packages/core/src/i18n/locale-provider/index.vue props:
```typescript
interface LocaleProviderProps<TMessages> {
  messages: TMessages   // locale messages object (e.g., zhCN or en)
  locale?: string       // defaults to 'zh-CN'
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand README with i18n sections (Quick Start -> i18n -> API Reference -> Custom Languages)</name>
  <files>README.md</files>
  <action>
    Append to existing README.md (keep the current 3 lines as header). Per D-01, write in Chinese with English API names. Per D-04, section order: Quick Start -> i18n -> API Reference -> Custom Languages.

    Section 1: "快速开始 (Quick Start)"
    - Install command: `pnpm add @lionad/vtu-components`
    - Basic usage: import a component and use it directly
    - Show Terminal component as simplest example with copy-paste code

    Section 2: "多语言 i18n"
    - Explain: zh-CN 默认语言，en fallback，零外部依赖
    - Show LocaleProvider wrapping example:
      ```vue
      <script setup lang="ts">
      import { LocaleProvider } from '@lionad/vtu-components'
      import { zhCN } from '@lionad/vtu-components/locales/zh-CN'
      </script>

      <template>
        <LocaleProvider :messages="zhCN" locale="zh-CN">
          <YourApp />
        </LocaleProvider>
      </template>
      ```
    - Show t() usage in a component:
      ```vue
      <script setup lang="ts">
      import { useI18n } from '@lionad/vtu-core/i18n'
      const { t } = useI18n()
      const copyText = t('shared.copy')  // ComputedRef<string>
      </script>
      <template>
        <button>{{ copyText }}</button>
      </template>
      ```
    - Explain: `t()` returns `ComputedRef<string>`, auto-updates when locale changes

    Section 3: "API 参考 (API Reference)" — condensed quick reference
    - LocaleProvider props table: `messages` (required, object), `locale` (optional, string, default 'zh-CN')
    - useI18n() return: `{ t, locale, setLocale }`
    - t() signature: `t(key: DeepKeyPath, params?: Record<string, ParamValue>) => ComputedRef<string>`
    - Link to full API doc: `见 [API-i18n.md](.planning/docs/API-i18n.md)`

    Section 4: "自定义语言 (Custom Languages)"
    - Show how to create a custom language file (e.g., ja.ts):
      ```typescript
      export const ja = {
        shared: {
          copy: 'コピー',
          copied: 'コピーしました',
          // ... same keys as zh-CN/en
        },
        // ... other namespaces
      } as const
      ```
    - Show how to use it: `<LocaleProvider :messages="ja" locale="ja">`
    - Explain: messages 只需与原文件 key 结构一致即可，不需要导入任何核心包

    Per D-05, README is condensed — link to full docs for details.
  </action>
  <verify>
    <automated>grep -c '多语言\|i18n\|LocaleProvider\|useI18n\|自定义语言' README.md</automated>
  </verify>
  <done>README contains all 4 sections (Quick Start, i18n, API Reference, Custom Languages) with copy-paste examples; Chinese primary text with English API names; links to full API doc</done>
</task>

<task type="auto">
  <name>Task 2: Create full API documentation and consumer onboarding guide</name>
  <files>.planning/docs/API-i18n.md, .planning/docs/CONSUMER-ONBOARDING.md</files>
  <action>
    Create `.planning/docs/API-i18n.md` — full API reference (per D-05):

    Structure:
    1. Overview: i18n system design (provide/inject, zero external deps, zh-CN default)
    2. LocaleProvider component: props table with types, defaults, examples
       - `messages`: `TMessages` (required) — the locale messages object
       - `locale`: `string` (optional, default `'zh-CN'`) — locale identifier
       - Generic type parameter for TypeScript inference
    3. useI18n composable: return type breakdown
       - `t(key, params?) => ComputedRef<string>` — translation function with type-safe keys
       - `locale => ReadonlyRef<string>` — current locale
       - `setLocale(locale: string) => void` — switch locale
    4. t() function: deep dive
       - Key path type inference (DeepKeyPath explanation with examples)
       - Param interpolation: `{param}` syntax, supported types (string, number, boolean, null, undefined)
       - Missing key behavior: dev warns, returns key string
    5. Message file format: show zh-CN.ts structure, explain namespace organization, show how to create new language
    6. Type definitions: DeepKeyPath, DeepValueOf, ParamValue, I18nContext, I18nReturn — each with explanation
    7. Export paths: `@lionad/vtu-components` (LocaleProvider), `@lionad/vtu-core/i18n` (useI18n, types, locales)

    Create `.planning/docs/CONSUMER-ONBOARDING.md` — step-by-step integration guide (DOCS-05):

    Structure:
    1. 第一步：安装组件库 (`pnpm add @lionad/vtu-components`)
    2. 第二步：添加 LocaleProvider（show app-level wrapping with zhCN）
    3. 第三步：在组件中使用 t()（show basic usage with ComputedRef）
    4. 第四步：添加语言切换功能（show setLocale usage）
    5. 扩展：添加新语言（show custom language file creation）
    6. 扩展：覆盖/自定义消息（show merging custom messages）
    7. 零侵入模式：不使用 i18n 时（explain: components still render with built-in zh-CN, no errors, no extra bundle size）
    8. Copy-Paste 模式：直接复制组件文件到项目中的使用方式

    Each step has: code example, expected result, common pitfalls.

    Avoid: Do NOT create documentation outside of .planning/docs/ and README.md. Do NOT generate API doc in Histoire format — that's covered by story files.
  </action>
  <verify>
    <automated>test -f .planning/docs/API-i18n.md && test -f .planning/docs/CONSUMER-ONBOARDING.md && grep -c 'LocaleProvider\|useI18n\|t(' .planning/docs/API-i18n.md</automated>
  </verify>
  <done>Both files exist; API-i18n.md covers all 4 API surfaces (LocaleProvider props, useI18n return, t() signature, message format); CONSUMER-ONBOARDING.md has 8 step-by-step sections including zero-intrusion mode and copy-paste usage</done>
</task>

<task type="auto">
  <name>Task 3: Update story files — props tables with bilingual headers + useStoryLocale for variant titles</name>
  <files>All 29 story files listed in files_modified (see below for groups)</files>
  <action>
    For each of the 29 story files in `src/stories/*.story.vue`, apply these changes:

    **A. Add useStoryLocale import** at top of `<script setup>`:
    ```typescript
    import { useStoryLocale } from './_shared/use-story-locale'
    ```
    Note: path is relative from `src/stories/` → `'./_shared/use-story-locale'`

    **B. Create props locale labels** (for files that have a props table with Name/Type/Description headers):
    ```typescript
    const headerName = useStoryLocale({ zh: '属性名', en: 'Name' })
    const headerType = useStoryLocale({ zh: '类型', en: 'Type' })
    const headerDefault = useStoryLocale({ zh: '默认值', en: 'Default' })
    const headerDesc = useStoryLocale({ zh: '描述', en: 'Description' })
    ```

    **C. Replace table headers** to use the computed labels:
    Change `<th>Name</th>` to `<th>{{ headerName }}</th>`, etc.

    **D. Variant title bilingual text** — for Variant `title="..."`, add a subtitle:
    Add after each `<Variant>` opening tag, a small description showing both languages:
    ```vue
    <Variant title="Props">
      <p class="mb-3 text-xs text-muted-foreground">组件属性说明 / Component props</p>
    ```
    This satisfies D-02 (story descriptions show zh-CN/en).

    **Story files by group (process all 29):**

    Group 1 — Has props table (Name/Type/Default/Description): terminal, chart, data-table, stats-display, weather-widget, code-block, code-diff, order-summary, question-flow, message-draft, audio, video, geo-map, preferences-panel, approval-card, citation, link-preview, x-post, instagram-post, linkedin-post, option-list, parameter-slider, progress-tracker, plan, image, item-carousel
    - Apply A, B, C, D

    Group 2 — No props table, only variants: image-gallery, landing
    - Apply A, D only (skip B and C — no table to modify)

    **Avoid:**
    - Do NOT import from `@lionad/vtu-core/i18n` in story files (per D-03)
    - Do NOT change component usage or logic — only add imports, replace static strings with computed labels, add bilingual subtitle
    - Do NOT modify `<template>` structure beyond header text replacement and subtitle addition
    - Keep all existing examples, variants, and demos exactly as-is
  </action>
  <verify>
    <automated>rg "use-story-locale" src/stories/*.story.vue | wc -l</automated>
  </verify>
  <done>All 29 story files import useStoryLocale; props table headers use computed bilingual labels; each Variant has a bilingual subtitle line; no @lionad/vtu-core/i18n imports in any story file</done>
</task>

</tasks>

<verification>
1. README.md has 4 sections with working code examples (Chinese primary, English API names)
2. `.planning/docs/API-i18n.md` covers all 4 API surfaces with type signatures
3. `.planning/docs/CONSUMER-ONBOARDING.md` has step-by-step guide including zero-intrusion mode
4. All 29 story files use `useStoryLocale` for table headers and have bilingual subtitles
5. Switching Histoire language toggle updates story text in real-time
6. No story file imports `@lionad/vtu-core/i18n`
7. `npx vue-tsc --noEmit` passes for all modified files
</verification>

<success_criteria>
- DOCS-01: README has i18n section with LocaleProvider example, t() usage, custom language example
- DOCS-02: Story descriptions show zh-CN/en via useStoryLocale, update on locale toggle
- DOCS-03: (Verified in Plan 01) Histoire language switcher exists and works
- DOCS-04: API docs cover LocaleProvider props, useI18n return type, t() signature, message file format
- DOCS-05: Consumer onboarding guide has step-by-step i18n integration instructions
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation/05-02-SUMMARY.md`
</output>
