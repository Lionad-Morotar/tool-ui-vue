---
phase: 02-component-i18n-high-medium
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/components/src/order-summary/i18n/zh-CN.json
  - packages/components/src/order-summary/i18n/en.json
  - packages/components/src/question-flow/i18n/zh-CN.json
  - packages/components/src/question-flow/i18n/en.json
  - packages/components/src/order-summary/index.vue
  - packages/components/src/question-flow/index.vue
autonomous: true
requirements:
  - COMPS-01
must_haves:
  truths:
    - "Order summary displays '订单摘要' (zh-CN) or 'Order Summary' (en) as title when no title prop provided"
    - "Order summary pricing labels (Subtotal, Shipping, Total, Free) show localized text"
    - "Order summary malformed payload state shows Chinese error message"
    - "Question flow progress shows '第 X 步 / 共 Y 步' in zh-CN"
    - "Question flow Back/Next/Complete buttons display localized text"
    - "Question flow receipt state shows Chinese completion text"
  artifacts:
    - path: "packages/components/src/order-summary/i18n/zh-CN.json"
      provides: "Order summary zh-CN messages"
      contains: "title, unableToRender, free, malformedPayload"
    - path: "packages/components/src/order-summary/i18n/en.json"
      provides: "Order summary en messages"
      contains: "title, unableToRender, free, malformedPayload"
    - path: "packages/components/src/question-flow/i18n/zh-CN.json"
      provides: "Question flow zh-CN messages"
      contains: "completed, complete, step, back, next"
    - path: "packages/components/src/question-flow/i18n/en.json"
      provides: "Question flow en messages"
      contains: "completed, complete, step, back, next"
    - path: "packages/components/src/order-summary/index.vue"
      provides: "Order summary with useI18n"
      contains: "useI18n, t('orderSummary."
    - path: "packages/components/src/question-flow/index.vue"
      provides: "Question flow with useI18n"
      contains: "useI18n, t('questionFlow."
  key_links:
    - from: "packages/components/src/order-summary/index.vue"
      to: "@lionad/vtu-core/i18n"
      via: "import { useI18n }"
      pattern: "useI18n\\(\\)"
    - from: "packages/components/src/question-flow/index.vue"
      to: "@lionad/vtu-core/i18n"
      via: "import { useI18n }"
      pattern: "useI18n\\(\\)"
---

<objective>
Enable i18n for order-summary and question-flow components (2 high-priority). Create i18n JSON files, wire useI18n(), replace hardcoded strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-component-i18n-high-medium/02-CONTEXT.md
@.planning/phases/01-i18n-core/01-SUMMARY.md
@packages/core/src/i18n/use-i18n.ts
@packages/core/src/i18n/locales/zh-CN.ts
@packages/components/src/order-summary/index.vue
@packages/components/src/question-flow/index.vue
</context>

<interfaces>
Same useI18n interface as Plan 01:
```typescript
import { useI18n } from '@lionad/vtu-core/i18n'
const { t } = useI18n()
// t(key, params?) returns ComputedRef<string>, auto-unwrapped in templates
```

Critical: Per D-08/D-09/D-10, do NOT i18n-ize schema fields:
- pricing.discountLabel — used as-is (fallback to t() key only if NOT provided)
- pricing.taxLabel — used as-is (fallback to t() key only if NOT provided)
- Schema prop values are consumed directly, NOT translated
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create i18n JSON files for order-summary and question-flow</name>
  <files>
    packages/components/src/order-summary/i18n/zh-CN.json
    packages/components/src/order-summary/i18n/en.json
    packages/components/src/question-flow/i18n/zh-CN.json
    packages/components/src/question-flow/i18n/en.json
  </files>
  <action>
    Create i18n/ directories and 4 JSON files.

    **packages/components/src/order-summary/i18n/zh-CN.json**:
    ```json
    {
      "orderSummary": {
        "title": "订单摘要",
        "unableToRender": "无法渲染订单摘要",
        "malformedPayload": "数据格式错误",
        "free": "免费"
      }
    }
    ```

    **packages/components/src/order-summary/i18n/en.json**:
    ```json
    {
      "orderSummary": {
        "title": "Order Summary",
        "unableToRender": "Unable to render order summary",
        "malformedPayload": "Malformed payload",
        "free": "Free"
      }
    }
    ```

    **packages/components/src/question-flow/i18n/zh-CN.json**:
    ```json
    {
      "questionFlow": {
        "completed": "已完成",
        "complete": "完成",
        "step": "第 {current} 步 / 共 {total} 步",
        "back": "返回",
        "next": "下一步",
        "receiptStatus": "已确认"
      }
    }
    ```

    **packages/components/src/question-flow/i18n/en.json**:
    ```json
    {
      "questionFlow": {
        "completed": "Completed",
        "complete": "Complete",
        "step": "Step {current} of {total}",
        "back": "Back",
        "next": "Next",
        "receiptStatus": "Confirmed"
      }
    }
    ```
  </action>
  <verify>
    <automated>node -e "['order-summary','question-flow'].forEach(f => ['zh-CN','en'].forEach(l => { const p = 'packages/components/src/'+f+'/i18n/'+l+'.json'; const d = JSON.parse(require('fs').readFileSync(p,'utf8')); console.log(f+'/'+l+':', JSON.stringify(Object.keys(Object.values(d)[0]))) }))"</automated>
  </verify>
  <done>
    4 JSON files created with correct namespaces (orderSummary=4 keys, questionFlow=6 keys) in both zh-CN and en
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire useI18n() into order-summary component</name>
  <files>
    packages/components/src/order-summary/index.vue
  </files>
  <read_first>
    packages/components/src/order-summary/index.vue
    packages/core/src/i18n/use-i18n.ts
  </read_first>
  <action>
    In packages/components/src/order-summary/index.vue:

    1. Add import after `import { useOrderSummary } from './states';`: `import { useI18n } from '@lionad/vtu-core/i18n'`
    2. After `const state = reactive(useOrderSummary(props));`, add: `const { t } = useI18n()`
    3. Replace hardcoded strings:

       **Lines 30, 65, 158 (title fallback — appears 3 times):**
       BEFORE: `{{ title || "Order Summary" }}`
       AFTER:  `{{ title || t('orderSummary.title') }}`

       **Line 33 (malformed payload error message):**
       BEFORE: `Unable to render order summary`
       AFTER:  `{{ t('orderSummary.unableToRender') }}`

       **Lines 124, 213 (shipping free fallback in ternary):**
       BEFORE: `pricing.shipping === 0 ? 'Free' : state.formatCurrency(...)`
       AFTER:  `pricing.shipping === 0 ? t('orderSummary.free') : state.formatCurrency(...)`

       **Lines 112, 201 (Subtotal label):**
       BEFORE: `<dt class="text-muted-foreground">Subtotal</dt>`
       AFTER:  `<dt class="text-muted-foreground">{{ t('orderSummary.subtotal') }}</dt>`
       Note: The zh-CN locale already has shared.subtotal = '小计'. Use orderSummary.subtotal key.
       WAIT — the orderSummary i18n JSON does NOT have a "subtotal" key. Add it:
       Add to orderSummary zh-CN: `"subtotal": "小计"` and en: `"subtotal": "Subtotal"`
       Actually, looking at the existing core locale (zh-CN.ts line 8), shared.subtotal = '小计' already exists.
       The orderSummary component uses "Subtotal" as a static label. Add `subtotal` to the component's i18n JSON:
       - zh-CN: `"subtotal": "小计"`
       - en: `"subtotal": "Subtotal"`

       **Lines 122, 211 (Shipping label):**
       BEFORE: `<dt class="text-muted-foreground">Shipping</dt>`
       AFTER:  `<dt class="text-muted-foreground">{{ t('orderSummary.shipping') }}</dt>`
       Add to i18n JSON — zh-CN: `"shipping": "运费"`, en: `"shipping": "Shipping"`

       **Lines 134, 223 (Total label):**
       BEFORE: `<dt class="font-medium">Total</dt>`
       AFTER:  `<dt class="font-medium">{{ t('orderSummary.total') }}</dt>`
       Add to i18n JSON — zh-CN: `"total": "总计"`, en: `"total": "Total"`

       **Lines 117, 206 (discountLabel fallback):**
       BEFORE: `{{ pricing.discountLabel || "Discount" }}`
       AFTER:  `{{ pricing.discountLabel || t('orderSummary.discount') }}`
       Add to i18n JSON — zh-CN: `"discount": "折扣"`, en: `"discount": "Discount"`
       NOTE: Per D-08, discountLabel prop value is used as-is. Only the fallback is i18n-ized.

       **Lines 129, 218 (taxLabel fallback):**
       BEFORE: `{{ pricing.taxLabel || "Tax" }}`
       AFTER:  `{{ pricing.taxLabel || t('orderSummary.tax') }}`
       Add to i18n JSON — zh-CN: `"tax": "税费"`, en: `"tax": "Tax"`
       NOTE: Per D-08, taxLabel prop value is used as-is. Only the fallback is i18n-ized.

       **Line 113 (Subtotal value display):**
       This renders `state.formatCurrency(pricing.subtotal, pricing.currency)` — no i18n needed (numeric formatting).

       UPDATE the orderSummary JSON files to include all keys:
       zh-CN: `{ "orderSummary": { "title": "订单摘要", "unableToRender": "无法渲染订单摘要", "malformedPayload": "数据格式错误", "free": "免费", "subtotal": "小计", "shipping": "运费", "total": "总计", "discount": "折扣", "tax": "税费" } }`
       en: `{ "orderSummary": { "title": "Order Summary", "unableToRender": "Unable to render order summary", "malformedPayload": "Malformed payload", "free": "Free", "subtotal": "Subtotal", "shipping": "Shipping", "total": "Total", "discount": "Discount", "tax": "Tax" } }`

       IMPORTANT: Rewrite the JSON files from Task 1 with the complete key set before proceeding.
  </action>
  <verify>
    <automated>grep -c "useI18n" packages/components/src/order-summary/index.vue | grep -v "^0$" && grep -cE '"Order Summary"|"Unable to render|"Discount"|"Free"|"Subtotal"|"Shipping"|"Total"|"Tax"' packages/components/src/order-summary/index.vue | grep "^0$"</automated>
  </verify>
  <done>
    order-summary imports useI18n, all 9 hardcoded English strings replaced with t() calls (discountLabel/taxLabel fallbacks only), schema prop values used as-is per D-08
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire useI18n() into question-flow component</name>
  <files>
    packages/components/src/question-flow/index.vue
  </files>
  <read_first>
    packages/components/src/question-flow/index.vue
    packages/core/src/i18n/use-i18n.ts
  </read_first>
  <action>
    In packages/components/src/question-flow/index.vue:

    1. Add import after `import { useQuestionFlow } from './states';`: `import { useI18n } from '@lionad/vtu-core/i18n'`
    2. After `const state = reactive(useQuestionFlow(props, emit));`, add: `const { t } = useI18n()`
    3. Replace hardcoded strings:

       **Line 57 (receipt title fallback):**
       BEFORE: `{{ state.receiptProps.choice?.title ?? 'Completed' }}`
       AFTER:  `{{ state.receiptProps.choice?.title ?? t('questionFlow.completed') }}`

       **Line 60 (receipt status badge text):**
       BEFORE: `Complete`
       AFTER:  `{{ t('questionFlow.complete') }}`

       **Line 105 (step progress — appears twice, the aria-label and visible text):**
       The visible text at line 105:
       BEFORE: `Step {{ state.currentStepNumber }} of {{ state.totalSteps }}`
       AFTER:  `{{ t('questionFlow.step', { current: state.currentStepNumber, total: state.totalSteps }) }}`

       The aria-label at line 103:
       BEFORE: `:aria-label="\`Step ${state.currentStepNumber} of ${state.totalSteps}\`"`
       AFTER:  `:aria-label="t('questionFlow.step', { current: state.currentStepNumber, total: state.totalSteps })"`

       **Line 335 (Back button text):**
       BEFORE: `Back`
       AFTER:  `{{ t('questionFlow.back') }}`

       **Line 351 (Complete/Next button ternary):**
       BEFORE: `{{ state.isLastStep ? 'Complete' : 'Next' }}`
       AFTER:  `{{ state.isLastStep ? t('questionFlow.complete') : t('questionFlow.next') }}`

       **Line 320 (Back button v-if condition stays unchanged — logic, not text)**

    Note: The `Step X of Y` text appears in BOTH the visible span (line 105) and the aria-label (line 103). Replace BOTH.
  </action>
  <verify>
    <automated>grep -c "useI18n" packages/components/src/question-flow/index.vue | grep -v "^0$" && grep -cE "'Completed'|'Complete'|'Back'|'Next'|'Step " packages/components/src/question-flow/index.vue | grep "^0$"</automated>
  </verify>
  <done>
    question-flow imports useI18n, all 6 hardcoded English strings replaced with t() calls, step counter uses {current} and {total} params
  </done>
</task>

</tasks>

<verification>
- 4 JSON files exist with correct keys (orderSummary=9 keys, questionFlow=6 keys)
- order-summary/index.vue: grep "useI18n" >= 1; grep '"Order Summary"' = 0; grep '"Discount"' = 0; grep '"Free"' = 0; grep '"Subtotal"' = 0; grep '"Shipping"' = 0; grep '"Total"' = 0; grep '"Tax"' = 0; grep "Unable to render" = 0
- question-flow/index.vue: grep "useI18n" >= 1; grep "'Completed'" = 0; grep "'Complete'" = 0; grep "'Back'" = 0; grep "'Next'" = 0; grep "'Step " = 0
- pricing.discountLabel and pricing.taxLabel fallback to t() but prop values used as-is (D-08)
</verification>

<success_criteria>
- order-summary: 9 i18n keys in zh-CN.json and en.json, all template strings use t() or prop values
- question-flow: 6 i18n keys in zh-CN.json and en.json, all template strings use t()
- Zero hardcoded English UI strings in templates (schema prop fallbacks use t())
- Both components import and call useI18n()
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-i18n-high-medium/02-02-SUMMARY.md`
</output>
