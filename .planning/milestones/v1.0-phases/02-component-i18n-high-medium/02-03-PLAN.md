---
phase: 02-component-i18n-high-medium
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/components/src/data-table/i18n/zh-CN.json
  - packages/components/src/data-table/i18n/en.json
  - packages/components/src/message-draft/i18n/zh-CN.json
  - packages/components/src/message-draft/i18n/en.json
  - packages/components/src/data-table/index.vue
  - packages/components/src/message-draft/cmpts/message-draft.vue
autonomous: true
requirements:
  - COMPS-01
must_haves:
  truths:
    - "Data table empty state shows Chinese text when no emptyMessage prop"
    - "Data table mobile card view has localized ARIA label and description"
    - "Data table array overflow shows '还有 X 项' in zh-CN"
    - "Data table null values display localized null text"
    - "Message-draft email metadata labels (From, To, Cc, Bcc) show Chinese"
    - "Message-draft action buttons (Send, Undo, Cancel) display localized text"
    - "Message-draft expand/collapse shows '查看更多'/'收起' in zh-CN"
  artifacts:
    - path: "packages/components/src/data-table/i18n/zh-CN.json"
      provides: "Data table zh-CN messages"
      contains: "noDataAvailable, nullLabel, moreItems, moreCount, mobileViewLabel, mobileViewDescription"
    - path: "packages/components/src/message-draft/i18n/zh-CN.json"
      provides: "Message draft zh-CN messages"
      contains: "messageSent, sentAt, fromLabel, toLabel, ccLabel, bccLabel, showLess, readMore, sendingIn, undo, cancel, send, members"
    - path: "packages/components/src/data-table/index.vue"
      provides: "Data table with useI18n"
      contains: "useI18n, t('dataTable."
    - path: "packages/components/src/message-draft/cmpts/message-draft.vue"
      provides: "Message draft with useI18n"
      contains: "useI18n, t('messageDraft."
  key_links:
    - from: "packages/components/src/data-table/index.vue"
      to: "@lionad/vtu-core/i18n"
      via: "import { useI18n }"
      pattern: "useI18n\\(\\)"
    - from: "packages/components/src/message-draft/cmpts/message-draft.vue"
      to: "@lionad/vtu-core/i18n"
      via: "import { useI18n }"
      pattern: "useI18n\\(\\)"
---

<objective>
Enable i18n for data-table and message-draft components (2 high-priority). Create i18n JSON files, wire useI18n(), replace hardcoded strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-component-i18n-high-medium/02-CONTEXT.md
@.planning/phases/01-i18n-core/01-SUMMARY.md
@packages/core/src/i18n/use-i18n.ts
@packages/core/src/i18n/locales/zh-CN.ts
@packages/components/src/data-table/index.vue
@packages/components/src/message-draft/cmpts/message-draft.vue
</context>

<interfaces>
Same useI18n interface. Note: message-draft's main component is at `cmpts/message-draft.vue`, not `index.vue`.

Per D-08: Schema fields like emptyMessage are NOT i18n-ized. Only the fallback value when emptyMessage is not provided.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create i18n JSON files for data-table and message-draft</name>
  <files>
    packages/components/src/data-table/i18n/zh-CN.json
    packages/components/src/data-table/i18n/en.json
    packages/components/src/message-draft/i18n/zh-CN.json
    packages/components/src/message-draft/i18n/en.json
  </files>
  <action>
    Create i18n/ directories and 4 JSON files.

    **packages/components/src/data-table/i18n/zh-CN.json**:
    ```json
    {
      "dataTable": {
        "noDataAvailable": "暂无数据",
        "nullLabel": "空",
        "moreItems": "项",
        "moreCount": "还有 {count} 项",
        "mobileViewLabel": "数据表格（移动端卡片视图）",
        "mobileViewDescription": "表格数据以可展开卡片形式显示。每张卡片代表一行数据。",
        "sortAscending": "升序",
        "sortDescending": "降序",
        "opensInNewTab": "在新标签页中打开"
      }
    }
    ```

    **packages/components/src/data-table/i18n/en.json**:
    ```json
    {
      "dataTable": {
        "noDataAvailable": "No data available",
        "nullLabel": "null",
        "moreItems": "more",
        "moreCount": "+{count} more",
        "mobileViewLabel": "Data table (mobile card view)",
        "mobileViewDescription": "Table data shown as expandable cards. Each card represents one row.",
        "sortAscending": "ascending",
        "sortDescending": "descending",
        "opensInNewTab": "Opens in new tab"
      }
    }
    ```

    **packages/components/src/message-draft/i18n/zh-CN.json**:
    ```json
    {
      "messageDraft": {
        "messageSent": "消息已发送",
        "sentAt": "发送于 {time}",
        "fromLabel": "发件人",
        "toLabel": "收件人",
        "ccLabel": "抄送",
        "bccLabel": "密送",
        "moreRecipients": "+{count} 更多",
        "members": "{count} 位成员",
        "showLess": "收起显示",
        "readMore": "查看更多",
        "sendingIn": "{count} 秒后发送",
        "undo": "撤销",
        "cancel": "取消",
        "send": "发送"
      }
    }
    ```

    **packages/components/src/message-draft/i18n/en.json**:
    ```json
    {
      "messageDraft": {
        "messageSent": "Message sent",
        "sentAt": "Sent at {time}",
        "fromLabel": "From",
        "toLabel": "To",
        "ccLabel": "Cc",
        "bccLabel": "Bcc",
        "moreRecipients": "+{count} more",
        "members": "{count} members",
        "showLess": "Show less",
        "readMore": "Read more",
        "sendingIn": "Sending in {count}s",
        "undo": "Undo",
        "cancel": "Cancel",
        "send": "Send"
      }
    }
    ```
  </action>
  <verify>
    <automated>node -e "['data-table','message-draft'].forEach(f => ['zh-CN','en'].forEach(l => { const p = 'packages/components/src/'+f+'/i18n/'+l+'.json'; const d = JSON.parse(require('fs').readFileSync(p,'utf8')); console.log(f+'/'+l+':', Object.keys(Object.values(d)[0]).length, 'keys') }))"</automated>
  </verify>
  <done>
    4 JSON files created (dataTable=8 keys, messageDraft=14 keys) in both zh-CN and en with matching key structures
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire useI18n() into data-table component</name>
  <files>
    packages/components/src/data-table/index.vue
  </files>
  <read_first>
    packages/components/src/data-table/index.vue
    packages/core/src/i18n/use-i18n.ts
  </read_first>
  <action>
    In packages/components/src/data-table/index.vue:

    1. Add import after `import { useDataTable } from './states';`: `import { useI18n } from '@lionad/vtu-core/i18n'`
    2. After `const state = reactive(useDataTable(props, emit));`, add: `const { t } = useI18n()`
    3. Replace hardcoded strings:

       **Lines 64, 253 (empty message fallback — appears twice):**
       BEFORE: `{{ emptyMessage || "No data available" }}`
       AFTER:  `{{ emptyMessage || t('dataTable.noDataAvailable') }}`

       **Lines 198, 408 (null value display in array format):**
       BEFORE: `{{ item === null ? "null" : String(item) }}`
       AFTER:  `{{ item === null ? t('dataTable.nullLabel') : String(item) }}`

       **Lines 204, 414 (array overflow count):**
       BEFORE: `+{{ state.getArrayItems(...).remaining }} more`
       AFTER:  `{{ t('dataTable.moreCount', { count: state.getArrayItems(row[column.key], column.format.maxVisible).remaining }) }}`

       **Line 242 (mobile view ARIA label):**
       BEFORE: `:aria-label="\`Data table (mobile card view)\`"`
       AFTER:  `:aria-label="t('dataTable.mobileViewLabel')"`

       **Line 246-248 (mobile view description):**
       BEFORE: `Table data shown as expandable cards. Each card represents one row. Columns: {{ ... }}.`
       AFTER:  `{{ t('dataTable.mobileViewDescription') }}<template v-if="columns.length > 0"> {{ t('dataTable.columnsLabel') }}: {{ columns.map((c) => c.label).join(", ") }}.</template>`
       ADD key `columnsLabel` to JSON — zh-CN: "列", en: "Columns"
       Actually, the description text is for screen readers and the columns list is dynamic. Simplify:
       Replace entire sr-only div content:
       BEFORE: `Table data shown as expandable cards. Each card represents one row. <template v-if="columns.length > 0"> Columns: {{ columns.map((c) => c.label).join(", ") }}. </template>`
       AFTER:  `{{ t('dataTable.mobileViewDescription') }}<template v-if="columns.length > 0"> {{ t('dataTable.columnsLabel') }}: {{ columns.map((c) => c.label).join(", ") }}.</template>`
       ADD to JSON — zh-CN columnsLabel: "列", en: "Columns"

       **Line 102 (sort aria-label ascending/descending):**
       BEFORE: `` (state.currentSort?.direction === 'asc' ? 'ascending' : 'descending') ``
       AFTER:  `` (state.currentSort?.direction === 'asc' ? t('dataTable.sortAscending') : t('dataTable.sortDescending')) ``

       **Line 182, 396 (external link ARIA — opens in new tab):**
       Line 182: `column.format.external ? \`${state.formatCellValue(...)} (opens in a new tab)\` : undefined`
       AFTER: `column.format.external ? t('dataTable.opensInNewTab', { label: state.formatCellValue(row[column.key], column) }) : undefined`
       Actually, the opensInNewTab key needs the label interpolated. Change the key to accept a param:
       zh-CN: `opensInNewTab: "{label}（在新标签页中打开）"`
       en: `opensInNewTab: "{label} (opens in new tab)"`
       Then use: `t('dataTable.opensInNewTab', { label: state.formatCellValue(row[column.key], column) })`

       Wait — the existing line 182 constructs the aria-label with string interpolation. Let me replace it properly:
       BEFORE: `` :aria-label="column.format.external ? `${state.formatCellValue(row[column.key], column)} (opens in a new tab)` : undefined" ``
       AFTER:  `` :aria-label="column.format.external ? t('dataTable.opensInNewTab', { label: state.formatCellValue(row[column.key], column) }) : undefined" ``

       Update the JSON opensInNewTab key to accept {label} param:
       zh-CN: `"opensInNewTab": "{label}（在新标签页中打开）"`
       en: `"opensInNewTab": "{label} (opens in new tab)"`

    UPDATE the data-table JSON files with the corrected opensInNewTab and added columnsLabel:
    zh-CN: add `"columnsLabel": "列"`, update `"opensInNewTab": "{label}（在新标签页中打开）"`
    en: add `"columnsLabel": "Columns"`, update `"opensInNewTab": "{label} (opens in new tab)"`

    NOTE: Column labels (column.label), column.abbr, and format values are data from props — NOT i18n-ized. Only component-generated UI text is replaced.
  </action>
  <verify>
    <automated>grep -c "useI18n" packages/components/src/data-table/index.vue | grep -v "^0$" && grep -cE '"No data available"|"null"| more|"Data table \(mobile|"Table data shown|"ascending"|"descending"|"opens in a new tab"' packages/components/src/data-table/index.vue | grep "^0$"</automated>
  </verify>
  <done>
    data-table imports useI18n, all hardcoded English UI strings replaced with t() calls, column data labels used as-is (not i18n-ized)
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire useI18n() into message-draft component</name>
  <files>
    packages/components/src/message-draft/cmpts/message-draft.vue
  </files>
  <read_first>
    packages/components/src/message-draft/cmpts/message-draft.vue
    packages/core/src/i18n/use-i18n.ts
  </read_first>
  <action>
    In packages/components/src/message-draft/cmpts/message-draft.vue:

    1. Add import after `import { useMessageDraft } from '../states';`: `import { useI18n } from '@lionad/vtu-core/i18n'`
    2. After `const state = reactive(useMessageDraft(props, emit));`, add: `const { t } = useI18n()`
    3. Replace hardcoded strings:

       **Line 50 (sent receipt ARIA label):**
       BEFORE: `aria-label="Message sent"`
       AFTER:  `:aria-label="t('messageDraft.messageSent')"`

       **Line 54 (sent time label):**
       BEFORE: `Sent at {{ state.formatSentTime(state.sentAt ?? new Date()) }}`
       AFTER:  `{{ t('messageDraft.sentAt', { time: state.formatSentTime(state.sentAt ?? new Date()) }) }}`

       **Line 111 (From label):**
       BEFORE: `From`
       AFTER:  `{{ t('messageDraft.fromLabel') }}`

       **Line 118 (To label):**
       BEFORE: `To`
       AFTER:  `{{ t('messageDraft.toLabel') }}`

       **Lines 127, 145, 167 (more recipients):**
       BEFORE: `+{{ (state.emailProps.to || []).length - 3 }} more`
       AFTER:  `{{ t('messageDraft.moreRecipients', { count: (state.emailProps.to || []).length - 3 }) }}`
       Same pattern for cc and bcc overflow lines.

       **Line 136 (Cc label):**
       BEFORE: `Cc`
       AFTER:  `{{ t('messageDraft.ccLabel') }}`

       **Line 154 (Bcc label):**
       BEFORE: `Bcc`
       AFTER:  `{{ t('messageDraft.bccLabel') }}`

       **Line 241 (Slack members count):**
       BEFORE: `{{ state.slackProps.target.memberCount.toLocaleString() }} members`
       AFTER:  `{{ t('messageDraft.members', { count: state.slackProps.target.memberCount.toLocaleString() }) }}`

       **Line 294 (expand/collapse button):**
       BEFORE: `{{ isExpanded ? "Show less" : "Read more" }}`
       AFTER:  `{{ isExpanded ? t('messageDraft.showLess') : t('messageDraft.readMore') }}`

       **Line 321 (sending countdown):**
       BEFORE: `Sending in {{ state.countdown }}s`
       AFTER:  `{{ t('messageDraft.sendingIn', { count: state.countdown }) }}`

       **Line 337 (Undo button):**
       BEFORE: `Undo`
       AFTER:  `{{ t('messageDraft.undo') }}`

       **Line 364 (Cancel button):**
       BEFORE: `Cancel`
       AFTER:  `{{ t('messageDraft.cancel') }}`

       **Line 378 (Send button):**
       BEFORE: `Send`
       AFTER:  `{{ t('messageDraft.send') }}`

    NOTE: Email body text (state.emailProps.body), Slack body (state.slackProps.body), and subject are user-provided content — NOT i18n-ized.
  </action>
  <verify>
    <automated>grep -c "useI18n" packages/components/src/message-draft/cmpts/message-draft.vue | grep -v "^0$" && grep -cE '"Message sent"|"Sent at "|"From"|"To"|"Cc"|"Bcc"|" more"|"Show less"|"Read more"|"Sending in "|"Undo"|"Cancel"|"Send"' packages/components/src/message-draft/cmpts/message-draft.vue | grep "^0$"</automated>
  </verify>
  <done>
    message-draft imports useI18n, all 14 hardcoded English strings replaced with t() calls, user content (email body, subject, Slack body) left unchanged
  </done>
</task>

</tasks>

<verification>
- 4 JSON files exist with correct keys (dataTable=9 keys after update, messageDraft=14 keys)
- data-table/index.vue: grep "useI18n" >= 1; grep '"No data available"' = 0; grep '"null"' = 0; grep ' more' = 0; grep '"Data table' = 0
- message-draft/cmpts/message-draft.vue: grep "useI18n" >= 1; grep '"Message sent"' = 0; grep '"From"' = 0; grep '"To"' = 0; grep '"Undo"' = 0; grep '"Cancel"' = 0; grep '"Send"' = 0
- Column labels, email body, Slack body, subject NOT modified (data, not UI text)
</verification>

<success_criteria>
- data-table: 9 i18n keys in zh-CN.json and en.json, all template strings use t()
- message-draft: 14 i18n keys in zh-CN.json and en.json, all template strings use t()
- Zero hardcoded English UI strings in templates
- Both components import and call useI18n()
- Column labels and user content (email/Slack body) left unchanged (not i18n targets)
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-i18n-high-medium/02-03-SUMMARY.md`
</output>
