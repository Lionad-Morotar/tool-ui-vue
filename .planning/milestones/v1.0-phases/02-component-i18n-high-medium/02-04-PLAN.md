---
phase: 02-component-i18n-high-medium
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - packages/components/src/audio/i18n/zh-CN.json
  - packages/components/src/audio/i18n/en.json
  - packages/components/src/video/i18n/zh-CN.json
  - packages/components/src/video/i18n/en.json
  - packages/components/src/geo-map/i18n/zh-CN.json
  - packages/components/src/geo-map/i18n/en.json
  - packages/components/src/item-carousel/i18n/zh-CN.json
  - packages/components/src/item-carousel/i18n/en.json
  - packages/components/src/preferences-panel/i18n/zh-CN.json
  - packages/components/src/preferences-panel/i18n/en.json
  - packages/components/src/audio/index.vue
  - packages/components/src/video/index.vue
  - packages/components/src/geo-map/index.vue
  - packages/components/src/item-carousel/index.vue
  - packages/components/src/preferences-panel/index.vue
autonomous: true
requirements:
  - COMPS-02
must_haves:
  truths:
    - "Audio play/pause button ARIA label switches between Chinese texts based on playing state"
    - "Video shows Chinese browser unsupported message and button labels"
    - "Geo-map loading indicator shows Chinese text"
    - "Item-carousel empty state, navigation buttons, and pagination show Chinese"
    - "Preferences-panel status badges (Error/Saved) and ARIA labels show Chinese"
    - "All medium priority components respond to locale switching without refresh"
  artifacts:
    - path: "packages/components/src/audio/i18n/zh-CN.json"
      provides: "Audio zh-CN messages (2 keys)"
      contains: "play, pause"
    - path: "packages/components/src/video/i18n/zh-CN.json"
      provides: "Video zh-CN messages (5 keys)"
      contains: "unsupportedBrowser, videoControls, open, pause, watch"
    - path: "packages/components/src/geo-map/i18n/zh-CN.json"
      provides: "Geo-map zh-CN messages (1 key)"
      contains: "loadingMap"
    - path: "packages/components/src/item-carousel/i18n/zh-CN.json"
      provides: "Item carousel zh-CN messages (7 keys)"
      contains: "noItems, itemCarouselLabel, scrollLeft, scrollRight, itemOf, paginationLabel, goToSlide"
    - path: "packages/components/src/preferences-panel/i18n/zh-CN.json"
      provides: "Preferences panel zh-CN messages (4 keys)"
      contains: "preferencesWithErrors, confirmedPreferences, error, saved"
    - path: "packages/components/src/audio/index.vue"
      provides: "Audio with useI18n"
      contains: "useI18n, t('audio."
    - path: "packages/components/src/preferences-panel/index.vue"
      provides: "Preferences panel with useI18n"
      contains: "useI18n, t('preferencesPanel."
  key_links:
    - from: "packages/components/src/audio/index.vue"
      to: "@lionad/vtu-core/i18n"
      via: "import { useI18n }"
      pattern: "useI18n\\(\\)"
    - from: "packages/components/src/video/index.vue"
      to: "@lionad/vtu-core/i18n"
      via: "import { useI18n }"
      pattern: "useI18n\\(\\)"
    - from: "packages/components/src/item-carousel/index.vue"
      to: "@lionad/vtu-core/i18n"
      via: "import { useI18n }"
      pattern: "useI18n\\(\\)"
---

<objective>
Enable i18n for 6 medium-priority components (audio, video, geo-map, item-carousel, preferences-panel). Note: image-gallery has NO hardcoded English strings — skip it. Create i18n JSON files, wire useI18n(), replace hardcoded strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-component-i18n-high-medium/02-CONTEXT.md
@.planning/phases/01-i18n-core/01-SUMMARY.md
@packages/core/src/i18n/use-i18n.ts
@packages/core/src/i18n/locales/zh-CN.ts
@packages/components/src/audio/index.vue
@packages/components/src/video/index.vue
@packages/components/src/geo-map/index.vue
@packages/components/src/item-carousel/index.vue
@packages/components/src/preferences-panel/index.vue
</context>

<interfaces>
Same useI18n interface as previous plans. 5 components need i18n wiring (image-gallery has zero hardcoded strings).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create i18n JSON files for audio, video, geo-map</name>
  <files>
    packages/components/src/audio/i18n/zh-CN.json
    packages/components/src/audio/i18n/en.json
    packages/components/src/video/i18n/zh-CN.json
    packages/components/src/video/i18n/en.json
    packages/components/src/geo-map/i18n/zh-CN.json
    packages/components/src/geo-map/i18n/en.json
  </files>
  <action>
    Create i18n/ directories and 6 JSON files.

    **packages/components/src/audio/i18n/zh-CN.json**:
    ```json
    {
      "audio": {
        "play": "播放",
        "pause": "暂停"
      }
    }
    ```

    **packages/components/src/audio/i18n/en.json**:
    ```json
    {
      "audio": {
        "play": "Play",
        "pause": "Pause"
      }
    }
    ```

    **packages/components/src/video/i18n/zh-CN.json**:
    ```json
    {
      "video": {
        "unsupportedBrowser": "您的浏览器不支持视频播放。",
        "videoControls": "视频控制",
        "open": "打开",
        "pause": "暂停",
        "watch": "观看"
      }
    }
    ```

    **packages/components/src/video/i18n/en.json**:
    ```json
    {
      "video": {
        "unsupportedBrowser": "Your browser does not support the video element.",
        "videoControls": "Video controls",
        "open": "Open",
        "pause": "Pause",
        "watch": "Watch"
      }
    }
    ```

    **packages/components/src/geo-map/i18n/zh-CN.json**:
    ```json
    {
      "geoMap": {
        "loadingMap": "地图加载中..."
      }
    }
    ```

    **packages/components/src/geo-map/i18n/en.json**:
    ```json
    {
      "geoMap": {
        "loadingMap": "Loading map..."
      }
    }
    ```
  </action>
  <verify>
    <automated>node -e "['audio','video','geo-map'].forEach(f => ['zh-CN','en'].forEach(l => { const p = 'packages/components/src/'+f+'/i18n/'+l+'.json'; const d = JSON.parse(require('fs').readFileSync(p,'utf8')); console.log(f+'/'+l+':', Object.keys(Object.values(d)[0]).join(', ')) }))"</automated>
  </verify>
  <done>
    6 JSON files created (audio=2 keys, video=5 keys, geoMap=1 key) in both zh-CN and en
  </done>
</task>

<task type="auto">
  <name>Task 2: Create i18n JSON files for item-carousel and preferences-panel</name>
  <files>
    packages/components/src/item-carousel/i18n/zh-CN.json
    packages/components/src/item-carousel/i18n/en.json
    packages/components/src/preferences-panel/i18n/zh-CN.json
    packages/components/src/preferences-panel/i18n/en.json
  </files>
  <action>
    Create i18n/ directories and 4 JSON files.

    **packages/components/src/item-carousel/i18n/zh-CN.json**:
    ```json
    {
      "itemCarousel": {
        "noItems": "暂无内容",
        "itemCarouselLabel": "商品轮播",
        "scrollLeft": "向左滚动",
        "scrollRight": "向右滚动",
        "itemOf": "第 {current} / 共 {total} 项",
        "paginationLabel": "轮播分页",
        "goToSlide": "跳转到第 {slide} 页"
      }
    }
    ```

    **packages/components/src/item-carousel/i18n/en.json**:
    ```json
    {
      "itemCarousel": {
        "noItems": "No items to display",
        "itemCarouselLabel": "Item carousel",
        "scrollLeft": "Scroll left",
        "scrollRight": "Scroll right",
        "itemOf": "Item {current} of {total}",
        "paginationLabel": "Carousel pagination",
        "goToSlide": "Go to slide {slide}"
      }
    }
    ```

    **packages/components/src/preferences-panel/i18n/zh-CN.json**:
    ```json
    {
      "preferencesPanel": {
        "preferencesWithErrors": "存在错误的偏好设置",
        "confirmedPreferences": "已确认的偏好设置",
        "error": "错误",
        "saved": "已保存"
      }
    }
    ```

    **packages/components/src/preferences-panel/i18n/en.json**:
    ```json
    {
      "preferencesPanel": {
        "preferencesWithErrors": "Preferences with errors",
        "confirmedPreferences": "Confirmed preferences",
        "error": "Error",
        "saved": "Saved"
      }
    }
    ```
  </action>
  <verify>
    <automated>node -e "['item-carousel','preferences-panel'].forEach(f => ['zh-CN','en'].forEach(l => { const p = 'packages/components/src/'+f+'/i18n/'+l+'.json'; const d = JSON.parse(require('fs').readFileSync(p,'utf8')); console.log(f+'/'+l+':', Object.keys(Object.values(d)[0]).join(', ')) }))"</automated>
  </verify>
  <done>
    4 JSON files created (itemCarousel=7 keys, preferencesPanel=4 keys) in both zh-CN and en
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire useI18n() into audio, video, geo-map components</name>
  <files>
    packages/components/src/audio/index.vue
    packages/components/src/video/index.vue
    packages/components/src/geo-map/index.vue
  </files>
  <read_first>
    packages/components/src/audio/index.vue
    packages/components/src/video/index.vue
    packages/components/src/geo-map/index.vue
    packages/core/src/i18n/use-i18n.ts
  </read_first>
  <action>
    **audio/index.vue:**
    1. Add import after `import { useAudio } from './states';`: `import { useI18n } from '@lionad/vtu-core/i18n'`
    2. In the destructuring of useAudio result, keep as-is. Add `const { t } = useI18n()` after it.
    3. Replace strings:

       Lines 118, 212 (play/pause aria-label in ternary):
       BEFORE: `:aria-label="playing ? 'Pause' : 'Play'"`
       AFTER:  `:aria-label="playing ? t('audio.pause') : t('audio.play')"`

       (Note: This appears TWICE — once for full player at line 118, once for compact player at line 212. Replace BOTH.)

    **video/index.vue:**
    1. Add import after `import { useVideo } from './states';`: `import { useI18n } from '@lionad/vtu-core/i18n'`
    2. After `const videoState = reactive(useVideo(props, emit));`, add: `const { t } = useI18n()`
    3. Replace strings:

       Line 77 (browser unsupported message inside video element):
       BEFORE: `<p>Your browser does not support the video element.</p>`
       AFTER:  `<p>{{ t('video.unsupportedBrowser') }}</p>`

       Line 95 (sr-only video controls label):
       BEFORE: `<span v-else class="sr-only">Video controls</span>`
       AFTER:  `<span v-else class="sr-only">{{ t('video.videoControls') }}</span>`

       Line 120 (Open button text):
       BEFORE: `Open`
       AFTER:  `{{ t('video.open') }}`

       Line 144 (Play/Pause button ternary):
       BEFORE: `{{ videoState.playing ? "Pause" : "Watch" }}`
       AFTER:  `{{ videoState.playing ? t('video.pause') : t('video.watch') }}`

    **geo-map/index.vue:**
    1. Add import after `import { useGeoMap } from './states';`: `import { useI18n } from '@lionad/vtu-core/i18n'`
    2. After `const state = reactive(useGeoMap(props, emit));`, add: `const { t } = useI18n()`
    3. Replace strings:

       Line 84 (loading label):
       BEFORE: `<span data-slot="geo-map-loading-label">Loading map...</span>`
       AFTER:  `<span data-slot="geo-map-loading-label">{{ t('geoMap.loadingMap') }}</span>`
  </action>
  <verify>
    <automated>grep -c "useI18n" packages/components/src/audio/index.vue packages/components/src/video/index.vue packages/components/src/geo-map/index.vue | grep -v ":0$"</automated>
  </verify>
  <done>
    All 3 components import useI18n and use t() for all hardcoded text. audio has 2 instances of play/pause, video has 4 strings, geo-map has 1 string
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire useI18n() into item-carousel and preferences-panel</name>
  <files>
    packages/components/src/item-carousel/index.vue
    packages/components/src/preferences-panel/index.vue
  </files>
  <read_first>
    packages/components/src/item-carousel/index.vue
    packages/components/src/preferences-panel/index.vue
    packages/core/src/i18n/use-i18n.ts
  </read_first>
  <action>
    **item-carousel/index.vue:**
    1. Add import after `import { useItemCarousel } from './states';`: `import { useI18n } from '@lionad/vtu-core/i18n'`
    2. After `const carouselState = useItemCarousel(props, ...);`, add: `const { t } = useI18n()`
    3. Replace strings:

       Line 42 (empty state):
       BEFORE: `<p class="text-sm text-muted-foreground">No items to display</p>`
       AFTER:  `<p class="text-sm text-muted-foreground">{{ t('itemCarousel.noItems') }}</p>`

       Line 58 (carousel region ARIA label fallback):
       BEFORE: `:aria-label="title || 'Item carousel'"`
       AFTER:  `:aria-label="title || t('itemCarousel.itemCarouselLabel')"`

       Lines 85, 117 (scroll button aria-labels):
       BEFORE: `:aria-label="'Scroll left'"` → AFTER: `:aria-label="t('itemCarousel.scrollLeft')"`
       BEFORE: `:aria-label="'Scroll right'"` → AFTER: `:aria-label="t('itemCarousel.scrollRight')"`

       Line 159 (item ARIA label):
       BEFORE: `:aria-label="\`Item ${index + 1} of ${items.length}\`"`
       AFTER:  `:aria-label="t('itemCarousel.itemOf', { current: index + 1, total: items.length })"`

       Line 176 (pagination ARIA label):
       BEFORE: `aria-label="Carousel pagination"`
       AFTER:  `:aria-label="t('itemCarousel.paginationLabel')"`

       Line 183 (go to slide button):
       BEFORE: `:aria-label="\`Go to slide ${index + 1}\`"`
       AFTER:  `:aria-label="t('itemCarousel.goToSlide', { slide: index + 1 })"`

    **preferences-panel/index.vue:**
    1. Add import after `import { usePreferencesPanel } from './states';`: `import { useI18n } from '@lionad/vtu-core/i18n'`
    2. After `const state = reactive(usePreferencesPanel(props, emit));`, add: `const { t } = useI18n()`
    3. Replace strings:

       Line 40 (receipt ARIA label ternary):
       BEFORE: `:aria-label="state.hasErrors ? 'Preferences with errors' : 'Confirmed preferences'"`
       AFTER:  `:aria-label="state.hasErrors ? t('preferencesPanel.preferencesWithErrors') : t('preferencesPanel.confirmedPreferences')"`

       Line 53 (error badge text):
       BEFORE: `Error`
       AFTER:  `{{ t('preferencesPanel.error') }}`

       Line 59 (saved badge text):
       BEFORE: `Saved`
       AFTER:  `{{ t('preferencesPanel.saved') }}`

    NOTE: preferences-panel has many `lang="en"` attributes (lines 38, 164). These set the HTML lang attribute. Leave as-is — they are not display text. They could be i18n-ized later but are not critical for this phase.

    NOTE: preferences-panel item.label, option.label, section.heading, action.label are ALL data from props — NOT i18n-ized (per D-08).
  </action>
  <verify>
    <automated>grep -c "useI18n" packages/components/src/item-carousel/index.vue packages/components/src/preferences-panel/index.vue | grep -v ":0$" && grep -cE '"No items to display"|"Item carousel"|"Scroll left"|"Scroll right"|"Preferences with errors"|"Confirmed preferences"|"Error"|"Saved"' packages/components/src/item-carousel/index.vue packages/components/src/preferences-panel/index.vue | grep ":0$"</automated>
  </verify>
  <done>
    Both item-carousel and preferences-panel import useI18n. item-carousel: 7 strings replaced. preferences-panel: 3 strings replaced. Zero hardcoded English UI strings remain.
  </done>
</task>

</tasks>

<verification>
- 10 JSON files exist (audio=2, video=5, geoMap=1, itemCarousel=7, preferencesPanel=4 keys each)
- audio/index.vue: grep "useI18n" >= 1; grep "'Pause'" = 0; grep "'Play'" = 0
- video/index.vue: grep "useI18n" >= 1; grep "Your browser does not support" = 0; grep "Video controls" = 0; grep ">Open<" = 0
- geo-map/index.vue: grep "useI18n" >= 1; grep "Loading map..." = 0
- item-carousel/index.vue: grep "useI18n" >= 1; grep "No items to display" = 0; grep "Item carousel" = 0
- preferences-panel/index.vue: grep "useI18n" >= 1; grep "Preferences with errors" = 0; grep ">Error<" = 0; grep ">Saved<" = 0
- image-gallery/index.vue: unchanged (no hardcoded strings to replace)
</verification>

<success_criteria>
- 5 medium-priority components (audio, video, geo-map, item-carousel, preferences-panel) use t() for all hardcoded text
- image-gallery unchanged (zero hardcoded strings found)
- Each of the 5 components has zh-CN.json and en.json in its i18n/ directory
- Zero hardcoded English UI strings remain in templates
- All 5 components import and call useI18n()
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-i18n-high-medium/02-04-SUMMARY.md`
</output>
