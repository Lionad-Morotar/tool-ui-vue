---
phase: 07-story-i18n
plan: 05
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/stories/preferences-panel.story.vue
  - src/stories/preferences-panel/index.story.vue
  - src/stories/preferences-panel/i18n/zh.ts
  - src/stories/preferences-panel/i18n/en.ts
  - src/stories/preferences-panel/i18n/index.ts
autonomous: true
requirements:
  - STORY-STRUCT-01
  - STORY-STRUCT-02
  - STORY-STRUCT-03
must_haves:
  truths:
    - preferences-panel story lives in src/stories/preferences-panel/index.story.vue
    - preferences-panel has extracted i18n/zh.ts and i18n/en.ts
    - No inline useStoryLocale label objects remain in preferences-panel
  artifacts:
    - path: src/stories/preferences-panel/index.story.vue
      provides: Migrated preferences-panel story
    - path: src/stories/preferences-panel/i18n/zh.ts
      provides: Chinese translations
    - path: src/stories/preferences-panel/i18n/en.ts
      provides: English translations
  key_links:
    - from: preferences-panel/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
---

<objective>
Migrate the preferences-panel story to a directory-based structure and extract all ~68 inline useStoryLocale calls into namespaced i18n files.

Purpose: Remove dense inline locale labels and make the story maintainable.
Output: src/stories/preferences-panel/ directory with index.story.vue and i18n files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-story-i18n/07-CONTEXT.md
@src/stories/preferences-panel.story.vue
@.planning/phases/07-story-i18n/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract preferences-panel translations into i18n files</name>
  <files>src/stories/preferences-panel/i18n/zh.ts, src/stories/preferences-panel/i18n/en.ts, src/stories/preferences-panel/i18n/index.ts</files>
  <read_first>src/stories/preferences-panel.story.vue</read_first>
  <action>
    Extract all strings from `useStoryLocale({ zh, en })` in `src/stories/preferences-panel.story.vue` into `src/stories/preferences-panel/i18n/` with these namespaces:
    - `variant.*` for Variant titles
    - `section.*` for section or category labels
    - `option.*` for option names or values
    - `desc.*` for descriptions
    - `ui.*` for button or action labels
    - `content.*` for any remaining body text

    Create `zh.ts`, `en.ts`, and `index.ts` (exporting `{ zh, en }`).
  </action>
  <verify>
    <automated>test -f src/stories/preferences-panel/i18n/zh.ts && test -f src/stories/preferences-panel/i18n/en.ts && test -f src/stories/preferences-panel/i18n/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three i18n files exist under `src/stories/preferences-panel/i18n/`
    - zh.ts and en.ts have identical key structures
    - No `useStoryLocale` references inside i18n files
  </acceptance_criteria>
  <done>preferences-panel translation files are created with all strings extracted.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite preferences-panel story and move to new directory</name>
  <files>src/stories/preferences-panel/index.story.vue, src/stories/preferences-panel.story.vue</files>
  <read_first>src/stories/preferences-panel.story.vue</read_first>
  <action>
    Create `src/stories/preferences-panel/index.story.vue` by converting all inline `useStoryLocale({ zh, en })` calls to key-based lookups.

    1. Change `use-story-locale` import to `'../_shared/use-story-locale'`.
    2. Add `import messages from './i18n'`.
    3. Replace each inline call with `useStoryLocale('namespace.key', messages)`.
    4. Preserve all reactive state, control bindings, and event handlers exactly.
    5. Delete `src/stories/preferences-panel.story.vue`.
  </action>
  <verify>
    <automated>test -f src/stories/preferences-panel/index.story.vue && test ! -f src/stories/preferences-panel.story.vue && ! grep -q "useStoryLocale({" src/stories/preferences-panel/index.story.vue && grep -q "import messages from './i18n'" src/stories/preferences-panel/index.story.vue && grep -q "../_shared/use-story-locale" src/stories/preferences-panel/index.story.vue</automated>
  </verify>
  <acceptance_criteria>
    - `src/stories/preferences-panel/index.story.vue` exists
    - `src/stories/preferences-panel.story.vue` does NOT exist
    - No `useStoryLocale({` remains in the new file
    - File imports `messages` from `'./i18n'`
    - File imports `useStoryLocale` from `'../_shared/use-story-locale'`
  </acceptance_criteria>
  <done>preferences-panel story is fully migrated with key-based locale lookups.</done>
</task>

</tasks>

<verification>
- `pnpm story:dev` shows preferences-panel variants without errors
- Locale toggle updates all section labels and option names correctly
</verification>

<success_criteria>
- preferences-panel directory exists with index.story.vue and i18n files
- Old flat file is removed
- All inline locale labels converted to key-based lookups
</success_criteria>

<output>
After completion, create `.planning/phases/07-story-i18n/07-05-SUMMARY.md`
</output>
