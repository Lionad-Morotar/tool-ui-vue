---
phase: 07-story-i18n
plan: 11
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/stories/order-summary.story.vue
  - src/stories/order-summary/index.story.vue
  - src/stories/order-summary/i18n/zh.ts
  - src/stories/order-summary/i18n/en.ts
  - src/stories/order-summary/i18n/index.ts
  - src/stories/citation.story.vue
  - src/stories/citation/index.story.vue
  - src/stories/citation/i18n/zh.ts
  - src/stories/citation/i18n/en.ts
  - src/stories/citation/i18n/index.ts
  - src/stories/audio.story.vue
  - src/stories/audio/index.story.vue
  - src/stories/audio/i18n/zh.ts
  - src/stories/audio/i18n/en.ts
  - src/stories/audio/i18n/index.ts
  - src/stories/message-draft.story.vue
  - src/stories/message-draft/index.story.vue
  - src/stories/message-draft/i18n/zh.ts
  - src/stories/message-draft/i18n/en.ts
  - src/stories/message-draft/i18n/index.ts
  - src/stories/code-block.story.vue
  - src/stories/code-block/index.story.vue
  - src/stories/code-block/i18n/zh.ts
  - src/stories/code-block/i18n/en.ts
  - src/stories/code-block/i18n/index.ts
  - src/stories/image.story.vue
  - src/stories/image/index.story.vue
  - src/stories/image/i18n/zh.ts
  - src/stories/image/i18n/en.ts
  - src/stories/image/i18n/index.ts
autonomous: true
requirements:
  - STORY-STRUCT-01
  - STORY-STRUCT-02
  - STORY-STRUCT-03
must_haves:
  truths:
    - order-summary, citation, audio, message-draft, code-block, and image stories live in directory structures
    - Each has extracted i18n/zh.ts and i18n/en.ts files
    - No inline useStoryLocale label objects remain in any of the six stories
  artifacts:
    - path: src/stories/order-summary/index.story.vue
      provides: Migrated order-summary story
    - path: src/stories/citation/index.story.vue
      provides: Migrated citation story
    - path: src/stories/audio/index.story.vue
      provides: Migrated audio story
    - path: src/stories/message-draft/index.story.vue
      provides: Migrated message-draft story
    - path: src/stories/code-block/index.story.vue
      provides: Migrated code-block story
    - path: src/stories/image/index.story.vue
      provides: Migrated image story
  key_links:
    - from: order-summary/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
    - from: citation/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
    - from: audio/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
    - from: message-draft/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
    - from: code-block/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
    - from: image/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
---

<objective>
Migrate order-summary, citation, audio, message-draft, code-block, and image stories into directory-based structures with extracted i18n files.

Purpose: Remove ~139 combined inline useStoryLocale calls across the six stories.
Output: Six new directories under src/stories/ with index.story.vue and i18n files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-story-i18n/07-CONTEXT.md
@src/stories/order-summary.story.vue
@src/stories/citation.story.vue
@src/stories/audio.story.vue
@src/stories/message-draft.story.vue
@src/stories/code-block.story.vue
@src/stories/image.story.vue
@.planning/phases/07-story-i18n/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate order-summary and citation</name>
  <files>src/stories/order-summary.story.vue, src/stories/order-summary/index.story.vue, src/stories/order-summary/i18n/zh.ts, src/stories/order-summary/i18n/en.ts, src/stories/order-summary/i18n/index.ts, src/stories/citation.story.vue, src/stories/citation/index.story.vue, src/stories/citation/i18n/zh.ts, src/stories/citation/i18n/en.ts, src/stories/citation/i18n/index.ts</files>
  <read_first>src/stories/order-summary.story.vue, src/stories/citation.story.vue</read_first>
  <action>
    Migrate order-summary and citation to their respective directory structures.

    For each story:
    1. Extract all `useStoryLocale` strings into `i18n/zh.ts` and `i18n/en.ts` with appropriate namespaces (`variant.*`, `item.*`, `desc.*`, `ui.*`, `content.*`, `quote.*` for citation).
    2. Create `i18n/index.ts` exporting `{ zh, en }`.
    3. Rewrite the story file: update `use-story-locale` import to `'../_shared/use-story-locale'`, add `import messages from './i18n'`, replace inline calls with `useStoryLocale('namespace.key', messages)`.
    4. Preserve reactive data, controls, and event handlers.
    5. Delete the old flat `.story.vue` files.
  </action>
  <verify>
    <automated>test -f src/stories/order-summary/index.story.vue && test ! -f src/stories/order-summary.story.vue && ! grep -q "useStoryLocale({" src/stories/order-summary/index.story.vue && grep -q "import messages from './i18n'" src/stories/order-summary/index.story.vue && test -f src/stories/citation/index.story.vue && test ! -f src/stories/citation.story.vue && ! grep -q "useStoryLocale({" src/stories/citation/index.story.vue && grep -q "import messages from './i18n'" src/stories/citation/index.story.vue</automated>
  </verify>
  <acceptance_criteria>
    - Both `src/stories/order-summary/index.story.vue` and `src/stories/citation/index.story.vue` exist
    - Old flat files `order-summary.story.vue` and `citation.story.vue` do NOT exist
    - i18n files exist under both `src/stories/order-summary/i18n/` and `src/stories/citation/i18n/`
    - No `useStoryLocale({` remains in either new file
    - Both files import `messages` from `'./i18n'` and `useStoryLocale` from `'../_shared/use-story-locale'`
  </acceptance_criteria>
  <done>order-summary and citation stories are fully migrated.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate audio and message-draft</name>
  <files>src/stories/audio.story.vue, src/stories/audio/index.story.vue, src/stories/audio/i18n/zh.ts, src/stories/audio/i18n/en.ts, src/stories/audio/i18n/index.ts, src/stories/message-draft.story.vue, src/stories/message-draft/index.story.vue, src/stories/message-draft/i18n/zh.ts, src/stories/message-draft/i18n/en.ts, src/stories/message-draft/i18n/index.ts</files>
  <read_first>src/stories/audio.story.vue, src/stories/message-draft.story.vue</read_first>
  <action>
    Migrate audio and message-draft to their respective directory structures.

    For each story:
    1. Extract all `useStoryLocale` strings into `i18n/zh.ts` and `i18n/en.ts` with namespaces like `variant.*`, `track.*`/`draft.*`, `desc.*`, `ui.*`, `content.*`.
    2. Create `i18n/index.ts` exporting `{ zh, en }`.
    3. Rewrite: update `use-story-locale` import, add `import messages from './i18n'`, replace inline calls with key-based lookups.
    4. Preserve reactive data and controls.
    5. Delete old flat `.story.vue` files.
  </action>
  <verify>
    <automated>test -f src/stories/audio/index.story.vue && test ! -f src/stories/audio.story.vue && ! grep -q "useStoryLocale({" src/stories/audio/index.story.vue && grep -q "import messages from './i18n'" src/stories/audio/index.story.vue && test -f src/stories/message-draft/index.story.vue && test ! -f src/stories/message-draft.story.vue && ! grep -q "useStoryLocale({" src/stories/message-draft/index.story.vue && grep -q "import messages from './i18n'" src/stories/message-draft/index.story.vue</automated>
  </verify>
  <acceptance_criteria>
    - Both `src/stories/audio/index.story.vue` and `src/stories/message-draft/index.story.vue` exist
    - Old flat files do NOT exist
    - i18n files exist under both directories
    - No `useStoryLocale({` remains in either new file
    - Correct imports for `messages` and `useStoryLocale`
  </acceptance_criteria>
  <done>audio and message-draft stories are fully migrated.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate code-block and image</name>
  <files>src/stories/code-block.story.vue, src/stories/code-block/index.story.vue, src/stories/code-block/i18n/zh.ts, src/stories/code-block/i18n/en.ts, src/stories/code-block/i18n/index.ts, src/stories/image.story.vue, src/stories/image/index.story.vue, src/stories/image/i18n/zh.ts, src/stories/image/i18n/en.ts, src/stories/image/i18n/index.ts</files>
  <read_first>src/stories/code-block.story.vue, src/stories/image.story.vue</read_first>
  <action>
    Migrate code-block and image to their respective directory structures.

    For each story:
    1. Extract all `useStoryLocale` strings into `i18n/zh.ts` and `i18n/en.ts` with namespaces like `variant.*`, `code.*`/`image.*`, `desc.*`, `ui.*`, `content.*`.
    2. Create `i18n/index.ts` exporting `{ zh, en }`.
    3. Rewrite: update `use-story-locale` import, add `import messages from './i18n'`, replace inline calls with key-based lookups.
    4. Preserve reactive data and controls.
    5. Delete old flat `.story.vue` files.
  </action>
  <verify>
    <automated>test -f src/stories/code-block/index.story.vue && test ! -f src/stories/code-block.story.vue && ! grep -q "useStoryLocale({" src/stories/code-block/index.story.vue && grep -q "import messages from './i18n'" src/stories/code-block/index.story.vue && test -f src/stories/image/index.story.vue && test ! -f src/stories/image.story.vue && ! grep -q "useStoryLocale({" src/stories/image/index.story.vue && grep -q "import messages from './i18n'" src/stories/image/index.story.vue</automated>
  </verify>
  <acceptance_criteria>
    - Both `src/stories/code-block/index.story.vue` and `src/stories/image/index.story.vue` exist
    - Old flat files do NOT exist
    - i18n files exist under both directories
    - No `useStoryLocale({` remains in either new file
    - Correct imports for `messages` and `useStoryLocale`
  </acceptance_criteria>
  <done>code-block and image stories are fully migrated.</done>
</task>

</tasks>

<verification>
- `pnpm story:dev` shows all six stories without errors
- Locale toggle updates all labels correctly
</verification>

<success_criteria>
- order-summary, citation, audio, message-draft, code-block, and image directories exist with index.story.vue and i18n files
- Old flat files are removed
- All inline locale labels converted to key-based lookups
</success_criteria>

<output>
After completion, create `.planning/phases/07-story-i18n/07-11-SUMMARY.md`
</output>
