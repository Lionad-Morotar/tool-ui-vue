---
phase: 07-story-i18n
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/stories/item-carousel.story.vue
  - src/stories/item-carousel/index.story.vue
  - src/stories/item-carousel/i18n/zh.ts
  - src/stories/item-carousel/i18n/en.ts
  - src/stories/item-carousel/i18n/index.ts
autonomous: true
requirements:
  - STORY-STRUCT-01
  - STORY-STRUCT-02
  - STORY-STRUCT-03
must_haves:
  truths:
    - item-carousel story lives in src/stories/item-carousel/index.story.vue
    - item-carousel has dedicated i18n/zh.ts and i18n/en.ts files
    - No inline useStoryLocale label objects remain in item-carousel
  artifacts:
    - path: src/stories/item-carousel/index.story.vue
      provides: Migrated item-carousel story
    - path: src/stories/item-carousel/i18n/zh.ts
      provides: Chinese translations
    - path: src/stories/item-carousel/i18n/en.ts
      provides: English translations
  key_links:
    - from: item-carousel/index.story.vue
      to: i18n/index.ts
      via: import messages from './i18n'
    - from: item-carousel/index.story.vue
      to: ../_shared/use-story-locale
      via: updated import path
---

<objective>
Migrate the high-density item-carousel story from the flat src/stories/ structure into a self-contained directory with extracted i18n files.

Purpose: Remove ~77 inline useStoryLocale calls and establish maintainable key-based translations for the most complex story.
Output: src/stories/item-carousel/ directory with index.story.vue and i18n files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-story-i18n/07-CONTEXT.md
@src/stories/item-carousel.story.vue
@.planning/phases/07-story-i18n/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract item-carousel translations into i18n files</name>
  <files>src/stories/item-carousel/i18n/zh.ts, src/stories/item-carousel/i18n/en.ts, src/stories/item-carousel/i18n/index.ts</files>
  <read_first>src/stories/item-carousel.story.vue</read_first>
  <action>
    Read `src/stories/item-carousel.story.vue` and extract every string passed to `useStoryLocale({ zh, en })` into namespaced i18n files.

    1. Create `src/stories/item-carousel/i18n/zh.ts` with nested namespaces. Use `variant.*` for all Variant titles. Use `data.*` for product/category item arrays and any other reactive bilingual data objects. Use `content.*` for description text, subtitles, or body copy.

    2. Create the equivalent `src/stories/item-carousel/i18n/en.ts` with identical key structure.

    3. Create `src/stories/item-carousel/i18n/index.ts`:
    ```ts
    import zh from './zh'
    import en from './en'
    export default { zh, en }
    ```

    Keep the object shapes identical between zh and en. For array-based data (like `productItemsZh` / `productItemsEn`), you may either:
    - Leave the arrays inside the story and only extract the plain string constants, OR
    - Move the arrays into the i18n files under a `data` namespace and import them.
    Either approach is acceptable as long as `watch(currentLocale, ...)` logic is preserved and functional.
  </action>
  <verify>
    <automated>test -f src/stories/item-carousel/i18n/zh.ts && test -f src/stories/item-carousel/i18n/en.ts && test -f src/stories/item-carousel/i18n/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/stories/item-carousel/i18n/zh.ts` exists with `export default { ... }`
    - `src/stories/item-carousel/i18n/en.ts` exists with matching key structure
    - `src/stories/item-carousel/i18n/index.ts` exports `{ zh, en }` default
    - zh.ts and en.ts contain no `useStoryLocale` references
  </acceptance_criteria>
  <done>item-carousel i18n files are created with complete translation coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite item-carousel story and move to new directory</name>
  <files>src/stories/item-carousel/index.story.vue, src/stories/item-carousel.story.vue</files>
  <read_first>src/stories/item-carousel.story.vue</read_first>
  <action>
    Create `src/stories/item-carousel/index.story.vue` by rewriting the original story to use key-based `useStoryLocale` calls.

    1. Add `import messages from './i18n'` at the top of `<script setup>`.
    2. Update the import path for `use-story-locale` from `'./_shared/use-story-locale'` to `'../_shared/use-story-locale'`.
    3. Replace every `const foo = useStoryLocale({ zh: '...', en: '...' })` with `const foo = useStoryLocale('namespace.key', messages)`. Match the namespaces you defined in Task 1.
    4. If you kept reactive data arrays inside the story, ensure the `watch(currentLocale, ...)` block is preserved.
    5. Verify all component imports (e.g. `@lionad/vtu-components`) are unchanged.
    6. Delete `src/stories/item-carousel.story.vue`.
  </action>
  <verify>
    <automated>test -f src/stories/item-carousel/index.story.vue && test ! -f src/stories/item-carousel.story.vue && ! grep -q "useStoryLocale({" src/stories/item-carousel/index.story.vue && grep -q "import messages from './i18n'" src/stories/item-carousel/index.story.vue && grep -q "../_shared/use-story-locale" src/stories/item-carousel/index.story.vue</automated>
  </verify>
  <acceptance_criteria>
    - `src/stories/item-carousel/index.story.vue` exists
    - `src/stories/item-carousel.story.vue` does NOT exist
    - No `useStoryLocale({` pattern remains in the new file
    - File imports `messages` from `'./i18n'`
    - File imports `useStoryLocale` from `'../_shared/use-story-locale'`
    - All `<Variant>` elements still have `:title` bindings
  </acceptance_criteria>
  <done>item-carousel story is fully migrated to the directory structure with zero inline locale labels.</done>
</task>

</tasks>

<verification>
- `pnpm story:dev` shows item-carousel stories without errors
- Locale toggle updates all Variant titles in item-carousel
</verification>

<success_criteria>
- item-carousel directory exists with index.story.vue and i18n files
- Old flat file is removed
- Key-based lookups replace all inline useStoryLocale calls
</success_criteria>

<output>
After completion, create `.planning/phases/07-story-i18n/07-02-SUMMARY.md`
</output>
