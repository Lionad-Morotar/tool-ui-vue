---
phase: 01-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - packages/components/package.json
  - packages/components/tsconfig.json
  - packages/components/tsconfig.node.json
  - packages/components/vite.config.ts
  - packages/components/src/index.ts
  - package.json
autonomous: true
requirements:
  - INFRA-03
  - INFRA-05

must_haves:
  truths:
    - "packages/components depends on @lionad/core via workspace:* protocol"
    - "pnpm -r build successfully builds theme -> core -> components in order"
    - "pnpm -r typecheck passes for all three packages"
    - "Root pnpm scripts support batch operations across workspace"
  artifacts:
    - path: "packages/components/package.json"
      provides: "Components package manifest"
      contains: "@lionad/components"
    - path: "packages/components/package.json"
      provides: "Workspace dependency on core"
      contains: "@lionad/core": "workspace:*"
    - path: "packages/components/vite.config.ts"
      provides: "Vite library build config for components"
      contains: "vite-plugin-dts"
  key_links:
    - from: "packages/components/package.json"
      to: "packages/core/package.json"
      via: "workspace:* dependency"
      pattern: "@lionad/core.*workspace:\\*"
    - from: "packages/components/vite.config.ts"
      to: "packages/core/src/index.ts"
      via: "external dependencies (core exports become externals)"
      pattern: "external:.*@lionad/core"
---

<objective>
Scaffold packages/components with dependency on @lionad/core, and verify the complete build chain works end-to-end.

Purpose: Complete the monorepo foundation -- all three packages buildable with correct dependency order.
Output: packages/components with Vite lib config, workspace dependency on core, successful pnpm -r build verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@packages/core/package.json
@packages/core/vite.config.ts
@packages/theme/package.json
@package.json

<interfaces>
<!-- packages/core pattern to follow for packages/components -->
```json
// packages/core/package.json (dependency pattern)
{
  "name": "@lionad/core",
  "peerDependencies": { "vue": "^3.4.0" },
  "dependencies": { "clsx": "...", "tailwind-merge": "..." }
}
```

<!-- packages/theme pattern (independent CSS package) -->
```json
// packages/theme/package.json
{
  "name": "@lionad/theme",
  "exports": { "./tokens.css": "./dist/tokens.css" }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold packages/components with workspace dependency on core</name>
  <files>packages/components/package.json, packages/components/tsconfig.json, packages/components/tsconfig.node.json, packages/components/vite.config.ts, packages/components/src/index.ts</files>
  <read_first>
    - packages/core/package.json (dependency pattern to follow)
    - packages/core/vite.config.ts (build config pattern)
    - package.json (root -- to understand current dependencies that move here)
  </read_first>
  <action>
1. **Create `packages/components/package.json`**:
```json
{
  "name": "@lionad/components",
  "version": "0.1.0",
  "description": "Tool call UI components for tool-ui-vue",
  "license": "MIT",
  "type": "module",
  "files": ["dist"],
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    },
    "./components/*": "./dist/components/*"
  },
  "scripts": {
    "build": "vue-tsc --noEmit && vite build",
    "typecheck": "vue-tsc --noEmit"
  },
  "peerDependencies": {
    "vue": "^3.4.0"
  },
  "dependencies": {
    "@lionad/core": "workspace:*",
    "@lionad/theme": "workspace:*",
    "@vue-leaflet/vue-leaflet": "^0.10.1",
    "@vueuse/core": "^14.2.1",
    "@vueuse/components": "^14.2.1",
    "@vueuse/integrations": "^14.2.1",
    "@vueuse/motion": "^3.0.3",
    "@vueuse/nuxt": "^14.2.1",
    "@vueuse/router": "^14.2.1",
    "ansi-to-html": "^0.7.2",
    "chart.js": "^4.5.1",
    "clsx": "^2.1.1",
    "diff": "^8.0.4",
    "leaflet": "^1.9.4",
    "lucide-vue-next": "^0.468.0",
    "shiki": "^1.24.0",
    "supercluster": "^8.0.1",
    "tailwind-merge": "^2.6.1",
    "vue-chartjs": "^5.3.3",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "@types/diff": "^8.0.0",
    "@types/leaflet": "^1.9.21",
    "@types/supercluster": "^7.1.3",
    "typescript": "^5.9.0",
    "vite": "^8.0.3",
    "vite-plugin-dts": "^4.5.0",
    "vue": "^3.5.0",
    "vue-tsc": "^2.2.0"
  }
}
```

Key points:
- `"@lionad/core": "workspace:*"` -- workspace dependency (INFRA-03)
- `"@lionad/theme": "workspace:*"` -- workspace dependency for theme
- All runtime dependencies from root move here (these are component-specific)
- devDependencies are package-local (vite, vue-tsc, typescript, etc.)

2. **Create `packages/components/tsconfig.json`** -- Same pattern as packages/core:
```json
{
  "compilerOptions": {
    "target": "ES2021",
    "forceConsistentCasingInFileNames": true,
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2021", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "preserve",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "declaration": true,
    "declarationMap": true,
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src/**/*.ts", "src/**/*.vue"],
  "exclude": ["src/**/*.test.ts"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

3. **Create `packages/components/tsconfig.node.json`**:
```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true
  },
  "include": ["vite.config.ts"]
}
```

4. **Create `packages/components/vite.config.ts`** -- Similar to core but with additional externals:
```typescript
import vue from '@vitejs/plugin-vue';
import { resolve } from 'path';
import { defineConfig } from 'vite';
import dts from 'vite-plugin-dts';

export default defineConfig({
  plugins: [
    vue(),
    dts({
      insertTypesEntry: true,
      rollupTypes: false,
      include: ['src/**/*'],
      skipDiagnostics: true,
    }),
  ],
  build: {
    lib: {
      entry: resolve(__dirname, 'src/index.ts'),
      name: 'ToolUiVueComponents',
      formats: ['es', 'cjs'],
      fileName: (format) => `index.${format === 'es' ? 'js' : 'cjs'}`,
    },
    rollupOptions: {
      external: [
        'vue',
        '@lionad/core',
        '@lionad/theme',
        'zod',
        'clsx',
        'tailwind-merge',
      ],
      output: {
        globals: {
          vue: 'Vue',
          '@lionad/core': 'ToolUiVueCore',
          '@lionad/theme': 'ToolUiVueTheme',
          zod: 'zod',
          clsx: 'clsx',
          'tailwind-merge': 'tailwindMerge',
        },
      },
    },
  },
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src'),
    },
  },
});
```

5. **Create `packages/components/src/index.ts`** -- Placeholder barrel export:
```typescript
// Components will be migrated from src/components/ in Phase 4
// For now, export a placeholder to make the package buildable
export const VERSION = '0.1.0';
```

6. Run `pnpm install` from root to link workspace packages.
  </action>
  <verify>
    <automated>cd /Users/lionad/Github/Lionad-Morotar/tool-ui-vue && cat packages/components/package.json | jq -r '.dependencies["@lionad/core"]' 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - packages/components/package.json has `"name": "@lionad/components"`
    - packages/components/package.json has `"@lionad/core": "workspace:*"` in dependencies
    - packages/components/package.json has `"@lionad/theme": "workspace:*"` in dependencies
    - packages/components/vite.config.ts externalizes `@lionad/core` and `@lionad/theme`
    - packages/components/vite.config.ts uses `vite-plugin-dts`
    - `pnpm install` succeeds without workspace resolution errors
  </acceptance_criteria>
  <done>packages/components package created with workspace:* dependency on @lionad/core. Package is buildable (empty placeholder for now).</done>
</task>

<task type="auto">
  <name>Task 2: Verify end-to-end build chain</name>
  <files>package.json</files>
  <read_first>
    - package.json (root -- verify scripts are correct)
    - pnpm-workspace.yaml (verify workspace resolution)
  </read_first>
  <action>
1. **Verify workspace resolution** -- Run:
```bash
pnpm install
```
This should resolve all workspace packages without errors.

2. **Verify batch build** -- Run:
```bash
pnpm -r build
```
Expected output (in order):
- @lionad/theme builds first (no dependencies)
- @lionad/core builds second (depends on vue peer dep only)
- @lionad/components builds third (depends on core and theme)

3. **Verify batch typecheck** -- Run:
```bash
pnpm -r typecheck
```
All three packages should pass type checking.

4. **Verify root scripts work** -- Update root `package.json` if needed to ensure:
```json
"scripts": {
  "build": "pnpm -r --filter='./packages/*' run build",
  "typecheck": "pnpm -r --filter='./packages/*' run typecheck",
  "build:all": "pnpm -r build",
  "typecheck:all": "pnpm -r typecheck"
}
```

5. **Run final verification**:
```bash
pnpm build && pnpm typecheck
```
Both should succeed.
  </action>
  <verify>
    <automated>cd /Users/lionad/Github/Lionad-Morotar/tool-ui-vue && pnpm -r build 2>&1 | grep -E "(✓|success|Built)" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` exits with code 0
    - `pnpm -r build` exits with code 0
    - Build output shows 3 packages built (theme, core, components)
    - `pnpm -r typecheck` exits with code 0
    - packages/theme/dist/tokens.css exists
    - packages/theme/dist/index.js exists
    - packages/core/dist/index.js exists
    - packages/core/dist/index.d.ts exists
    - packages/components/dist/index.js exists (placeholder)
  </acceptance_criteria>
  <done>All three packages (theme, core, components) build successfully with pnpm -r build. Type checking passes. Workspace dependencies resolve correctly.</done>
</task>

</tasks>

<verification>
1. `pnpm install` from root completes without workspace resolution errors
2. `pnpm -r build` builds all three packages in correct dependency order
3. `pnpm -r typecheck` passes for all packages
4. Root `pnpm build` and `pnpm typecheck` scripts work correctly
</verification>

<success_criteria>
- packages/components has workspace:* dependency on @lionad/core
- pnpm -r build succeeds for all 3 packages (theme, core, components)
- pnpm -r typecheck passes for all 3 packages
- Root package.json scripts support batch operations
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-02-SUMMARY.md`
</output>
