---
phase: 03-p1-components-theme-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/components/badge/variants.ts
  - packages/core/src/components/badge/index.vue
  - packages/core/src/components/badge/index.ts
  - packages/core/src/components/badge/__tests__/index.test.ts
  - packages/core/src/components/copy-button/index.vue
  - packages/core/src/components/copy-button/index.ts
  - packages/core/src/components/copy-button/__tests__/index.test.ts
  - packages/core/src/index.ts
autonomous: true
requirements:
  - BASE-05
  - BASE-06

must_haves:
  truths:
    - "Badge renders with default/secondary/destructive/outline variant classes"
    - "Badge exports from @lionad/core alongside Button and Card"
    - "CopyButton calls navigator.clipboard.writeText and shows icon state transition (copy -> check)"
    - "CopyButton exports from @lionad/core"
  artifacts:
    - path: "packages/core/src/components/badge/variants.ts"
      provides: "Badge cva variant definitions + BadgeProps type"
      exports: ["badgeVariants", "BadgeProps"]
    - path: "packages/core/src/components/badge/index.vue"
      provides: "Badge component with data-slot and variant rendering"
      contains: "data-slot"
    - path: "packages/core/src/components/badge/index.ts"
      provides: "Badge barrel exports"
      exports: ["Badge", "badgeVariants"]
    - path: "packages/core/src/components/copy-button/index.vue"
      provides: "CopyButton component with clipboard logic and icon state"
      contains: "navigator.clipboard"
    - path: "packages/core/src/components/copy-button/index.ts"
      provides: "CopyButton barrel exports"
      exports: ["CopyButton"]
    - path: "packages/core/src/index.ts"
      provides: "Core barrel with Badge + CopyButton exports added"
      contains: "Badge"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/components/badge/index.ts"
      via: "re-export"
      pattern: "Badge.*badge"
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/components/copy-button/index.ts"
      via: "re-export"
      pattern: "CopyButton.*copy-button"
    - from: "packages/core/src/components/badge/index.vue"
      to: "packages/core/src/components/badge/variants.ts"
      via: "import badgeVariants"
      pattern: "badgeVariants"
---

<objective>
Implement Badge and CopyButton base components in @lionad/core, following the established Button/Card patterns.

Purpose: Badge and CopyButton complete the core base component layer, enabling Phase 4 migration to replace 7 inline badge patterns and 3 inline copy-to-clipboard patterns across tool components.
Output: Badge component with 4 cva variants, CopyButton component with clipboard logic + icon state, both exported from @lionad/core.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Reference: Phase 2 Button pattern to follow -->
@packages/core/src/components/button/variants.ts
@packages/core/src/components/button/index.vue
@packages/core/src/components/button/__tests__/index.test.ts
@packages/core/src/index.ts

<interfaces>
<!-- Established patterns from Phase 2 - executor MUST follow these exactly -->

From packages/core/src/components/button/variants.ts:
```typescript
// Pattern: cva() call with variants + defaultVariants
// Pattern: Export VariantProps type as XxxVariants
// Pattern: Export XxxProps interface with variant/size/class fields
import { cva, type VariantProps } from 'class-variance-authority'
export const buttonVariants = cva("base-classes", { variants: {...}, defaultVariants: {...} })
export type ButtonVariants = VariantProps<typeof buttonVariants>
export type ButtonProps = { variant?: ...; size?: ...; class?: string }
```

From packages/core/src/components/button/index.vue:
```vue
<script setup lang="ts">
// Pattern: import cn from '../../utils', import variants + types from './variants'
// Pattern: withDefaults(defineProps<XxxProps>(), { variant: 'default', ... })
// Pattern: defineOptions({ name: 'Xxx', inheritAttrs: false })
// Pattern: data-slot attribute for identification
// Pattern: computed classes via cn(variants({...}))
// Pattern: v-bind="{ ...attrs, ...$attrs }" for attribute forwarding
</script>
```

From packages/core/src/components/button/index.ts:
```typescript
// Pattern: export component (from .vue) + variants + types
export { default as Button } from './index.vue'
export { buttonVariants } from './variants'
export type { ButtonProps, ButtonVariants } from './variants'
```

From packages/core/src/index.ts:
```typescript
// Pattern: named exports + type exports on separate lines
export { Badge, badgeVariants } from './components/badge'
export type { BadgeProps, BadgeVariants } from './components/badge'
export { CopyButton } from './components/copy-button'
```

Reference Badge from /Users/lionad/Github/Run/tool-ui/apps/www/components/ui/badge.tsx:
- Base: "inline-flex items-center justify-center rounded-full border px-2 py-0.5 text-xs font-medium w-fit whitespace-nowrap shrink-0 [&>svg]:size-3 gap-1 [&>svg]:pointer-events-none"
- Variants: default (bg-primary text-primary-foreground), secondary (bg-secondary text-secondary-foreground), destructive (bg-destructive text-white), outline (text-foreground)
- Renders as <span> with data-slot="badge"

Copy pattern from src/components/code-block/states/useCodeBlock.ts:
- navigator.clipboard.writeText(text)
- isCopied ref with setTimeout(() => isCopied = false, 2000)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Badge component with cva variants</name>
  <files>
    packages/core/src/components/badge/variants.ts
    packages/core/src/components/badge/index.vue
    packages/core/src/components/badge/index.ts
    packages/core/src/components/badge/__tests__/index.test.ts
  </files>
  <read_first>
    packages/core/src/components/button/variants.ts
    packages/core/src/components/button/index.vue
    packages/core/src/components/button/index.ts
    packages/core/src/components/button/__tests__/index.test.ts
  </read_first>
  <acceptance_criteria>
    - grep -q "badgeVariants" packages/core/src/components/badge/variants.ts
    - grep -q "BadgeProps" packages/core/src/components/badge/variants.ts
    - grep -q "data-slot.*badge" packages/core/src/components/badge/index.vue
    - grep -q "export.*Badge" packages/core/src/components/badge/index.ts
    - pnpm --filter @lionad/core typecheck passes
    - pnpm vitest run packages/core/src/components/badge passes
  </acceptance_criteria>
  <behavior>
    - Test: renders with default variant classes (bg-primary, text-primary-foreground)
    - Test: renders with secondary variant classes (bg-secondary, text-secondary-foreground)
    - Test: renders with destructive variant classes (bg-destructive, text-white)
    - Test: renders with outline variant classes (text-foreground)
    - Test: renders base classes (inline-flex, rounded-full, text-xs)
    - Test: has data-slot="badge" attribute
    - Test: applies custom className when provided
    - Test: renders as <span> element
    - Test: supports asChild mode (renders child instead of span)
  </behavior>
  <action>
    Create Badge component following Button pattern exactly.

    1. Create `packages/core/src/components/badge/variants.ts`:
       - Import cva and VariantProps from class-variance-authority
       - Define badgeVariants with cva(), base classes from reference:
         "inline-flex items-center justify-center rounded-full border px-2 py-0.5 text-xs font-medium w-fit whitespace-nowrap shrink-0 [&>svg]:size-3 gap-1 [&>svg]:pointer-events-none focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px] aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive transition-[color,box-shadow] overflow-hidden"
       - Variants object with single key "variant" containing 4 options:
         - default: "border-transparent bg-primary text-primary-foreground [a&]:hover:bg-primary/90"
         - secondary: "border-transparent bg-secondary text-secondary-foreground [a&]:hover:bg-secondary/90"
         - destructive: "border-transparent bg-destructive text-white [a&]:hover:bg-destructive/90 focus-visible:ring-destructive/20 dark:focus-visible:ring-destructive/40 dark:bg-destructive/60"
         - outline: "text-foreground [a&]:hover:bg-accent [a&]:hover:text-accent-foreground"
       - defaultVariants: { variant: "default" }
       - Export BadgeVariants type from VariantProps
       - Export BadgeProps interface: { variant?: NonNullable<BadgeVariants['variant']>; asChild?: boolean; class?: string }

    2. Create `packages/core/src/components/badge/index.vue`:
       - Follow Button index.vue pattern exactly
       - Use `<span>` as default element (not <button>)
       - import cn from '../../utils', badgeVariants and BadgeProps from './variants'
       - Props: variant defaults to 'default', asChild defaults to false
       - defineOptions({ name: 'Badge', inheritAttrs: false })
       - data-slot="badge", data-variant attribute
       - Use same asChild pattern as Button (h() + mergeProps)
       - No size prop for Badge (Badge has no size variants)

    3. Create `packages/core/src/components/badge/index.ts`:
       - export { default as Badge } from './index.vue'
       - export { badgeVariants } from './variants'
       - export type { BadgeProps, BadgeVariants } from './variants'

    4. Create `packages/core/src/components/badge/__tests__/index.test.ts`:
       - Follow Button test pattern
       - Mock navigator.clipboard is NOT needed (Badge has no clipboard logic)
       - Test all 4 variants render correct classes
       - Test base classes present (inline-flex, rounded-full, text-xs)
       - Test data-slot="badge"
       - Test custom className
       - Test renders as <span>
       - Test asChild mode works
  </action>
  <verify>
    <automated>cd /Users/lionad/Github/Lionad-Morotar/tool-ui-vue && pnpm vitest run packages/core/src/components/badge/__tests__/index.test.ts && pnpm --filter @lionad/core typecheck</automated>
  </verify>
  <done>Badge component with 4 cva variants (default/secondary/destructive/outline) passing all tests, exporting from packages/core/src/components/badge/, typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CopyButton component with clipboard logic and icon state</name>
  <files>
    packages/core/src/components/copy-button/index.vue
    packages/core/src/components/copy-button/index.ts
    packages/core/src/components/copy-button/__tests__/index.test.ts
  </files>
  <read_first>
    packages/core/src/components/button/index.vue
    packages/core/src/components/button/__tests__/index.test.ts
    src/components/code-block/states/useCodeBlock.ts (lines 220-231 for copy pattern)
  </read_first>
  <acceptance_criteria>
    - grep -q "navigator.clipboard" packages/core/src/components/copy-button/index.vue
    - grep -q "data-slot.*copy-button" packages/core/src/components/copy-button/index.vue
    - grep -q "export.*CopyButton" packages/core/src/components/copy-button/index.ts
    - pnpm --filter @lionad/core typecheck passes
    - pnpm vitest run packages/core/src/components/copy-button passes
  </acceptance_criteria>
  <behavior>
    - Test: renders as a button element with data-slot="copy-button"
    - Test: calls navigator.clipboard.writeText with the value prop when clicked
    - Test: shows "copy" state by default (isCopied = false)
    - Test: transitions to "check" state after successful copy (isCopied = true)
    - Test: reverts to "copy" state after 2000ms timeout
    - Test: emits "copied" event after successful copy
    - Test: applies correct variant/size classes (uses buttonVariants)
    - Test: does not throw when clipboard API fails
  </behavior>
  <action>
    Create CopyButton component as a specialized Button with clipboard logic.

    1. Create `packages/core/src/components/copy-button/index.vue`:
       - Import cn from '../../utils'
       - Import Button and buttonVariants from '../button' (reuse Button's variant system)
       - CopyButton does NOT have its own cva -- it wraps Button's variant system

       Props interface (define inline or in index.ts):
       ```typescript
       interface CopyButtonProps {
         value: string           // Text to copy
         variant?: 'ghost' | 'outline' | 'secondary' | 'default'  // defaults to 'ghost'
         size?: 'default' | 'sm' | 'lg' | 'icon' | 'icon-sm' | 'icon-lg'  // defaults to 'icon-sm'
         class?: string
       }
       ```

       Script setup:
       - props with defaults: variant='ghost', size='icon-sm'
       - defineOptions({ name: 'CopyButton', inheritAttrs: false })
       - const isCopied = ref(false)
       - async function handleCopy():
         ```typescript
         try {
           await navigator.clipboard.writeText(props.value)
           isCopied.value = true
           emit('copied')
           setTimeout(() => { isCopied.value = false }, 2000)
         } catch { /* ignore clipboard errors */ }
         ```
       - emit: { copied: [] }

       Template:
       - Render a <button> with data-slot="copy-button"
       - Use buttonVariants classes via cn(): cn(buttonVariants({ variant, size }), props.class)
       - Type="button" (accessibility)
       - @click="handleCopy"
       - :aria-label="isCopied ? 'Copied' : 'Copy to clipboard'"
       - Slot for icon: default slot receives { isCopied } via scoped slot
         ```html
         <slot :is-copied="isCopied">
           <!-- Default fallback: show text "Copy"/"Copied" if no slot content -->
           <span v-if="!isCopied">Copy</span>
           <span v-else>Copied!</span>
         </slot>
         ```

    2. Create `packages/core/src/components/copy-button/index.ts`:
       - export { default as CopyButton } from './index.vue'
       - export type { CopyButtonProps } from './index.vue' (use defineExpose or separate type file if needed)
       - NOTE: Since CopyButtonProps is defined in the .vue file, create a types approach that works:
         Option A: Define CopyButtonProps in a separate types.ts file
         Option B: Use Vue's InstanceType approach
         PREFERRED: Define interface in index.ts and import in .vue file

         Actually, following project convention (types in .ts files, not .vue):
         Create the interface in index.ts and have .vue import it.

         index.ts:
         ```typescript
         export { default as CopyButton } from './index.vue'
         export interface CopyButtonProps {
           value: string
           variant?: 'ghost' | 'outline' | 'secondary' | 'default'
           size?: 'default' | 'sm' | 'lg' | 'icon' | 'icon-sm' | 'icon-lg'
           class?: string
         }
         ```

    3. Create `packages/core/src/components/copy-button/__tests__/index.test.ts`:
       - Mock navigator.clipboard: { writeText: vi.fn().mockResolvedValue(undefined) }
       - Test: renders button with data-slot="copy-button"
       - Test: clicking calls navigator.clipboard.writeText with props.value
       - Test: isCopied transitions to true after click
       - Test: isCopied reverts to false after 2000ms (use vi.useFakeTimers)
       - Test: variant and size classes applied correctly
       - Test: emits 'copied' event after successful copy
       - Test: does not throw when clipboard.writeText rejects
       - Use vi.useFakeTimers() for timeout testing, vi.useRealTimers() in afterEach
  </action>
  <verify>
    <automated>cd /Users/lionad/Github/Lionad-Morotar/tool-ui-vue && pnpm vitest run packages/core/src/components/copy-button/__tests__/index.test.ts && pnpm --filter @lionad/core typecheck</automated>
  </verify>
  <done>CopyButton component that copies text to clipboard, shows icon state transition (copy -> check via scoped slot), emits 'copied' event, all tests passing, typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Export Badge and CopyButton from @lionad/core barrel</name>
  <files>
    packages/core/src/index.ts
  </files>
  <read_first>
    packages/core/src/index.ts
    packages/core/src/components/badge/index.ts
    packages/core/src/components/copy-button/index.ts
  </read_first>
  <acceptance_criteria>
    - grep -q "Badge.*badge" packages/core/src/index.ts
    - grep -q "CopyButton.*copy-button" packages/core/src/index.ts
    - pnpm --filter @lionad/core build succeeds
  </acceptance_criteria>
  <action>
    Update packages/core/src/index.ts to add Badge and CopyButton exports.

    Add after the existing Card exports:
    ```typescript
    // Export Badge component and variants
    export { Badge, badgeVariants } from './components/badge'
    export type { BadgeProps, BadgeVariants } from './components/badge'

    // Export CopyButton component
    export { CopyButton } from './components/copy-button'
    export type { CopyButtonProps } from './components/copy-button'
    ```

    Then run `pnpm --filter @lionad/core build` to verify the barrel file compiles correctly.
  </action>
  <verify>
    <automated>cd /Users/lionad/Github/Lionad-Morotar/tool-ui-vue && pnpm --filter @lionad/core build && pnpm --filter @lionad/core typecheck</automated>
  </verify>
  <done>Badge, badgeVariants, BadgeProps, BadgeVariants, CopyButton, CopyButtonProps all exported from @lionad/core. Build passes.</done>
</task>

</tasks>

<verification>
1. All Badge tests pass: `pnpm vitest run packages/core/src/components/badge/__tests__/`
2. All CopyButton tests pass: `pnpm vitest run packages/core/src/components/copy-button/__tests__/`
3. Core package builds: `pnpm --filter @lionad/core build`
4. Typecheck passes: `pnpm --filter @lionad/core typecheck`
5. Imports resolve: `node -e "const m = require('@lionad/core'); console.log(Object.keys(m))"` should list Badge, badgeVariants, CopyButton
</verification>

<success_criteria>
- Badge component renders with 4 cva variants (default/secondary/destructive/outline)
- CopyButton copies text to clipboard and provides icon state transition via scoped slot
- Both exported from @lionad/core alongside Button and Card
- All tests pass, build succeeds, typecheck clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-p1-components-theme-system/03-01-SUMMARY.md`
</output>
