---
phase: 04-component-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - packages/components/src/approval-card/*
  - packages/components/src/audio/*
  - packages/components/src/citation/*
  - packages/components/src/code-diff/*
  - packages/components/src/image/*
  - packages/components/src/instagram-post/*
  - packages/components/src/link-preview/*
  - packages/components/src/linkedin-post/*
  - packages/components/src/message-draft/*
  - packages/components/src/order-summary/*
  - packages/components/src/plan/*
  - packages/components/src/preferences-panel/*
  - packages/components/src/progress-tracker/*
  - packages/components/src/terminal/*
  - packages/components/src/x-post/*
  - packages/components/src/index.ts
autonomous: true
requirements:
  - MIGR-01
  - MIGR-02
  - MIGR-04
user_setup: []

must_haves:
  truths:
    - "Each migrated component is importable from @lionad/components"
    - "Component schemas reference @lionad/core for shared utilities"
    - "Inline button patterns replaced with <Button> from @lionad/core"
    - "Inline card patterns replaced with <Card> components from @lionad/core"
  artifacts:
    - path: "packages/components/src/index.ts"
      provides: "Barrel exports for all 15 migrated components"
      min_lines: 50
    - path: "packages/components/src/approval-card/index.vue"
      provides: "Migrated ApprovalCard component using core Button/Card"
      imports: ["@lionad/core"]
    - path: "packages/components/src/terminal/index.vue"
      provides: "Migrated Terminal component"
      imports: ["@lionad/core"]
  key_links:
    - from: "packages/components/src/*/index.vue"
      to: "@lionad/core"
      via: "import"
      pattern: "import.*Button.*from.*['\"]@lionad/core['\"]"
    - from: "packages/components/src/*/index.ts"
      to: "packages/components/src/index.ts"
      via: "barrel export"
      pattern: "export.*from.*['\"]\\.\\/[a-z-]+['\"]"
---

<objective>
Migrate 15 simple tool components (5-6 files each, no complex composables/effects) from src/components/ to packages/components/, replacing inline patterns with core Button/Card/Badge.

Purpose: Move copy-paste components into the installable @lionad/components package, consuming base components from core.
Output: 15 components available from @lionad/components with Zod schemas unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@src/components/approval-card/index.vue
@src/components/approval-card/index.ts
@src/components/approval-card/schema.ts
@src/components/terminal/index.vue
@src/components/terminal/index.ts
@src/components/terminal/schema.ts
</context>

<interfaces>
<!-- Core components available for migration -->

From packages/core/src/index.ts:
```typescript
// Button
export { Button, buttonVariants }
export type { ButtonProps, ButtonVariants }

// Card
export { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter }

// Badge
export { Badge, badgeVariants }
export type { BadgeProps, BadgeVariants }

// CopyButton
export { CopyButton }
export type { CopyButtonProps }

// Shared utilities
export { cn, defineToolUiContract }
export { ToolUIIdSchema, ActionSchema, ... }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create migration script for simple components</name>
  <files>packages/components/src/index.ts</files>
  <action>
    For each of the 15 simple components, perform the following migration steps:

    **Components to migrate (simple tier: 5-6 files, no complex deps):**
    1. approval-card (6 files: index.vue, index.ts, schema.ts, states/, __tests__/, cmpts/ if exists)
    2. audio (5 files)
    3. citation (5 files)
    4. code-diff (5 files)
    5. image (5 files)
    6. instagram-post (5 files)
    7. link-preview (5 files)
    8. linkedin-post (5 files)
    9. message-draft (5 files)
    10. order-summary (5 files)
    11. plan (5 files)
    12. preferences-panel (6 files)
    13. progress-tracker (6 files)
    14. terminal (5 files)
    15. x-post (5 files)

    **For each component directory:**
    1. Copy entire directory: src/components/{name}/ → packages/components/src/{name}/
    2. Update cn import: `from '../../utils'` → `from '@lionad/core'`
    3. Update shared imports: `from '../../shared/contract'` → `from '@lionad/core'`
    4. Update shared imports: `from '../../shared/schema'` → `from '@lionad/core'`

    Keep the directory structure intact (states/, cmpts/, __tests__/ subdirectories).
  </action>
  <verify>
    <automated>ls packages/components/src/ | wc -l</automated>
  </verify>
  <done>15 component directories exist in packages/components/src/</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline patterns with core components</name>
  <files>
    packages/components/src/approval-card/index.vue,
    packages/components/src/audio/index.vue,
    packages/components/src/citation/index.vue,
    packages/components/src/code-diff/index.vue,
    packages/components/src/image/index.vue,
    packages/components/src/instagram-post/index.vue,
    packages/components/src/link-preview/index.vue,
    packages/components/src/linkedin-post/index.vue,
    packages/components/src/message-draft/index.vue,
    packages/components/src/order-summary/index.vue,
    packages/components/src/plan/index.vue,
    packages/components/src/preferences-panel/index.vue,
    packages/components/src/progress-tracker/index.vue,
    packages/components/src/terminal/index.vue,
    packages/components/src/x-post/index.vue
  </files>
  <action>
    For each migrated component's index.vue, replace inline Tailwind patterns with core components:

    **Button pattern replacement:**
    Find inline button styles like:
    ```vue
    <button
      :class="cn(
        'inline-flex items-center justify-center rounded-full px-4 text-base font-medium',
        'bg-primary text-primary-foreground hover:bg-primary/90',
        'focus-visible:ring-2 focus-visible:ring-ring',
      )"
    >
    ```
    Replace with:
    ```vue
    <Button variant="default" size="default">
    ```

    **Card container pattern replacement:**
    Find inline card containers like:
    ```vue
    <div :class="cn('rounded-2xl border border-border bg-card p-5 shadow-xs')">
    ```
    Replace with:
    ```vue
    <Card>
    ```

    **Specific component patterns:**

    approval-card: Lines 94-96 (Card), Lines 153-168 and 169-186 (Button x2)
    preferences-panel: Card container + Button patterns
    progress-tracker: Card container + Button patterns
    plan: Card container

    Use ripgrep to find patterns:
    - `rg 'rounded-2xl.*border.*bg-card' packages/components/src/*/index.vue`
    - `rg 'rounded-full.*px-4.*font-medium' packages/components/src/*/index.vue`
  </action>
  <verify>
    <automated>grep -r "rounded-2xl border border-border bg-card" packages/components/src/ | grep -v "states/" | wc -l</automated>
  </verify>
  <done>Inline card patterns replaced with <Card> components (0 or only in states/ files)</done>
</task>

<task type="auto">
  <name>Task 3: Update barrel exports for migrated components</name>
  <files>packages/components/src/index.ts</files>
  <action>
    Update packages/components/src/index.ts to export all 15 migrated components:

    For each component (approval-card, audio, citation, code-diff, image, instagram-post, link-preview, linkedin-post, message-draft, order-summary, plan, preferences-panel, progress-tracker, terminal, x-post):

    ```typescript
    // approval-card
    export { ApprovalCard } from './approval-card'
    export type {
      ApprovalCardProps,
      ApprovalCardBaseProps,
      SerializableApprovalCard,
      ApprovalDecision,
      MetadataItem,
    } from './approval-card'
    export {
      SerializableApprovalCardSchema,
      ApprovalDecisionSchema,
      MetadataItemSchema,
      parseSerializableApprovalCard,
      safeParseSerializableApprovalCard,
    } from './approval-card'

    // audio
    export { Audio } from './audio'
    export type { AudioProps, SerializableAudio, AudioVariant } from './audio'
    export { SerializableAudioSchema, AudioVariantSchema, parseSerializableAudio, safeParseSerializableAudio } from './audio'
    ```

    Continue this pattern for all 15 components. Each component's index.ts already exports the component, types, schemas, and parsers - just re-export from the barrel.

    Also export any sub-components (cmpts/) if they exist:
    - citation: CitationList
    - linkedin-post: LinkedInPost sub-components
    - message-draft: MessageDraft sub-components
  </action>
  <verify>
    <automated>grep -c "export.*from './" packages/components/src/index.ts</automated>
  </verify>
  <done>Barrel file exports all 15 components with types and schemas</done>
</task>

<task type="auto">
  <name>Task 4: Run tests for migrated components</name>
  <files>packages/components/src/**/__tests__/*.test.ts</files>
  <action>
    Run the existing Vitest tests for the 15 migrated components:

    ```bash
    pnpm test -- --run src/components/approval-card
    pnpm test -- --run src/components/audio
    pnpm test -- --run src/components/citation
    pnpm test -- --run src/components/code-diff
    pnpm test -- --run src/components/image
    pnpm test -- --run src/components/instagram-post
    pnpm test -- --run src/components/link-preview
    pnpm test -- --run src/components/linkedin-post
    pnpm test -- --run src/components/message-draft
    pnpm test -- --run src/components/order-summary
    pnpm test -- --run src/components/plan
    pnpm test -- --run src/components/preferences-panel
    pnpm test -- --run src/components/progress-tracker
    pnpm test -- --run src/components/terminal
    pnpm test -- --run src/components/x-post
    ```

    Tests should still run from src/components/ since they haven't been migrated yet. The migrated packages/components/ versions are separate.

    Note: The tests verify the original src/components/ code. Once we confirm the migration is correct, we can remove the old src/components/ directories in a later phase.
  </action>
  <verify>
    <automated>pnpm test -- --run src/components/approval-card 2>&1 | tail -10</automated>
  </verify>
  <done>Tests pass for original components (verification that migration didn't break source)</done>
</task>

</tasks>

<verification>
- [ ] 15 component directories exist in packages/components/src/
- [ ] Each component's index.vue imports cn from @lionad/core
- [ ] Each component's schema.ts imports from @lionad/core (not ../../shared)
- [ ] Inline Card patterns replaced with <Card> component
- [ ] Inline Button patterns replaced with <Button> component
- [ ] packages/components/src/index.ts exports all 15 components
- [ ] pnpm --filter=@lionad/components typecheck passes
- [ ] Original src/components/ tests still pass
</verification>

<success_criteria>
15 simple components migrated to packages/components/:
- approval-card, audio, citation, code-diff, image, instagram-post, link-preview, linkedin-post, message-draft, order-summary, plan, preferences-panel, progress-tracker, terminal, x-post
- All consume @lionad/core for cn, shared schemas, Button, Card
- Zod schemas unchanged
- Barrel exports configured for tree-shaking
</success_criteria>

<output>
After completion, create .planning/phases/04-component-migration/04-02-SUMMARY.md documenting:
- 15 components migrated (list)
- Pattern replacements made (Card, Button)
- Import updates performed
- Test verification results
- Any issues encountered
</output>
