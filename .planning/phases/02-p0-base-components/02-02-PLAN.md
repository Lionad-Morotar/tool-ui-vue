---
phase: 02-p0-base-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/components/card/index.vue
  - packages/core/src/components/card/index.ts
autonomous: true
requirements:
  - BASE-03

must_haves:
  truths:
    - "Card renders as a container with rounded corners, border, and shadow"
    - "CardHeader positions header content with proper padding"
    - "CardContent renders body content with proper padding"
    - "CardFooter renders footer content with proper padding"
    - "Card sub-components compose into a cohesive container"
  artifacts:
    - path: "packages/core/src/components/card/index.vue"
      provides: "Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter sub-components"
      min_lines: 80
    - path: "packages/core/src/components/card/index.ts"
      provides: "Card exports"
      exports: ["Card", "CardHeader", "CardTitle", "CardDescription", "CardContent", "CardFooter"]
  key_links:
    - from: "packages/core/src/components/card/index.vue"
      to: "packages/core/src/utils.ts"
      via: "cn() import"
      pattern: "import.*cn.*from.*utils"
---

<objective>
Implement Card component with sub-components following the reference React Card pattern.

Purpose: Replace 25 occurrences of inline card container styles across the codebase with a reusable Card component system.

Output: Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter components ready for export from @lionad/core.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@packages/core/src/utils.ts
@packages/theme/src/tokens.css
</context>

<interfaces>
<!-- Key types and contracts from existing code -->

From packages/core/src/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

From packages/theme/src/tokens.css (CSS variables used by Card):
```css
--color-card, --color-card-foreground
--color-border, --color-muted-foreground
--radius-xl
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Card component with all sub-components</name>
  <files>packages/core/src/components/card/index.vue, packages/core/src/components/card/index.ts</files>
  <action>
    Create packages/core/src/components/card/index.vue with all sub-components in a single file using Vue 3 <script setup>:

    All components follow the same pattern:
    - Use <script setup lang="ts">
    - Accept class prop (merged with cn())
    - Forward all remaining attrs via v-bind="$attrs" (inheritAttrs: false)
    - Set data-slot attribute for identification

    Component definitions:

    1. Card: Root container
       - Element: <div>
       - Base classes: "bg-card text-card-foreground flex flex-col gap-6 rounded-xl border py-6 shadow-sm"
       - data-slot="card"
       - defineOptions({ name: 'Card', inheritAttrs: false })

    2. CardHeader: Header section with grid layout
       - Element: <div>
       - Base classes: "grid auto-rows-min grid-rows-[auto_auto] items-start gap-2 px-6 has-data-[slot=card-action]:grid-cols-[1fr_auto] [.border-b]:pb-6"
       - data-slot="card-header"
       - defineOptions({ name: 'CardHeader', inheritAttrs: false })

    3. CardTitle: Title text
       - Element: <div>
       - Base classes: "leading-none font-semibold"
       - data-slot="card-title"
       - defineOptions({ name: 'CardTitle', inheritAttrs: false })

    4. CardDescription: Description text
       - Element: <div>
       - Base classes: "text-muted-foreground text-sm"
       - data-slot="card-description"
       - defineOptions({ name: 'CardDescription', inheritAttrs: false })

    5. CardContent: Main content area
       - Element: <div>
       - Base classes: "px-6"
       - data-slot="card-content"
       - defineOptions({ name: 'CardContent', inheritAttrs: false })

    6. CardFooter: Footer section
       - Element: <div>
       - Base classes: "flex items-center px-6 [.border-t]:pt-6"
       - data-slot="card-footer"
       - defineOptions({ name: 'CardFooter', inheritAttrs: false })

    Each component:
    - Props: { class?: string } (optional, merged via cn())
    - Template: <div :class="cn(baseClasses, props.class)" v-bind="$attrs">
    - Import cn from '../../utils'

    Create packages/core/src/components/card/index.ts:
    Export all 6 sub-components from the barrel file.
    Since Vue 3 SFCs with <script setup> compile to default exports,
    use virtual module queries to export named components:
    ```typescript
    export { default as Card } from './index.vue?component=Card'
    export { default as CardHeader } from './index.vue?component=CardHeader'
    // ... etc for each sub-component
    ```
    
    Alternatively, define each component separately in separate .vue files
    if virtual module queries don't work with the build setup.
  </action>
  <behavior>
    - Test 1: Card renders with base container classes (bg-card, rounded-xl, border, shadow-sm)
    - Test 2: CardHeader renders with grid layout and padding classes
    - Test 3: CardContent renders with horizontal padding only (px-6)
    - Test 4: CardFooter renders with flex layout and horizontal padding
    - Test 5: Custom class prop merges with base classes via cn()
  </behavior>
  <verify>
    <automated>cd packages/core && pnpm typecheck</automated>
    <manual>Verify card/index.vue has 6 sub-components: Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter</manual>
  </verify>
  <done>
    - card/index.vue created with all 6 sub-components
    - card/index.ts exports all 6 sub-components
    - Each sub-component has data-slot attribute for identification
    - TypeScript typecheck passes
  </done>
</task>

</tasks>

<verification>
1. `cd packages/core && pnpm typecheck` passes
2. Card sub-components render with correct Tailwind classes
3. data-slot attributes set on each sub-component
4. cn() used for class merging on all sub-components
</verification>

<success_criteria>
- Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter components created
- Card renders with bg-card, rounded-xl, border, shadow-sm base styles
- Sub-components compose into a cohesive container
- CSS variables from theme are used (card, card-foreground, muted-foreground)
- Components ready for barrel export (export happens in 02-03)
</success_criteria>

<output>
After completion, create `.planning/phases/02-p0-base-components/02-02-SUMMARY.md`
</output>
