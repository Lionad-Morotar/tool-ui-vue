---
phase: 02-p0-base-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/package.json
  - packages/core/src/components/button/index.vue
  - packages/core/src/components/button/index.ts
  - packages/core/src/index.ts
autonomous: true
requirements:
  - BASE-01
  - BASE-02

must_haves:
  truths:
    - "Button renders with different cva variants (default, destructive, outline, secondary, ghost, link)"
    - "Button renders with different sizes (default, sm, lg, icon)"
    - "Button asChild mode allows child element to inherit Button styles without rendering <button> tag"
  artifacts:
    - path: "packages/core/src/components/button/index.vue"
      provides: "Button component with cva variants and asChild support"
      min_lines: 60
    - path: "packages/core/src/components/button/index.ts"
      provides: "Button export and buttonVariants type"
      exports: ["Button", "buttonVariants", "ButtonProps"]
    - path: "packages/core/package.json"
      provides: "cva dependency"
      contains: "class-variance-authority"
  key_links:
    - from: "packages/core/src/components/button/index.vue"
      to: "packages/core/src/utils.ts"
      via: "cn() import"
      pattern: "import.*cn.*from.*utils"
    - from: "packages/core/src/components/button/index.vue"
      to: "class-variance-authority"
      via: "cva import"
      pattern: "import.*cva.*from.*class-variance-authority"
---

<objective>
Implement Button component with cva variant system and asChild/slot pattern support.

Purpose: Replace 17 occurrences of inline button styles across the codebase with a reusable, consistent Button component.

Output: Button component exported from @lionad/core with variants (default/destructive/outline/secondary/ghost/link) and sizes (default/sm/lg/icon).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@packages/core/src/utils.ts
@packages/core/src/index.ts
@packages/core/package.json
@packages/theme/src/tokens.css
</context>

<interfaces>
<!-- Key types and contracts from existing code -->

From packages/core/src/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

From packages/theme/src/tokens.css (CSS variables to use):
```css
--color-primary, --color-primary-foreground
--color-destructive, --color-destructive-foreground
--color-secondary, --color-secondary-foreground
--color-muted, --color-muted-foreground
--color-accent, --color-accent-foreground
--color-background, --color-foreground
--color-border, --color-input, --color-ring
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cva dependency and create Button component</name>
  <files>packages/core/package.json, packages/core/src/components/button/index.vue, packages/core/src/components/button/index.ts</files>
  <action>
    1. Add class-variance-authority to packages/core dependencies:
       - Run: `pnpm add -w class-variance-authority` in packages/core directory
       - Version: ^0.9.0 or latest compatible
    
    2. Create packages/core/src/components/button/index.vue with:
       - Use <script setup lang="ts"> with TypeScript
       - Define ButtonProps interface with:
         - variant: 'default' | 'destructive' | 'outline' | 'secondary' | 'ghost' | 'link' (default: 'default')
         - size: 'default' | 'sm' | 'lg' | 'icon' | 'icon-sm' | 'icon-lg' (default: 'default')
         - asChild: boolean (default: false)
         - className: string (optional, via defineProps)
       - Create buttonVariants using cva() with base classes:
         - Base: "inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium transition-colors disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg:not([class*='size-'])]:size-4 shrink-0 [&_svg]:shrink-0 outline-none focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px] aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive"
         - Variants:
           - default: "bg-primary text-primary-foreground hover:bg-primary/90"
           - destructive: "bg-destructive text-white hover:bg-destructive/90 focus-visible:ring-destructive/20 dark:focus-visible:ring-destructive/40 dark:bg-destructive/60"
           - outline: "border bg-background shadow-xs hover:bg-accent hover:text-accent-foreground dark:bg-input/30 dark:border-input dark:hover:bg-input/50"
           - secondary: "bg-secondary text-secondary-foreground hover:bg-secondary/80"
           - ghost: "hover:bg-accent hover:text-accent-foreground dark:hover:bg-accent/50"
           - link: "text-primary underline-offset-4 hover:underline"
         - Sizes:
           - default: "h-9 px-4 py-2 has-[>svg]:px-3"
           - sm: "h-8 rounded-md gap-1.5 px-3 has-[>svg]:px-2.5"
           - lg: "h-10 rounded-md px-6 has-[>svg]:px-4"
           - icon: "size-9"
           - icon-sm: "size-8"
           - icon-lg: "size-10"
       - Template: Use <component :is="asChild ? 'slot' : 'button'"> pattern for asChild support
         - When asChild=true: render slot content directly, styles applied via provide/inject or v-slot
         - When asChild=false: render <button> element with computed classes
       - Add data-slot="button", data-variant, data-size attributes for debugging/testing
       - Use cn(buttonVariants({ variant, size, class: className })) for class computation
    
    3. Create packages/core/src/components/button/index.ts:
       - Export Button component
       - Export buttonVariants
       - Export ButtonProps type
  </action>
  <behavior>
    - Test 1: Button renders with default variant classes when no variant prop
    - Test 2: Button renders destructive classes when variant="destructive"
    - Test 3: Button renders correct size classes when size="lg"
    - Test 4: Button with asChild=true does NOT render <button> element
    - Test 5: Child element with asChild inherits Button variant classes
  </behavior>
  <verify>
    <automated>cd packages/core && pnpm typecheck</automated>
    <manual>Verify button.vue has cva variants defined for all 6 variants and 6 sizes</manual>
  </verify>
  <done>
    - cva added to package.json dependencies
    - button/index.vue created with all variants, sizes, and asChild support
    - button/index.ts exports Button, buttonVariants, ButtonProps
    - TypeScript typecheck passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Export Button from core barrel file</name>
  <files>packages/core/src/index.ts</files>
  <action>
    Update packages/core/src/index.ts to export Button component:
    
    1. Add re-export for Button component:
       ```typescript
       export { Button, buttonVariants, type ButtonProps } from './components/button';
       ```
    
    2. Ensure the export path is correct (components/button/index.ts barrel)
  </action>
  <verify>
    <automated>cd packages/core && pnpm typecheck</automated>
    <manual>Verify packages/core/src/index.ts exports Button, buttonVariants, ButtonProps</manual>
  </verify>
  <done>
    - Button component exported from @lionad/core
    - buttonVariants exported for consumers who want custom button implementations
    - ButtonProps type exported for TypeScript consumers
  </done>
</task>

</tasks>

<verification>
1. `pnpm add -w class-variance-authority` succeeds without errors
2. `cd packages/core && pnpm typecheck` passes
3. Button component imports cn from utils and cva from class-variance-authority
4. All 6 variants and 6 sizes are defined in buttonVariants
5. asChild pattern works: `<Button asChild><a href="...">Link</a></Button>` renders styled anchor
</verification>

<success_criteria>
- Button component is importable from @lionad/core
- Button renders with correct Tailwind classes for each variant/size combination
- asChild mode allows wrapping any element with Button styles
- TypeScript types are properly exported
</success_criteria>

<output>
After completion, create `.planning/phases/02-p0-base-components/02-01-SUMMARY.md`
</output>
