---
phase: 02-p0-base-components
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - packages/core/src/index.ts
autonomous: true
requirements:
  - BASE-04

must_haves:
  truths:
    - "Button is importable from @lionad/core"
    - "Card sub-components are importable from @lionad/core"
    - "Exports use proper TypeScript types"
  artifacts:
    - path: "packages/core/src/index.ts"
      provides: "Barrel exports for Button and Card components"
      exports: ["Button", "buttonVariants", "ButtonProps", "Card", "CardHeader", "CardTitle", "CardDescription", "CardContent", "CardFooter"]
      min_lines: 10
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/components/button/index.ts"
      via: "re-export"
      pattern: "export.*Button.*from.*components/button"
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/components/card/index.ts"
      via: "re-export"
      pattern: "export.*Card.*from.*components/card"
---

<objective>
Export Button and Card components from @lionad/core barrel file.

Purpose: Make Button and Card components consumable by downstream packages (packages/components and external users).

Output: packages/core/src/index.ts re-exports all Button and Card sub-components with proper TypeScript types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@packages/core/src/index.ts
@packages/core/src/components/button/index.ts
@packages/core/src/components/card/index.ts
</context>

<interfaces>
<!-- Key types from Button and Card components -->

From packages/core/src/components/button/index.ts (after 02-01):
```typescript
export { default as Button } from './index.vue'
export { buttonVariants } from './index.vue'
export type { ButtonProps } from './index.vue'
```

From packages/core/src/components/card/index.ts (after 02-02):
```typescript
export { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update core barrel file with Button and Card exports</name>
  <files>packages/core/src/index.ts</files>
  <action>
    Update packages/core/src/index.ts to export all Button and Card components:

    Replace existing content with:
    ```typescript
    // Re-export cn utility for downstream packages
    export { cn } from './utils';

    // Re-export Button component and variants
    export { Button, buttonVariants } from './components/button';
    export type { ButtonProps } from './components/button';

    // Re-export Card sub-components
    export {
      Card,
      CardHeader,
      CardTitle,
      CardDescription,
      CardContent,
      CardFooter,
    } from './components/card';
    ```

    Ensure:
    - Named exports match what's exported from component barrel files
    - Type exports use `export type {}` syntax
    - Import paths are correct (relative to src/index.ts)
  </action>
  <verify>
    <automated>cd packages/core && pnpm typecheck</automated>
    <manual>Verify packages/core/src/index.ts exports: cn, Button, buttonVariants, ButtonProps, Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter</manual>
  </verify>
  <done>
    - All Button exports added (component, variants, props type)
    - All 6 Card sub-component exports added
    - cn utility still exported
    - TypeScript typecheck passes with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify end-to-end build and consumption</name>
  <files>packages/core/src/index.ts</files>
  <action>
    Verify the complete build chain works:

    1. Run full monorepo build:
       ```bash
       cd /Users/lionad/Github/Lionad-Morotar/tool-ui-vue
       pnpm -r build
       ```
       Expected output:
       - theme builds successfully (tokens.css generated)
       - core builds successfully (dist/index.js, dist/index.cjs, dist/index.d.ts)
       - components builds successfully

    2. Run full monorepo typecheck:
       ```bash
       pnpm -r typecheck
       ```
       Expected: All 3 packages pass

    3. Verify dist output contains expected exports:
       ```bash
       cat packages/core/dist/index.d.ts | head -30
       ```
       Should show Button, Card, etc. type declarations

    4. Optional: Create a test import in packages/components to verify consumption:
       ```typescript
       // packages/components/src/test-import.ts
       import { Button, Card } from '@lionad/core';
       // Should compile without errors
       ```
  </action>
  <verify>
    <automated>pnpm -r build && pnpm -r typecheck</automated>
    <manual>Verify packages/core/dist/index.d.ts contains Button, Card type declarations</manual>
  </verify>
  <done>
    - pnpm -r build completes without errors
    - pnpm -r typecheck passes for all 3 packages
    - dist/index.d.ts contains type declarations for Button and Card components
    - Downstream packages can import from @lionad/core
  </done>
</task>

</tasks>

<verification>
1. packages/core/src/index.ts exports all 10 items: cn, Button, buttonVariants, ButtonProps, Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter
2. pnpm -r build succeeds
3. pnpm -r typecheck succeeds
4. packages/core/dist/index.d.ts contains proper type declarations
</verification>

<success_criteria>
- BASE-04 requirement satisfied: Button and Card exported from @lionad/core
- Downstream packages (packages/components) can import and use Button and Card
- TypeScript consumers get full IntelliSense support
- Build output includes ESM and CJS formats
</success_criteria>

<output>
After completion, create `.planning/phases/02-p0-base-components/02-03-SUMMARY.md`
</output>
