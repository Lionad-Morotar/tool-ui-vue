# Playground Dev Notes

此目录包含 `playground/` —— 一个**纯辅助开发和测试时使用的页面**。

- **用途**：本地快速验证组件行为、调试单组件状态、尝试临时改动。
- **非正式文档**：正式组件展示、API 说明、可交互 demo 请访问 Histoire 文档站。
- **入口**：`pnpm dev` 启动后默认打开此页面（端口 5740）。
- **文档站**：`pnpm story:dev` 启动 Histoire（端口 5741）。
